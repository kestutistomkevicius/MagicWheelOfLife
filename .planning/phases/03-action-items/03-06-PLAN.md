---
phase: 03-action-items
plan: 06
type: execute
wave: 5
depends_on: [03-05]
files_modified: []
autonomous: false
requirements: [ACTION-01, ACTION-02, ACTION-03, ACTION-04]

must_haves:
  truths:
    - "User can expand a category and see its action items"
    - "User can add an action item (free text, Enter to confirm)"
    - "Add button is absent when 7 items are present"
    - "User can set a deadline date on an item and see it displayed"
    - "User can check an item complete and see it visually struck through"
    - "User can delete an item and it disappears immediately"
    - "All action item changes persist across page refresh"
  artifacts: []
  key_links: []
---

<objective>
Human verification checkpoint: confirm all ACTION-01..04 flows work end-to-end in the running application before marking Phase 3 complete.

Purpose: Unit tests verify behavior in isolation; this checkpoint verifies the integrated experience — Supabase RLS, real data, browser rendering.
Output: Human confirmation that all four action item requirements are working, or a list of issues to fix.
</objective>

<execution_context>
@C:/Users/KestutisTomkevicius/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/KestutisTomkevicius/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-action-items/03-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 3 complete implementation:
    - action_items DB table with RLS (migration 20260315000001)
    - useActionItems hook (load, add, toggle, setDeadline, delete)
    - ActionItemList component with optimistic UI
    - WheelPage expand/collapse per category with lazy loading
    - CategorySlider rename UX debt resolved
    - Seed data: 6 action items for free@test.com (Health/Career/Finance), 5 for premium@test.com
  </what-built>
  <how-to-verify>
    Prerequisites:
    1. Run `supabase start` (if not already running)
    2. Run `supabase db reset` to apply migration and seed
    3. Run `npm run dev`
    4. Open http://localhost:5173

    Sign in as free@test.com / testpass123.

    **Test 1 — Automated suite (run first):**
    - Run `npm test` and confirm all tests pass.

    **Test 2 — Add up to 7 items (ACTION-01):**
    - Open the wheel page. Click the expand toggle (▼) next to the Health category.
    - Verify: existing seed action items appear (Run 3x per week, Sleep before midnight, Schedule annual checkup).
    - Click "+ Add action item", type "Drink more water", press Enter.
    - Verify: item appears in the list immediately.
    - Add 4 more items to reach 7 total.
    - Verify: the "+ Add action item" button is no longer visible.

    **Test 3 — Optional deadline (ACTION-02):**
    - On any action item, click the date input field.
    - Select a date (e.g., tomorrow's date).
    - Verify: the date appears in the input field in the item row.
    - Refresh the page (F5), re-expand Health.
    - Verify: the deadline is still there (persisted to DB).

    **Test 4 — Mark complete (ACTION-03):**
    - Click the checkbox on an open action item.
    - Verify: the item text becomes struck through immediately.
    - Refresh, re-expand the category.
    - Verify: the item is still shown as checked (persisted).

    **Test 5 — Delete item (ACTION-04):**
    - Click the × button on any action item.
    - Verify: the item disappears from the list immediately (optimistic removal).
    - Refresh, re-expand the category.
    - Verify: the item is gone (persisted delete).

    **Test 6 — Rename inline edit:**
    - Click the "Rename" button on any category slider.
    - Verify: an inline text input appears (not an instant rename).
    - Press Escape to cancel.
  </how-to-verify>
  <resume-signal>Type "approved" if all flows work correctly, or describe any issues found (e.g., "deadline not persisting", "add button still visible at 7")</resume-signal>
</task>

</tasks>

<verification>
Human sign-off on all four ACTION-0* requirement behaviors in the running app.
</verification>

<success_criteria>
1. ACTION-01: Can add items; blocked at 7
2. ACTION-02: Deadline date saves and persists
3. ACTION-03: Checkbox complete state saves and persists; line-through visible
4. ACTION-04: Delete is immediate and persists
5. `npm test` exits 0 (full suite green)
6. Human types "approved"
</success_criteria>

<output>
After completion, create `.planning/phases/03-action-items/03-06-SUMMARY.md`
</output>
