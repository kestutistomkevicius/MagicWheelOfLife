---
phase: 03-action-items
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/database.ts
  - src/hooks/useActionItems.test.ts
  - src/components/ActionItemList.test.tsx
  - src/components/ui/checkbox.tsx
autonomous: true
requirements: [ACTION-01, ACTION-02, ACTION-03, ACTION-04]

must_haves:
  truths:
    - "ActionItemRow type is exported and used by hook and component files"
    - "Test stubs exist for all ACTION-01..04 behaviors (it.todo stubs pass)"
    - "shadcn Checkbox component file exists at src/components/ui/checkbox.tsx"
  artifacts:
    - path: "src/types/database.ts"
      provides: "ActionItemRow type + Database table entry for action_items"
      contains: "ActionItemRow"
    - path: "src/hooks/useActionItems.test.ts"
      provides: "Test stubs for hook behaviors"
      contains: "it.todo"
    - path: "src/components/ActionItemList.test.tsx"
      provides: "Test stubs for component behaviors"
      contains: "it.todo"
    - path: "src/components/ui/checkbox.tsx"
      provides: "shadcn Checkbox component"
  key_links:
    - from: "src/hooks/useActionItems.test.ts"
      to: "src/hooks/useActionItems.ts"
      via: "import (file doesn't exist yet — stubs verify against future implementation)"
    - from: "src/components/ActionItemList.test.tsx"
      to: "src/components/ActionItemList.tsx"
      via: "import (file doesn't exist yet — stubs verify against future implementation)"
---

<objective>
Wave 0 prerequisites for Phase 3: install shadcn Checkbox, add ActionItemRow type to database.ts, and create test stub files for useActionItems and ActionItemList.

Purpose: Nyquist compliance — every subsequent plan's <verify> command must run green immediately. Stubs use it.todo() so the test runner reports them as acknowledged but not failing.
Output: checkbox.tsx installed, ActionItemRow type exported, two test files with it.todo stubs covering all ACTION-01..04 behaviors.
</objective>

<execution_context>
@C:/Users/KestutisTomkevicius/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/KestutisTomkevicius/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-action-items/03-RESEARCH.md
@.planning/phases/03-action-items/03-VALIDATION.md

<interfaces>
<!-- Existing types in src/types/database.ts (to be extended, not replaced) -->
From src/types/database.ts:
```typescript
export type CategoryRow = {
  id: string
  wheel_id: string
  user_id: string
  name: string
  position: number
  score_asis: number
  score_tobe: number
  created_at: string
  updated_at: string
}

export type Database = {
  public: {
    Tables: {
      categories: {
        Row: CategoryRow
        Insert: Omit<CategoryRow, 'id' | 'created_at' | 'updated_at'>
        Update: Partial<Pick<CategoryRow, 'name' | 'position' | 'score_asis' | 'score_tobe' | 'updated_at'>>
        Relationships: []
      }
      // ... profiles, wheels also present
    }
  }
}
```

<!-- Existing CategorySlider test mock pattern (to mirror in ActionItemList.test.tsx) -->
From src/components/CategorySlider.test.tsx:
```typescript
vi.mock('@/components/ui/slider', () => ({ ... }))
// Pattern: mock heavy Radix components to native HTML equivalents for jsdom
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ActionItemRow type and install shadcn Checkbox</name>
  <files>src/types/database.ts, src/components/ui/checkbox.tsx</files>
  <action>
    Step A — Install shadcn Checkbox by running:
    ```
    npx shadcn@latest add checkbox
    ```
    This creates src/components/ui/checkbox.tsx. If the CLI prompts about existing config, accept defaults.

    Step B — Update src/types/database.ts by adding the ActionItemRow type and the action_items table entry to the Database type. Append AFTER the existing types (do not replace anything):

    Add the standalone type:
    ```typescript
    export type ActionItemRow = {
      id: string
      category_id: string
      user_id: string
      text: string
      is_complete: boolean
      deadline: string | null  // 'YYYY-MM-DD' or null
      position: number
      created_at: string
      updated_at: string
    }
    ```

    Add to the Database.public.Tables object alongside existing tables:
    ```typescript
    action_items: {
      Row: ActionItemRow
      Insert: Omit<ActionItemRow, 'id' | 'created_at' | 'updated_at'>
      Update: Partial<Pick<ActionItemRow, 'text' | 'is_complete' | 'deadline' | 'position' | 'updated_at'>>
      Relationships: []
    }
    ```

    Do NOT modify ProfileRow, WheelRow, CategoryRow, or the existing table entries.
  </action>
  <verify>
    <automated>npm run build -- --noEmit 2>/dev/null || npx tsc --noEmit</automated>
  </verify>
  <done>src/types/database.ts exports ActionItemRow; src/components/ui/checkbox.tsx exists; TypeScript compiles without errors</done>
</task>

<task type="auto">
  <name>Task 2: Create Wave 0 test stubs for useActionItems and ActionItemList</name>
  <files>src/hooks/useActionItems.test.ts, src/components/ActionItemList.test.tsx</files>
  <action>
    Create src/hooks/useActionItems.test.ts with it.todo stubs covering all hook behaviors. The import of useActionItems will fail until Plan 03 creates the file — use a try/catch around the import or structure tests so the describe block is always valid. Best approach: import the file path in a comment and leave the implementation import commented out, using only it.todo stubs that don't require the actual module.

    Use this exact structure:
    ```typescript
    // src/hooks/useActionItems.test.ts
    // Wave 0: stubs only. Implementation in 03-03-PLAN.md.
    import { describe, it } from 'vitest'

    describe('useActionItems', () => {
      describe('addActionItem', () => {
        it.todo('returns error when currentCount >= 7 (ACTION-01 limit)')
        it.todo('returns ActionItemRow on successful insert (ACTION-01)')
      })

      describe('setDeadline', () => {
        it.todo('sends null to Supabase when value is empty string (ACTION-02 pitfall)')
        it.todo('sends ISO date string when value is set (ACTION-02)')
      })

      describe('toggleActionItem', () => {
        it.todo('calls Supabase update with negated is_complete value (ACTION-03)')
      })

      describe('deleteActionItem', () => {
        it.todo('calls Supabase DELETE with the correct item id (ACTION-04)')
      })
    })
    ```

    Create src/components/ActionItemList.test.tsx with it.todo stubs covering all component behaviors:
    ```typescript
    // src/components/ActionItemList.test.tsx
    // Wave 0: stubs only. Implementation in 03-04-PLAN.md.
    import { describe, it } from 'vitest'

    describe('ActionItemList', () => {
      describe('ACTION-01: add item', () => {
        it.todo('renders "+ Add action item" button when item count < 7')
        it.todo('does NOT render add button when item count = 7')
        it.todo('adds item to list after pressing Enter in input')
      })

      describe('ACTION-02: deadline', () => {
        it.todo('renders date input for each item')
        it.todo('shows empty date input when deadline is null')
        it.todo('shows ISO date value when deadline is set')
      })

      describe('ACTION-03: complete toggle', () => {
        it.todo('renders Checkbox checked when item.is_complete is true')
        it.todo('item text has line-through class when is_complete is true')
        it.todo('calls toggleActionItem when Checkbox is clicked')
      })

      describe('ACTION-04: delete', () => {
        it.todo('removes item from list immediately on delete click (optimistic)')
        it.todo('calls deleteActionItem with correct id')
      })
    })
    ```
  </action>
  <verify>
    <automated>npm test -- --run src/hooks/useActionItems.test.ts src/components/ActionItemList.test.tsx</automated>
  </verify>
  <done>Both test files run without errors; all cases show as "todo" in Vitest output (not failing, not skipped as errors)</done>
</task>

</tasks>

<verification>
- `npm test -- --run` passes with all tests green or todo
- `npx tsc --noEmit` passes (ActionItemRow type correctly structured)
- `src/components/ui/checkbox.tsx` exists (created by shadcn CLI)
- No existing tests broken
</verification>

<success_criteria>
1. ActionItemRow exported from src/types/database.ts with all 9 fields
2. action_items entry present in Database.public.Tables
3. src/components/ui/checkbox.tsx exists
4. src/hooks/useActionItems.test.ts has 6 it.todo stubs across 4 describe blocks
5. src/components/ActionItemList.test.tsx has 10 it.todo stubs across 4 describe blocks
6. `npm test -- --run` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/03-action-items/03-01-SUMMARY.md`
</output>
