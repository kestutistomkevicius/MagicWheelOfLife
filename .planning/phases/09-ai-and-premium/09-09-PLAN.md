---
phase: 09-ai-and-premium
plan: 09
type: execute
wave: 4
depends_on:
  - 09-06
  - 09-07
  - 09-08
  - 09-03
files_modified:
  - src/components/CategorySlider.tsx
  - src/components/CategorySlider.test.tsx
  - src/pages/WheelPage.tsx
  - src/pages/SettingsPage.tsx
  - src/pages/SettingsPage.test.tsx
autonomous: true
requirements:
  - AI-01
  - PREMIUM-01
  - PREMIUM-02
must_haves:
  truths:
    - "AI Coach button appears in CategorySlider header row next to the star icon"
    - "Premium user clicking AI Coach opens AiCoachDrawer for that category"
    - "Free user clicking AI Coach sees the upgrade modal"
    - "Applying a score from the drawer updates the WheelPage slider immediately"
    - "WheelChart receives color props derived from PaletteContext on WheelPage"
    - "SettingsPage ColorSchemePicker appears below the Plan section"
    - "Clicking a swatch on SettingsPage calls updateColorScheme and applyPalette"
    - "Tier toggle visible when import.meta.env.DEV OR VITE_SHOW_TIER_TOGGLE === 'true'"
  artifacts:
    - path: src/components/CategorySlider.tsx
      provides: "onAiCoach prop + AI Coach button in header row"
    - path: src/pages/WheelPage.tsx
      provides: "AiCoachDrawer state + selectedCategory + palette-derived WheelChart color props"
    - path: src/pages/SettingsPage.tsx
      provides: "ColorSchemePicker wired to useProfile.updateColorScheme + PREMIUM-01 toggle fix"
  key_links:
    - from: "WheelPage.tsx"
      to: "AiCoachDrawer"
      via: "drawerOpen state + selectedCategoryId"
      pattern: "AiCoachDrawer"
    - from: "WheelPage.tsx"
      to: "WheelChart color props"
      via: "usePalette() + PALETTES[currentPalette]"
      pattern: "primaryColor|usePalette"
    - from: "SettingsPage.tsx"
      to: "ColorSchemePicker + updateColorScheme"
      via: "onSelect={name => { void updateColorScheme(name); applyPalette(name) }}"
      pattern: "ColorSchemePicker|updateColorScheme"
---

<objective>
Wire up all three tracks into the existing pages: add AI Coach button to CategorySlider, manage AiCoachDrawer state in WheelPage (including score apply), pass palette-derived colors to WheelChart, insert ColorSchemePicker into SettingsPage, and fix the PREMIUM-01 tier toggle condition.

Purpose: Integration plan — connects all previously built components and hooks into user-visible flows.
Output: Updated CategorySlider, WheelPage, SettingsPage — all three requirements wired end-to-end.
</objective>

<execution_context>
@C:/Users/KestutisTomkevicius/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/KestutisTomkevicius/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-ai-and-premium/09-CONTEXT.md
@.planning/phases/09-ai-and-premium/09-RESEARCH.md
@src/components/CategorySlider.tsx
@src/components/CategorySlider.test.tsx
@src/pages/WheelPage.tsx
@src/pages/SettingsPage.tsx
@src/pages/SettingsPage.test.tsx
@src/contexts/PaletteContext.tsx

<interfaces>
<!-- CategorySlider new props (add to existing interface): -->
onAiCoach?: () => void
isPremiumForAi?: boolean   // drives free-user upgrade modal for AI Coach

<!-- WheelPage existing state (from prior phase summaries): -->
<!-- categories, localCategories, expandedCategories, actionItemsByCategory, etc. -->
<!-- Add: drawerOpen: boolean, drawerCategoryId: string | null -->

<!-- usePalette() from PaletteContext returns: { currentPalette: string, applyPalette: (name: string) => void } -->
<!-- PALETTES[currentPalette] gives the hex map with --palette-primary, --palette-secondary, etc. -->
<!-- WheelChart expects: primaryColor, secondaryColor, importantColor, highlightColor -->

<!-- SettingsPage existing: -->
<!-- tier, avatarUrl, loading, updateAvatar, updateTier from useProfile -->
<!-- Add: colorScheme, updateColorScheme from useProfile (after Plan 04) -->
<!-- TODO comment location: "TODO Phase 9: color scheme selector (PREMIUM-02)" -->

<!-- PREMIUM-01 current condition (line 52 in SettingsPage): -->
<!-- {import.meta.env.DEV && ( -->
<!-- Change to: -->
<!-- {(import.meta.env.DEV || import.meta.env.VITE_SHOW_TIER_TOGGLE === 'true') && ( -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CategorySlider AI Coach button + WheelPage drawer wiring</name>
  <files>src/components/CategorySlider.tsx, src/components/CategorySlider.test.tsx, src/pages/WheelPage.tsx</files>
  <behavior>
    - Test: CategorySlider renders an "AI Coach" button in the header row when onAiCoach is provided
    - Test: clicking AI Coach calls onAiCoach when isPremiumForAi=true
    - Test: clicking AI Coach does NOT call onAiCoach when isPremiumForAi=false; shows upgrade modal instead
    - Test: upgrade modal (free AI lock) has a dismiss/close button
  </behavior>
  <action>
**CategorySlider.tsx:**

Read the file. Add to `CategorySliderProps`:
```typescript
onAiCoach?: () => void
isPremiumForAi?: boolean
```

In the header row (next to the star icon), add an AI Coach button:
- Icon: lucide `BrainCircuit` or `Sparkles` (Claude's discretion — pick whichever is more visually descriptive)
- Button: `type="button"`, `aria-label="AI Coach"`
- If `isPremiumForAi`: onClick calls `onAiCoach?.()`
- If `!isPremiumForAi`: onClick shows a local upgrade modal (same plain Tailwind pattern as star feature)
- Only render the button when `onAiCoach` is defined (not undefined) so existing tests without the prop are unaffected

Add upgrade modal state `showAiUpgrade: boolean` for the free-user case.

**WheelPage.tsx:**

Read the entire file before editing. Add:
1. State: `drawerCategoryId: string | null = null`
2. Derive `drawerOpen = drawerCategoryId !== null`
3. In the `isPremium` computation (from `useWheel` or `useProfile`): pass to `isPremiumForAi` on each `CategorySlider`
4. On each `CategorySlider`, add:
   ```tsx
   onAiCoach={() => setDrawerCategoryId(category.id)}
   isPremiumForAi={isPremium}
   ```
5. Render `AiCoachDrawer` when `drawerCategoryId !== null`:
   - Find the selected category from `localCategories`
   - Pass `categoryId, categoryName, asisScore, tobeScore`
   - `onApplyAsis={(v) => { /* call the same handler as slider commit for as-is */ }}`
   - `onApplyTobe={(v) => { /* call the same handler as slider commit for to-be */ }}`
   - `onClose={() => setDrawerCategoryId(null)}`
6. Pass palette colors to WheelChart:
   - Import `usePalette` and `PALETTES`
   - `const { currentPalette } = usePalette()`
   - `const paletteVars = PALETTES[currentPalette] ?? PALETTES.amber`
   - Pass to WheelChart: `primaryColor={paletteVars['--palette-primary']} secondaryColor={paletteVars['--palette-secondary']} importantColor={paletteVars['--palette-important']} highlightColor={paletteVars['--palette-highlight']}`

**Tests:** Extend `CategorySlider.test.tsx` — add new tests for onAiCoach behavior; do NOT remove existing tests.
  </action>
  <verify>
    <automated>npm test -- --run src/components/CategorySlider.test.tsx</automated>
  </verify>
  <done>CategorySlider tests pass including new AI Coach tests. WheelPage compiles. npm run build exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SettingsPage — ColorSchemePicker + PREMIUM-01 tier toggle fix</name>
  <files>src/pages/SettingsPage.tsx, src/pages/SettingsPage.test.tsx</files>
  <behavior>
    - Test: ColorSchemePicker renders inside SettingsPage
    - Test: clicking a swatch calls updateColorScheme with the palette name
    - Test: tier toggle rendered when VITE_SHOW_TIER_TOGGLE is 'true' (import.meta.env mock)
    - Test: tier toggle NOT rendered when neither DEV nor VITE_SHOW_TIER_TOGGLE is set
  </behavior>
  <action>
**SettingsPage.tsx:**

Read the file. Make these changes:

1. Import `ColorSchemePicker` from `@/components/ColorSchemePicker`
2. Import `usePalette` from `@/contexts/PaletteContext`
3. Destructure `colorScheme, updateColorScheme` from `useProfile(userId)` (already extended in Plan 04)
4. Destructure `applyPalette` from `usePalette()`
5. At the `/* TODO Phase 9: color scheme selector (PREMIUM-02) */` comment, replace with:
   ```tsx
   <section>
     <h2 className="text-sm font-medium text-stone-500 mb-3">Color scheme</h2>
     <ColorSchemePicker
       currentPalette={colorScheme}
       isPremium={tier === 'premium'}
       onSelect={(name) => {
         applyPalette(name)
         void updateColorScheme(name)
       }}
     />
   </section>
   ```
6. Fix PREMIUM-01 tier toggle condition (line ~52):
   - Find: `{import.meta.env.DEV && (`
   - Replace: `{(import.meta.env.DEV || import.meta.env.VITE_SHOW_TIER_TOGGLE === 'true') && (`

**SettingsPage.test.tsx:**

Read the existing test file. Add new tests:
- Mock `useProfile` to return `colorScheme: 'amber'` and `updateColorScheme: vi.fn()`
- Mock `usePalette` to return `{ currentPalette: 'amber', applyPalette: vi.fn() }`
- Mock `ColorSchemePicker` with `vi.mock('@/components/ColorSchemePicker')` — render a simple button
- Test: ColorSchemePicker is rendered with currentPalette='amber'
- Test: clicking a swatch calls updateColorScheme (via mock ColorSchemePicker)
- For PREMIUM-01: mock `import.meta.env.VITE_SHOW_TIER_TOGGLE` using `vi.stubEnv` or `vi.mock` on import.meta — test toggle visibility

Do not remove any existing SettingsPage tests.
  </action>
  <verify>
    <automated>npm test -- --run src/pages/SettingsPage.test.tsx</automated>
  </verify>
  <done>All SettingsPage tests pass including new ColorSchemePicker and PREMIUM-01 tests. npm test -- --run exits 0 for full suite.</done>
</task>

</tasks>

<verification>
`npm test -- --run` — full suite green.
`npm run build` — exits 0.
All three requirements wired: AI drawer accessible from CategorySlider, palette picker in SettingsPage, tier toggle visible in dev/staging.
</verification>

<success_criteria>
- AI Coach button visible in CategorySlider header row
- WheelPage renders AiCoachDrawer when drawer category selected; applying score updates the wheel immediately
- WheelChart receives palette-derived color props from PaletteContext
- ColorSchemePicker in SettingsPage calls updateColorScheme + applyPalette on swatch click
- Tier toggle condition updated to `DEV || VITE_SHOW_TIER_TOGGLE === 'true'`
- Full test suite green, build exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/09-ai-and-premium/09-09-SUMMARY.md`
</output>
