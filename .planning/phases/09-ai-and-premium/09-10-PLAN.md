---
phase: 09-ai-and-premium
plan: 10
type: execute
wave: 5
depends_on:
  - 09-09
files_modified: []
autonomous: false
requirements:
  - AI-01
  - PREMIUM-01
  - PREMIUM-02
must_haves:
  truths:
    - "Premium user can open AI Coach drawer from any category, send a message, and see streaming response"
    - "AI Coach proposes scores after conversation; user can apply each independently"
    - "Conversation history persists — reopening the drawer restores prior messages"
    - "Premium user can switch palettes in Settings; wheel chart AND sidebar update immediately"
    - "Palette persists across page reload"
    - "Free user sees upgrade modal for AI Coach and locked swatches for non-amber palettes"
    - "Tier toggle visible in dev environment; hidden in standard build"
  artifacts:
    - path: supabase/functions/ai-coach/index.ts
      provides: "Live Edge Function serving premium AI Coach"
    - path: src/components/AiCoachDrawer.tsx
      provides: "Verified drawer component"
    - path: src/contexts/PaletteContext.tsx
      provides: "Verified palette system"
  key_links:
    - from: "CategorySlider AI Coach button"
      to: "AiCoachDrawer + Edge Function"
      via: "WheelPage drawer state + useAiChat fetch"
      pattern: "end-to-end AI flow"
    - from: "SettingsPage ColorSchemePicker"
      to: "WheelChart + Sidebar"
      via: "PaletteContext applyPalette"
      pattern: "end-to-end palette flow"
---

<objective>
Human verification of all Phase 9 features end-to-end. Run automated suite first, then manually verify the three flows that require live Supabase + Anthropic connections: AI coaching chat, palette switching, and tier toggle visibility.

Purpose: Gate before phase completion — confirm all AI-01, PREMIUM-01, PREMIUM-02 behaviors work in the browser.
Output: Phase 9 verified and ready for merge to master.
</objective>

<execution_context>
@C:/Users/KestutisTomkevicius/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/KestutisTomkevicius/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-ai-and-premium/09-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated pre-flight checks</name>
  <files></files>
  <action>
Before human verification, run automated checks and ensure all pass:

1. Full test suite:
   ```bash
   npm test -- --run
   ```
   All tests must pass with zero failures.

2. Production build:
   ```bash
   npm run build
   ```
   Must exit 0 with no TypeScript errors.

3. Start Supabase (from memory note — must run before any manual testing):
   ```bash
   npx supabase start
   ```

4. Start dev server:
   ```bash
   npm run dev
   ```

5. Set Anthropic secrets (required for AI Coach to work locally):
   ```bash
   supabase secrets set ANTHROPIC_API_KEY=sk-ant-YOUR_KEY_HERE
   supabase secrets set ANTHROPIC_MODEL=claude-haiku-4-5-20251001
   ```

6. Start Edge Function locally:
   ```bash
   supabase functions serve ai-coach
   ```
   (Keep running in a separate terminal)

Report results to the user.
  </action>
  <verify>
    <automated>npm test -- --run && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>All automated checks pass. Dev server running. Supabase running. Edge Function serving locally.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 9 complete implementation:
    - AI Coach chat drawer (AI-01): per-category coaching, streaming responses, score proposal card with apply buttons, conversation history persistence
    - Color palette system (PREMIUM-02): 4 palettes, swatch grid in Settings, full-app theme update including WheelChart and Sidebar
    - Tier toggle visibility fix (PREMIUM-01): dev + VITE_SHOW_TIER_TOGGLE staging support
  </what-built>
  <how-to-verify>
Open http://localhost:5173 and log in. For AI Coach flows, log in as the PREMIUM dev user (seed data). For free-user flows, log in as the FREE dev user.

**AI-01: AI Coach Chat**

With premium user, open the Wheel page:
1. Click "AI Coach" button in any category row — confirm drawer slides in from the right
2. Confirm the category name appears in the drawer header
3. If prior history exists, confirm messages are restored; if new, the AI sends an opening message
4. Type a response and send — confirm streaming tokens appear incrementally in the assistant bubble
5. Continue the conversation until AI proposes scores — confirm proposal card appears pinned at the bottom with "Suggested scores: As-Is: X · To-Be: Y"
6. Click "Apply to As-Is" — confirm the category's as-is slider updates immediately in the wheel (no page reload)
7. Click "Apply to To-Be" — confirm the to-be slider updates immediately
8. Close the drawer; reopen for the same category — confirm prior messages are restored (history persisted)

With FREE dev user:
9. Click "AI Coach" button — confirm upgrade modal appears instead of the drawer
10. Confirm no API call is made to the Edge Function

**PREMIUM-02: Color Palettes**

With premium user, open Settings:
11. Confirm color scheme section appears below the Plan section
12. Confirm 4 circular swatches are visible (Amber, Ocean, Forest, Rose)
13. Click "Ocean" swatch — confirm: wheel chart colors change immediately (As-Is becomes teal-blue, To-Be becomes indigo), sidebar background updates to dark blue
14. Navigate to Wheel page — confirm WheelChart uses ocean colors
15. Reload the page — confirm ocean palette is still applied (not reverted to amber)
16. Return to Settings, click "Amber" — confirm everything reverts to original amber/brown

With FREE dev user:
17. Open Settings — confirm Ocean/Forest/Rose swatches have lock overlays
18. Click a locked swatch — confirm upgrade modal appears

**PREMIUM-01: Tier Toggle Visibility**

19. With dev server running (`npm run dev`): go to Settings, confirm the tier toggle section is visible
20. Toggle tier from Free to Premium — confirm tier-gated features respond immediately (AI Coach button appears/disappears on WheelPage without reload)
  </how-to-verify>
  <resume-signal>Type "verified" if all flows work, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Human confirms all 20 verification steps pass.
</verification>

<success_criteria>
- AI Coach drawer opens for premium users; free users see upgrade modal
- Streaming responses render token by token; proposal card appears with both Apply buttons functional
- Conversation history restored on drawer reopen
- Palette switch updates WheelChart colors AND sidebar background immediately
- Palette persists across page reload
- Free users see locked swatches with upgrade modal
- Tier toggle visible in dev; hidden when neither DEV nor VITE_SHOW_TIER_TOGGLE is set
- All 20 manual verification steps pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-ai-and-premium/09-10-SUMMARY.md`
</output>
