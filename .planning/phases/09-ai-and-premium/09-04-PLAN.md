---
phase: 09-ai-and-premium
plan: 04
type: execute
wave: 2
depends_on:
  - 09-01
  - 09-02
files_modified:
  - src/contexts/PaletteContext.tsx
  - src/hooks/useProfile.ts
  - src/hooks/useProfile.test.ts
  - tailwind.config.ts
autonomous: true
requirements:
  - PREMIUM-02
must_haves:
  truths:
    - "applyPalette sets all five CSS custom properties on document.documentElement"
    - "PaletteContext provides current palette name and applyPalette to the component tree"
    - "useProfile exposes colorScheme and updateColorScheme"
    - "Tailwind config has palette.* tokens mapped to CSS custom properties"
  artifacts:
    - path: src/contexts/PaletteContext.tsx
      provides: "PALETTES map, applyPalette(), PaletteProvider, usePalette hook"
      exports:
        - PaletteProvider
        - usePalette
        - applyPalette
        - PALETTES
    - path: src/hooks/useProfile.ts
      provides: "colorScheme state + updateColorScheme mutation alongside existing tier/avatar"
    - path: tailwind.config.ts
      provides: "palette.primary/secondary/accent/important/highlight tokens as var() references"
  key_links:
    - from: "PaletteContext.tsx"
      to: "document.documentElement.style.setProperty"
      via: "applyPalette()"
      pattern: "setProperty.*--palette-"
    - from: "useProfile.ts"
      to: "profiles.color_scheme"
      via: "Supabase .update({ color_scheme })"
      pattern: "color_scheme"
---

<objective>
Build the palette system foundation: the PaletteContext (holds palette definitions, applies CSS vars to :root, provides to component tree) and extend useProfile to read/write color_scheme from the profiles table. Update tailwind.config.ts with palette tokens that reference the CSS custom properties.

Purpose: Enables Plans 07 (ColorSchemePicker UI) and 08 (WheelChart/Sidebar palette application) to consume a stable palette API.
Output: PaletteContext.tsx, extended useProfile, updated tailwind.config.ts.
</objective>

<execution_context>
@C:/Users/KestutisTomkevicius/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/KestutisTomkevicius/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-ai-and-premium/09-CONTEXT.md
@.planning/phases/09-ai-and-premium/09-RESEARCH.md
@src/hooks/useProfile.ts
@src/hooks/useProfile.test.ts
@tailwind.config.ts

<interfaces>
<!-- Existing useProfile (from Plan 02 summary context): -->
export interface UseProfileResult {
  tier: 'free' | 'premium'
  avatarUrl: string | null
  loading: boolean
  updateAvatar: (file: File) => Promise<void>
  updateTier: (newTier: 'free' | 'premium') => Promise<void>
}
<!-- extend with: colorScheme: string, updateColorScheme: (name: string) => Promise<void> -->

<!-- Current WheelChart hardcoded hex values (to be replaced by palette props in Plan 08): -->
<!-- As-Is: '#e8a23a', To-Be: '#60a5fa', important: '#b45309', highlight: '#fbbf24' -->

<!-- Amber palette must preserve these exact hex values (no visual regression) -->

<!-- CRITICAL from RESEARCH.md pitfall 5: -->
<!-- Apply palette eagerly from localStorage BEFORE React render to avoid FOUC -->
<!-- Strategy: store palette name in localStorage as fast path; PaletteProvider reads it synchronously -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PaletteContext + tailwind.config.ts palette tokens</name>
  <files>src/contexts/PaletteContext.tsx, tailwind.config.ts, src/contexts/PaletteContext.test.tsx</files>
  <behavior>
    - Test: applyPalette('amber') sets --palette-primary to '#e8a23a' on document.documentElement
    - Test: applyPalette('amber') sets --palette-secondary to '#60a5fa'
    - Test: applyPalette('amber') sets --palette-accent to '#292524'
    - Test: applyPalette('amber') sets --palette-important to '#b45309'
    - Test: applyPalette('amber') sets --palette-highlight to '#fbbf24'
    - Test: applyPalette('ocean') sets --palette-primary to '#0ea5e9'
    - Test: applyPalette with unknown name falls back to amber palette values
    - Test: usePalette() returns current palette name and applyPalette function
    - Test: PaletteProvider reads localStorage 'palette' key on mount and applies it
    - Test: PaletteProvider falls back to colorScheme prop when localStorage has no entry
  </behavior>
  <action>
Create `src/contexts/PaletteContext.tsx`:

Define PALETTES constant with four palettes (amber, ocean, forest, rose) using the exact hex values from RESEARCH.md. Amber must use the exact current WheelChart hex values.

`applyPalette(name: string)` — reads PALETTES[name] ?? PALETTES.amber, sets all five CSS variables on `document.documentElement.style.setProperty`.

To avoid FOUC (Pitfall 5): `applyPaletteEagerly()` — called immediately (not in useEffect); reads `localStorage.getItem('palette')` synchronously and calls `applyPalette` before React mounts.

`PaletteProvider` component:
- Accepts `colorScheme: string` prop (from useProfile, async)
- On mount: apply from localStorage (fast path) OR fall back to colorScheme prop
- When colorScheme prop changes: call applyPalette(colorScheme) and persist to localStorage
- Provides `{ currentPalette, applyPalette }` via context

`usePalette()` — hook that reads from PaletteContext, throws if used outside PaletteProvider.

In `tailwind.config.ts`, add to the `colors` section (inside `extend.colors`):
```typescript
palette: {
  primary:   'var(--palette-primary)',
  secondary: 'var(--palette-secondary)',
  accent:    'var(--palette-accent)',
  important: 'var(--palette-important)',
  highlight: 'var(--palette-highlight)',
},
```

Fill in the PaletteContext.test.tsx stubs (from Plan 01) with real tests — mock `document.documentElement.style.setProperty` with `vi.spyOn`, render with PaletteProvider, assert CSS var calls.
  </action>
  <verify>
    <automated>npm test -- --run src/contexts/PaletteContext.test.tsx</automated>
  </verify>
  <done>All PaletteContext tests pass (no todos remaining). tailwind.config.ts has palette tokens. `npm run build` exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useProfile color_scheme extension</name>
  <files>src/hooks/useProfile.ts, src/hooks/useProfile.test.ts</files>
  <behavior>
    - Test: useProfile returns colorScheme: 'amber' when profile.color_scheme is 'amber'
    - Test: useProfile returns colorScheme: 'ocean' when profile.color_scheme is 'ocean'
    - Test: useProfile defaults colorScheme to 'amber' when profile.color_scheme is missing
    - Test: updateColorScheme calls supabase .update({ color_scheme: 'ocean' }) on the profiles table
    - Test: updateColorScheme updates colorScheme state optimistically before the DB write resolves
  </behavior>
  <action>
Read `src/hooks/useProfile.ts` and extend it:

1. Add `color_scheme` to the Supabase `.select()` query: `'id, tier, avatar_url, color_scheme'`
2. Add `colorScheme` state: `useState<string>('amber')`
3. On profile fetch: `setColorScheme(profile?.color_scheme ?? 'amber')`
4. Add `updateColorScheme(name: string)` function:
   - Set local state optimistically: `setColorScheme(name)`
   - Call `supabase.from('profiles').update({ color_scheme: name }).eq('id', userId)`
5. Add `colorScheme` and `updateColorScheme` to the `UseProfileResult` interface and return object.

Extend `src/hooks/useProfile.test.ts`: add the new behavior tests alongside existing tests. Use the same `buildChain` mock pattern already established in the file. Do not remove any existing tests.
  </action>
  <verify>
    <automated>npm test -- --run src/hooks/useProfile.test.ts</automated>
  </verify>
  <done>All useProfile tests pass including new colorScheme tests. No pre-existing tests regress.</done>
</task>

</tasks>

<verification>
`npm test -- --run src/contexts/PaletteContext.test.tsx src/hooks/useProfile.test.ts` — all pass.
`npm run build` — exits 0 with new tailwind tokens and extended types.
</verification>

<success_criteria>
- `PALETTES` constant covers amber, ocean, forest, rose — amber values match current WheelChart hex strings exactly
- `applyPalette()` sets all five `--palette-*` CSS variables on `document.documentElement`
- FOUC guard: palette applied synchronously from localStorage before first render
- `PaletteProvider` + `usePalette` hook exported from `src/contexts/PaletteContext.tsx`
- `useProfile` exposes `colorScheme` and `updateColorScheme`
- tailwind.config.ts has `palette.*` tokens mapped to CSS vars
- `npm test -- --run` exits 0, `npm run build` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/09-ai-and-premium/09-04-SUMMARY.md`
</output>
