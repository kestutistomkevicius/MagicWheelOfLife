---
phase: 09-ai-and-premium
plan: 08
type: execute
wave: 3
depends_on:
  - 09-04
files_modified:
  - src/components/WheelChart.tsx
  - src/components/WheelChart.test.tsx
  - src/components/AppShell.tsx
  - src/components/Sidebar.tsx
autonomous: true
requirements:
  - PREMIUM-02
must_haves:
  truths:
    - "WheelChart accepts color props instead of hardcoded hex strings"
    - "AppShell wraps with PaletteProvider, applying palette on mount"
    - "Sidebar background reads from --palette-accent CSS var (via Tailwind token or inline style)"
    - "Switching palette via applyPalette() updates sidebar background and WheelChart colors without reload"
  artifacts:
    - path: src/components/WheelChart.tsx
      provides: "WheelChart with color props: primaryColor, secondaryColor, importantColor, highlightColor"
    - path: src/components/AppShell.tsx
      provides: "PaletteProvider wrapping the app shell with colorScheme from useProfile"
    - path: src/components/Sidebar.tsx
      provides: "Sidebar using bg-palette-accent (Tailwind token) or inline style from CSS var for background"
  key_links:
    - from: "AppShell.tsx"
      to: "PaletteProvider"
      via: "wrap Outlet with PaletteProvider, pass colorScheme from useProfile"
      pattern: "PaletteProvider"
    - from: "Sidebar.tsx"
      to: "--palette-accent CSS var"
      via: "bg-palette-accent Tailwind class"
      pattern: "palette-accent|--palette-accent"
    - from: "WheelChart.tsx"
      to: "color props"
      via: "destructured props replacing hardcoded hex strings"
      pattern: "primaryColor|secondaryColor"
---

<objective>
Refactor WheelChart to accept color props instead of hardcoded hex strings, wrap AppShell with PaletteProvider to apply CSS vars on mount, and update Sidebar to use the palette accent color. This makes the entire app visually respond to palette changes.

Purpose: Without this plan, switching palette updates CSS vars but WheelChart (which uses Recharts SVG props — not CSS vars) stays amber. This plan closes that gap.
Output: Updated WheelChart, AppShell, Sidebar — all palette-aware.
</objective>

<execution_context>
@C:/Users/KestutisTomkevicius/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/KestutisTomkevicius/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-ai-and-premium/09-CONTEXT.md
@.planning/phases/09-ai-and-premium/09-RESEARCH.md
@src/components/WheelChart.tsx
@src/components/WheelChart.test.tsx
@src/components/AppShell.tsx
@src/components/Sidebar.tsx
@src/contexts/PaletteContext.tsx

<interfaces>
<!-- WheelChart current hardcoded colors (from codebase read): -->
<!-- As-Is radar: stroke="#e8a23a" fill="#e8a23a" -->
<!-- To-Be radar: stroke="#60a5fa" fill="#60a5fa" -->
<!-- Important radar: stroke="#b45309" fill="#b45309" -->
<!-- Highlight radar: fill="#fbbf24" -->
<!-- Tick highlight fill: '#d97706' (this is a derived shade — keep as static or make it palette.highlight) -->

<!-- WheelChart current interface (from codebase read): -->
interface WheelChartProps {
  data: WheelChartPoint[]
  highlightedCategory?: string | null
  importantCategories?: string[]
}

<!-- New WheelChart interface (add optional color props with amber defaults): -->
interface WheelChartProps {
  data: WheelChartPoint[]
  highlightedCategory?: string | null
  importantCategories?: string[]
  primaryColor?: string      // default: '#e8a23a'
  secondaryColor?: string    // default: '#60a5fa'
  importantColor?: string    // default: '#b45309'
  highlightColor?: string    // default: '#fbbf24'
}

<!-- CRITICAL (from RESEARCH.md pitfall 3): -->
<!-- Recharts reads stroke/fill as JS string props at render time -->
<!-- CSS vars like 'var(--palette-primary)' do NOT work in SVG fill/stroke props -->
<!-- WheelPage must pass actual hex strings from PaletteContext -->

<!-- Sidebar current background: className="... bg-[#292524] ..." (static) -->
<!-- Replace with inline style={{ backgroundColor: 'var(--palette-accent)' }} -->
<!-- OR use bg-palette-accent Tailwind token defined in tailwind.config.ts -->
<!-- Recommendation from RESEARCH.md: use bg-palette-accent (keeps styling in classes) -->

<!-- AppShell currently: wraps Sidebar + Outlet only -->
<!-- Needs: useAuth + useProfile to get colorScheme, wrap with PaletteProvider -->
<!-- OR: Move PaletteProvider higher (in App.tsx) — cleaner approach -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor WheelChart to accept color props with amber defaults</name>
  <files>src/components/WheelChart.tsx, src/components/WheelChart.test.tsx</files>
  <action>
Read `src/components/WheelChart.tsx` fully before editing.

Add four optional color props with amber defaults to `WheelChartProps`:
- `primaryColor?: string` (default `'#e8a23a'`) — As-Is radar stroke + fill
- `secondaryColor?: string` (default `'#60a5fa'`) — To-Be radar stroke + fill
- `importantColor?: string` (default `'#b45309'`) — Important radar stroke + fill
- `highlightColor?: string` (default `'#fbbf24'`) — Highlight radar fill

Replace the four sets of hardcoded hex strings in the JSX with the corresponding prop values (using the defaults if not provided).

The tick highlight color (`'#d97706'`) is a derived shade of the highlight — update it to use `highlightColor` as a reasonable approximation, or keep it static if visual testing reveals it's fine. Claude's discretion.

**Existing callers** (WheelPage, ComparisonChart, LandingPage, SnapshotsPage): adding optional props with defaults means all existing call sites work unchanged. Do NOT break existing callers.

**Tests:** Read `src/components/WheelChart.test.tsx`. Add tests confirming that when `primaryColor='#ff0000'` is passed, the rendered Radar components use that color. Use the existing mock pattern (Radar mock already exposes props). Do not remove any existing tests.
  </action>
  <verify>
    <automated>npm test -- --run src/components/WheelChart.test.tsx</automated>
  </verify>
  <done>WheelChart tests pass including new color prop tests. All existing WheelChart callers compile (npm run build exits 0).</done>
</task>

<task type="auto">
  <name>Task 2: Wrap AppShell with PaletteProvider + update Sidebar accent color</name>
  <files>src/components/AppShell.tsx, src/components/Sidebar.tsx</files>
  <action>
**AppShell.tsx:**

The component needs to know the user's `colorScheme` to initialize the palette. Two options — choose the cleaner one:

Option A (preferred): Move `PaletteProvider` into `App.tsx` (or `main.tsx`) above the router so it wraps everything including the landing page. Inside PaletteProvider: read `colorScheme` from a shared profile context. This is cleaner but requires a new context or lifting state.

Option B: Import `useAuth` in AppShell to get `userId`, call `useProfile(userId)` to get `colorScheme`, then wrap the existing JSX with `<PaletteProvider colorScheme={colorScheme}>`. This is simpler and consistent with how AppShell already works (it already renders Sidebar which uses useAuth + useProfile internally).

Use Option B (simpler, consistent). Read AppShell.tsx first, then:
- Import `useAuth` from `@/hooks/useAuth`
- Import `useProfile` from `@/hooks/useProfile`
- Import `PaletteProvider` from `@/contexts/PaletteContext`
- Get `userId` from `useAuth().session?.user.id ?? ''`
- Get `colorScheme` from `useProfile(userId).colorScheme`
- Wrap the return JSX: `<PaletteProvider colorScheme={colorScheme}>..existing div..</PaletteProvider>`

**Sidebar.tsx:**

Read the file. Find the `bg-[#292524]` class on the `<aside>` element. Replace it with:
- Option A: `className="... bg-palette-accent ..."` — uses the Tailwind token from tailwind.config.ts
- Option B: remove the static class and add `style={{ backgroundColor: 'var(--palette-accent)' }}`

Use Option A (bg-palette-accent) — keeps styling in Tailwind classes per RESEARCH.md recommendation. Also apply `bg-palette-accent` to any other sidebar-adjacent elements that currently have `#292524` hardcoded.

No tests needed for AppShell/Sidebar wiring (pure layout changes, verified in human checkpoint Plan 10).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>npm run build exits 0. AppShell wraps with PaletteProvider. Sidebar uses bg-palette-accent. No TypeScript errors.</done>
</task>

</tasks>

<verification>
`npm test -- --run` — full suite green.
`npm run build` — exits 0.
</verification>

<success_criteria>
- WheelChart accepts primaryColor, secondaryColor, importantColor, highlightColor props with amber defaults
- All existing WheelChart call sites still compile without changes
- AppShell wraps with PaletteProvider, passing colorScheme from useProfile
- Sidebar uses bg-palette-accent (Tailwind token) for background instead of hardcoded hex
- Full test suite green, build exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/09-ai-and-premium/09-08-SUMMARY.md`
</output>
