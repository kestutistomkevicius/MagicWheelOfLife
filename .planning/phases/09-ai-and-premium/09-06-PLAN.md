---
phase: 09-ai-and-premium
plan: 06
type: execute
wave: 3
depends_on:
  - 09-05
files_modified:
  - src/components/AiCoachDrawer.tsx
  - src/components/AiCoachDrawer.test.tsx
autonomous: true
requirements:
  - AI-01
must_haves:
  truths:
    - "Drawer renders category name in header and all messages in the chat thread"
    - "Streaming assistant message updates token by token in the last bubble"
    - "Proposal card appears pinned at bottom when hook exposes a proposal"
    - "Apply to As-Is and Apply to To-Be buttons call their respective callbacks with the suggested value"
    - "Send button disabled while streaming; input clears on send"
    - "Inline error message with Retry button when hook.error is set"
    - "Close button calls onClose"
  artifacts:
    - path: src/components/AiCoachDrawer.tsx
      provides: "Slide-in right drawer with chat thread, proposal card, send input, close button"
      exports:
        - AiCoachDrawer
        - AiCoachDrawerProps
  key_links:
    - from: "src/components/AiCoachDrawer.tsx"
      to: "useAiChat"
      via: "hook import and prop pass-through"
      pattern: "useAiChat"
    - from: "AiCoachDrawer"
      to: "onApplyAsis / onApplyTobe"
      via: "proposal card button onClick"
      pattern: "onApplyAsis|onApplyTobe"
---

<objective>
Build the AiCoachDrawer component: the slide-in right panel that hosts the AI coaching chat. Takes useAiChat hook result as props (or category params), renders the message thread, streaming state, proposal card, send form, error/retry, and close button.

Purpose: The visible AI-01 surface that users interact with from WheelPage.
Output: AiCoachDrawer.tsx with full implementation and passing tests.
</objective>

<execution_context>
@C:/Users/KestutisTomkevicius/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/KestutisTomkevicius/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-ai-and-premium/09-CONTEXT.md
@.planning/phases/09-ai-and-premium/09-RESEARCH.md
@src/hooks/useAiChat.ts
@src/components/SnapshotNameDialog.tsx

<interfaces>
<!-- AiCoachDrawer props: -->
export interface AiCoachDrawerProps {
  categoryId: string
  categoryName: string
  asisScore: number
  tobeScore: number
  onApplyAsis: (value: number) => void
  onApplyTobe: (value: number) => void
  onClose: () => void
}

<!-- useAiChat result (from Plan 05): -->
<!-- { messages, streaming, proposal, error, sendMessage, retry, loadHistory } -->

<!-- Modal/drawer pattern from existing code: -->
<!-- Plain Tailwind div with backdrop (not shadcn Dialog — Radix portal issues in tests) -->
<!-- See SnapshotNameDialog.tsx for reference pattern -->

<!-- CONTEXT.md specifics: -->
<!-- Drawer: slide-in from right, overlaying WheelPage -->
<!-- Category name at top in drawer header -->
<!-- Chat thread in middle (scrollable) -->
<!-- Proposal card PINNED at bottom when AI has suggestion ready -->
<!-- Proposal card: "Suggested scores: As-Is: X · To-Be: Y" + two buttons -->
<!-- Opening message from AI Coach (from useAiChat.loadHistory — if no history, useAiChat sends an opening turn) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build AiCoachDrawer component with full tests</name>
  <files>src/components/AiCoachDrawer.tsx, src/components/AiCoachDrawer.test.tsx</files>
  <behavior>
    - Test: renders category name (e.g., "Health") in drawer header
    - Test: renders user and assistant messages in the thread
    - Test: streaming assistant message (content updating) renders in the last bubble
    - Test: proposal card not visible when proposal=null
    - Test: proposal card visible with "Suggested scores: As-Is: 5 · To-Be: 8" when proposal set
    - Test: "Apply to As-Is" button calls onApplyAsis(5)
    - Test: "Apply to To-Be" button calls onApplyTobe(8)
    - Test: send button is disabled while streaming=true
    - Test: submitting the input form calls sendMessage with the typed text
    - Test: input clears after send
    - Test: error message and "Retry" button visible when error is set
    - Test: Retry button calls retry()
    - Test: close button calls onClose
  </behavior>
  <action>
Create `src/components/AiCoachDrawer.tsx`:

**Layout (Tailwind):**
- Backdrop: `fixed inset-0 bg-black/40 z-40` — clicking it calls onClose
- Drawer panel: `fixed top-0 right-0 h-full w-96 bg-white shadow-xl z-50 flex flex-col`
- Header: category name + close button (X icon) — `flex items-center justify-between p-4 border-b`
- Chat thread: `flex-1 overflow-y-auto p-4 space-y-3` — scrollable message list
- Proposal card (when proposal exists): `border-t p-4 bg-stone-50` pinned above input
- Send form: `border-t p-4` — text input + send button

**Message bubbles:**
- User: right-aligned, amber background (`bg-amber-100`)
- Assistant: left-aligned, white/stone background (`bg-stone-100`)
- Streaming indicator: a blinking cursor or `...` appended to the last assistant bubble while `streaming=true`

**Proposal card:**
```
┌─ Suggested scores ──────────────────────────┐
│  As-Is: 5   ·   To-Be: 8                    │
│  [Apply to As-Is]          [Apply to To-Be] │
└──────────────────────────────────────────────┘
```
Elevated card feel: `rounded-lg border border-amber-200 bg-amber-50 p-3 space-y-2`

**useAiChat integration:**
- The component calls `useAiChat({ categoryId, categoryName, asisScore, tobeScore })` internally
- On mount: call `loadHistory(categoryId)` — restores prior conversation thread
- If messages is empty after loadHistory: the hook auto-sends the AI opening message (coordinate this in the hook or trigger a special "start" call)

**Tests:** Mock `useAiChat` via `vi.mock('@/hooks/useAiChat')` returning controlled values. Fill in the AiCoachDrawer.test.tsx stubs from Plan 01.
  </action>
  <verify>
    <automated>npm test -- --run src/components/AiCoachDrawer.test.tsx</automated>
  </verify>
  <done>All AiCoachDrawer tests pass, no todos remaining. `npm test -- --run` exits 0.</done>
</task>

</tasks>

<verification>
`npm test -- --run src/components/AiCoachDrawer.test.tsx` — all tests pass.
`npm run build` — exits 0.
</verification>

<success_criteria>
- AiCoachDrawer renders category name, message thread, proposal card (conditional), send form, close button
- Proposal card has two independent Apply buttons calling onApplyAsis/onApplyTobe
- Send button disabled while streaming
- Retry button visible when error is set
- All AiCoachDrawer tests pass, no todos
</success_criteria>

<output>
After completion, create `.planning/phases/09-ai-and-premium/09-06-SUMMARY.md`
</output>
