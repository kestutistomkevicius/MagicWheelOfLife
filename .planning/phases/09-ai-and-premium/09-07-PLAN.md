---
phase: 09-ai-and-premium
plan: 07
type: execute
wave: 3
depends_on:
  - 09-04
files_modified:
  - src/components/ColorSchemePicker.tsx
  - src/components/ColorSchemePicker.test.tsx
autonomous: true
requirements:
  - PREMIUM-02
must_haves:
  truths:
    - "Swatch grid renders one circular button per palette"
    - "Selected swatch has a visible ring/checkmark indicator"
    - "Clicking a swatch calls onSelect with the palette name (premium user)"
    - "Free user sees a lock overlay on non-amber swatches"
    - "Free user clicking a locked swatch triggers the upgrade prompt modal"
  artifacts:
    - path: src/components/ColorSchemePicker.tsx
      provides: "Circular swatch grid component with premium gating and selection indicator"
      exports:
        - ColorSchemePicker
        - ColorSchemePickerProps
  key_links:
    - from: "ColorSchemePicker"
      to: "onSelect callback"
      via: "swatch button onClick"
      pattern: "onSelect"
    - from: "ColorSchemePicker"
      to: "PALETTES from PaletteContext"
      via: "import { PALETTES }"
      pattern: "PALETTES"
---

<objective>
Build the ColorSchemePicker component: a grid of circular color swatches displayed in SettingsPage. Premium users click to apply a palette immediately; free users see a lock overlay on non-default swatches and an upgrade prompt on click.

Purpose: The PREMIUM-02 UI surface for palette selection. Consumed by SettingsPage in Plan 09.
Output: ColorSchemePicker.tsx with full implementation and passing tests.
</objective>

<execution_context>
@C:/Users/KestutisTomkevicius/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/KestutisTomkevicius/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-ai-and-premium/09-CONTEXT.md
@.planning/phases/09-ai-and-premium/09-RESEARCH.md
@src/contexts/PaletteContext.tsx
@src/components/SnapshotNameDialog.tsx

<interfaces>
<!-- ColorSchemePicker props: -->
export interface ColorSchemePickerProps {
  currentPalette: string        // e.g. 'amber'
  isPremium: boolean
  onSelect: (paletteName: string) => void
}

<!-- PALETTES constant (from PaletteContext.tsx — import it): -->
<!-- Keys: 'amber', 'ocean', 'forest', 'rose' -->
<!-- Each palette has --palette-primary as the representative swatch color -->

<!-- CONTEXT.md UX: -->
<!-- Circular swatches (w-10 h-10 rounded-full) -->
<!-- Selected swatch: ring-2 ring-offset-2 ring-amber-600 (or equivalent) + checkmark icon inside -->
<!-- Non-selected: plain circle filled with palette primary color -->
<!-- Free user: lock icon overlay on non-amber swatches; clicking opens upgrade prompt -->
<!-- Upgrade prompt: same pattern as star feature (plain Tailwind modal, not shadcn Dialog) -->

<!-- Swatch representative color: use the --palette-primary value from each palette -->
<!-- Since these are hex strings in PALETTES (not CSS vars), can use directly as inline style -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build ColorSchemePicker component with full tests</name>
  <files>src/components/ColorSchemePicker.tsx, src/components/ColorSchemePicker.test.tsx</files>
  <behavior>
    - Test: renders one swatch button for each palette key in PALETTES (4 swatches)
    - Test: current swatch (currentPalette='amber') has aria-pressed=true or role indicator
    - Test: clicking a swatch as premium user calls onSelect('ocean') for ocean swatch
    - Test: free user — amber swatch (default) is clickable without upgrade prompt
    - Test: free user — clicking ocean swatch shows upgrade prompt modal, onSelect not called
    - Test: upgrade prompt modal has a close/dismiss button
    - Test: premium user clicking all swatches calls onSelect with the correct palette name
  </behavior>
  <action>
Create `src/components/ColorSchemePicker.tsx`:

**Swatch rendering:**
Iterate over `Object.entries(PALETTES)`. For each `[name, vars]`:
- Swatch `button` element: `w-10 h-10 rounded-full relative` with `style={{ backgroundColor: vars['--palette-primary'] }}`
- If `name === currentPalette`: add ring indicator (`ring-2 ring-offset-2 ring-stone-700`) and a small checkmark icon (lucide `Check`) inside
- If `!isPremium && name !== 'amber'`: add lock overlay `absolute inset-0 rounded-full bg-black/40 flex items-center justify-center` with a lucide `Lock` icon; clicking triggers upgrade modal instead of onSelect

**Palette label:** below each swatch, show the palette name capitalized (e.g., "Amber", "Ocean").

**Layout:** `flex gap-4 flex-wrap` — responsive swatch grid row.

**Upgrade modal:**
Plain Tailwind modal (consistent with FeatureRequestModal/SnapshotNameDialog pattern — no Radix):
```
"Color palettes are a premium feature. Upgrade to unlock all color schemes."
[Close]
```
Local state `showUpgradeModal: boolean` controls visibility.

**Tests:** Fill in stubs from Plan 01. Mock PALETTES if needed or import directly (PALETTES is a const, not a hook — direct import fine in tests).
  </action>
  <verify>
    <automated>npm test -- --run src/components/ColorSchemePicker.test.tsx</automated>
  </verify>
  <done>All ColorSchemePicker tests pass, no todos remaining. `npm test -- --run` exits 0.</done>
</task>

</tasks>

<verification>
`npm test -- --run src/components/ColorSchemePicker.test.tsx` — all pass.
`npm run build` — exits 0.
</verification>

<success_criteria>
- Swatch grid renders one circular button per palette with the palette's primary color as fill
- Selected swatch has ring indicator
- Premium user: clicking any swatch calls onSelect
- Free user: non-amber swatches show lock overlay; clicking shows upgrade modal
- All tests pass, no todos
</success_criteria>

<output>
After completion, create `.planning/phases/09-ai-and-premium/09-07-SUMMARY.md`
</output>
