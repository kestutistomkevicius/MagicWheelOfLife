---
phase: 12-multi-wheel-ux
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified: []
autonomous: false
requirements:
  - MW-01
  - MW-02

must_haves:
  truths:
    - "Premium user with 2 wheels sees a wheel selector dropdown on TrendPage"
    - "Switching between wheels loads the correct snapshot data for each wheel"
    - "Sidebar label shows 'My Wheels' for the premium user who has 2 wheels"
  artifacts:
    - path: "src/pages/TrendPage.tsx"
      provides: "Wheel selector UI + stale clear on switch"
    - path: "src/components/Sidebar.tsx"
      provides: "Dynamic wheel label (singular/plural)"
  key_links:
    - from: "TrendPage wheel selector"
      to: "snapshot chart"
      via: "selectWheel -> useEffect([wheel?.id]) -> listSnapshots"
      pattern: "selectWheel"
---

<objective>
UAT checkpoint: verify multi-wheel UX end-to-end against live local Supabase with the seeded premium user.

Purpose: Unit tests confirm the logic, but runtime verification against real seed data confirms the feature works for users in practice. The premium seed user has 2 wheels — this checkpoint confirms the selector appears, switching works, and the sidebar label is correct.

Output: Human confirmation that MW-01 and MW-02 work end-to-end.
</objective>

<execution_context>
@C:/Users/KestutisTomkevicius/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/KestutisTomkevicius/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-multi-wheel-ux/12-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>UAT: Verify multi-wheel UX end-to-end in browser</name>
  <action>Human verification of MW-01 and MW-02 against live Supabase with seeded premium user data.</action>
  <what-built>
    Plan 01 added test coverage for MW-01 (TrendPage snapshot reload on wheel switch) and MW-02 (Sidebar plural label). It also added synchronous stale-data clearing in TrendPage when the selected wheel changes.

    All unit tests pass. This checkpoint verifies the feature works end-to-end in the browser with the real seeded premium user.
  </what-built>
  <how-to-verify>
    1. Start Supabase: `npx supabase start` (wait for "Started supabase local development setup")
    2. Start the dev server: `npm run dev`
    3. Open http://localhost:5173 in browser
    4. Log in as the premium user: email `premium@test.com`, password `password123`
    5. Navigate to "Trend" in the sidebar

    CHECK MW-01 (TrendPage wheel selector):
    - A dropdown selector should appear next to the "Trend" heading
    - It should show two options: "My Wheel" and "Work &amp; Purpose" (or similar second wheel name)
    - Select the second wheel — the chart area should briefly clear (stale data gone) then show the second wheel's snapshot data
    - Select back to the first wheel — chart should update back to the first wheel's data
    - Both wheels should have at least 3 snapshots and show a trend chart (not the "not enough snapshots" empty state)

    CHECK MW-02 (Sidebar plural label):
    - The sidebar navigation link that goes to /wheel should read "My Wheels" (plural, capital W) for the premium user (who has 2 wheels)
    - Navigate to /wheel — the wheel page should still work normally

    CHECK no regressions:
    - Snapshots page, wheel editing, settings still work
  </how-to-verify>
  <verify>Human approved</verify>
  <done>Both MW-01 and MW-02 confirmed working end-to-end against live Supabase.</done>
  <resume-signal>
    Type "approved" if both MW-01 and MW-02 work correctly end-to-end.
    Describe any issues found and Claude will fix them before re-verifying.
  </resume-signal>
</task>

</tasks>

<verification>
Human confirmation that:
1. TrendPage shows wheel selector for premium user with 2 wheels
2. Switching wheels loads correct snapshot data
3. Sidebar shows "My Wheels" label for premium user
</verification>

<success_criteria>
1. Premium user sees wheel selector dropdown on TrendPage with both wheels listed
2. Switching wheels on TrendPage loads that wheel's snapshots and updates the chart
3. Sidebar label reads "My Wheels" for the premium user (2 wheels)
4. No regressions in other pages
</success_criteria>

<output>
After completion, create `.planning/phases/12-multi-wheel-ux/12-02-SUMMARY.md`
</output>
