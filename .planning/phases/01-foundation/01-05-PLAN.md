---
phase: 01-foundation
plan: 05
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - src/components/AppShell.tsx
  - src/components/Sidebar.tsx
  - src/components/Sidebar.test.tsx
  - src/App.tsx
autonomous: true
requirements:
  - AUTH-04
  - AUTH-05

must_haves:
  truths:
    - "Authenticated users see a sidebar on the left and content area on the right"
    - "Sidebar shows four navigation links: My Wheel, Snapshots, Trend, Settings"
    - "The active route is visually highlighted in the sidebar"
    - "Sidebar has a sign-out button that, when clicked, signs the user out and redirects to /auth"
    - "The layout uses the warm/earthy palette — dark sidebar (stone-800 background), warm content area"
    - "The layout is responsive: sidebar visible on desktop/tablet"
  artifacts:
    - path: "src/components/AppShell.tsx"
      provides: "Layout wrapper: sidebar + content Outlet, warm/earthy styling"
      exports: ["AppShell"]
    - path: "src/components/Sidebar.tsx"
      provides: "Navigation sidebar with 4 links + signOut button"
      exports: ["Sidebar"]
  key_links:
    - from: "src/App.tsx"
      to: "src/components/AppShell.tsx"
      via: "Route element={<AppShell />} wrapping all protected routes"
      pattern: "AppShell"
    - from: "src/components/AppShell.tsx"
      to: "src/components/Sidebar.tsx"
      via: "Sidebar rendered inside AppShell layout"
      pattern: "Sidebar"
    - from: "src/components/Sidebar.tsx"
      to: "src/contexts/AuthContext.tsx"
      via: "useAuth().signOut() called on sign-out button click"
      pattern: "signOut"
---

<objective>
Build the AppShell layout wrapper (sidebar + content area) and the Sidebar navigation component. Wire AppShell into App.tsx routing. Sidebar handles sign-out (AUTH-05) and applies warm/earthy palette.

Purpose: This is the persistent container all authenticated content renders inside. Auth-05 (sign-out from any page) is implemented here.
Output: Full SaaS dashboard shell with working navigation, active-state highlighting, and sign-out.
</objective>

<execution_context>
@C:/Users/KestutisTomkevicius/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/KestutisTomkevicius/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<interfaces>
<!-- Key contracts from prior plans -->

From src/contexts/AuthContext.tsx:
```typescript
interface AuthContextType {
  session: Session | null | undefined
  signOut: () => Promise<void>
}
export function useAuth(): AuthContextType
```

From src/hooks/useAuth.ts:
```typescript
export { useAuth } from '../contexts/AuthContext'
```

From react-router (NavLink for active state):
```typescript
import { NavLink, Outlet, useNavigate } from 'react-router'
// NavLink provides isActive callback for className
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sidebar component with nav links + sign-out</name>
  <files>
    src/components/Sidebar.tsx,
    src/components/Sidebar.test.tsx
  </files>
  <behavior>
    - Renders four NavLinks: My Wheel (/wheel), Snapshots (/snapshots), Trend (/trend), Settings (/settings)
    - Active link has a distinct visual state (e.g., white text + brand accent left border)
    - Sign-out button calls signOut() from useAuth
    - Sidebar background is stone-800 (#292524) — warm dark
    - Logo/brand text at the top of the sidebar
  </behavior>
  <action>
    Create src/components/Sidebar.tsx:

    ```typescript
    import { NavLink } from 'react-router'
    import { useAuth } from '@/hooks/useAuth'
    import { cn } from '@/lib/utils'
    import {
      Circle,
      Camera,
      TrendingUp,
      Settings,
      LogOut,
    } from 'lucide-react'

    const navItems = [
      { to: '/wheel', label: 'My Wheel', icon: Circle },
      { to: '/snapshots', label: 'Snapshots', icon: Camera },
      { to: '/trend', label: 'Trend', icon: TrendingUp },
      { to: '/settings', label: 'Settings', icon: Settings },
    ]

    export function Sidebar() {
      const { signOut } = useAuth()

      return (
        <aside className="flex h-screen w-56 flex-col bg-[#292524] text-stone-300">
          {/* Brand */}
          <div className="px-4 py-6">
            <span className="text-base font-bold text-white">JustAWheelOfLife</span>
          </div>

          {/* Nav links */}
          <nav className="flex-1 space-y-1 px-2" aria-label="Main navigation">
            {navItems.map(({ to, label, icon: Icon }) => (
              <NavLink
                key={to}
                to={to}
                className={({ isActive }) =>
                  cn(
                    'flex items-center gap-3 rounded-lg px-3 py-2 text-sm transition-colors',
                    isActive
                      ? 'bg-brand-400/20 text-white border-l-2 border-brand-400'
                      : 'text-stone-400 hover:bg-stone-700 hover:text-stone-100'
                  )
                }
              >
                <Icon className="h-4 w-4 shrink-0" aria-hidden="true" />
                {label}
              </NavLink>
            ))}
          </nav>

          {/* Sign out */}
          <div className="border-t border-stone-700 p-2">
            <button
              onClick={signOut}
              className="flex w-full items-center gap-3 rounded-lg px-3 py-2 text-sm text-stone-400 transition-colors hover:bg-stone-700 hover:text-stone-100"
            >
              <LogOut className="h-4 w-4 shrink-0" aria-hidden="true" />
              Sign out
            </button>
          </div>
        </aside>
      )
    }
    ```

    Replace stub content in src/components/Sidebar.test.tsx with real tests:

    ```typescript
    import { describe, it, expect, vi } from 'vitest'
    import { render, screen } from '@testing-library/react'
    import userEvent from '@testing-library/user-event'
    import { MemoryRouter } from 'react-router'
    import { Sidebar } from './Sidebar'

    vi.mock('@/hooks/useAuth', () => ({
      useAuth: vi.fn(),
    }))

    import { useAuth } from '@/hooks/useAuth'
    const mockUseAuth = vi.mocked(useAuth)

    describe('Sidebar', () => {
      beforeEach(() => {
        mockUseAuth.mockReturnValue({ session: {} as any, signOut: vi.fn() })
      })

      it('renders navigation links for My Wheel, Snapshots, Trend, Settings', () => {
        render(
          <MemoryRouter initialEntries={['/wheel']}>
            <Sidebar />
          </MemoryRouter>
        )
        expect(screen.getByRole('link', { name: /my wheel/i })).toBeInTheDocument()
        expect(screen.getByRole('link', { name: /snapshots/i })).toBeInTheDocument()
        expect(screen.getByRole('link', { name: /trend/i })).toBeInTheDocument()
        expect(screen.getByRole('link', { name: /settings/i })).toBeInTheDocument()
      })

      it('calls signOut when the sign-out button is clicked', async () => {
        const mockSignOut = vi.fn().mockResolvedValue(undefined)
        mockUseAuth.mockReturnValue({ session: {} as any, signOut: mockSignOut })
        const user = userEvent.setup()
        render(
          <MemoryRouter>
            <Sidebar />
          </MemoryRouter>
        )
        await user.click(screen.getByRole('button', { name: /sign out/i }))
        expect(mockSignOut).toHaveBeenCalledOnce()
      })
    })
    ```

    Run: `npx shadcn@latest add` to ensure lucide-react is available (it ships with shadcn). If cn() utility is not yet present, shadcn init creates it at src/lib/utils.ts.
  </action>
  <verify>
    <automated>npm test -- --run src/components/Sidebar.test.tsx</automated>
  </verify>
  <done>
    Both Sidebar tests pass. Sidebar renders all four nav links and a sign-out button. `npm run build` exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: AppShell layout + wire into App.tsx</name>
  <files>
    src/components/AppShell.tsx,
    src/App.tsx
  </files>
  <action>
    Create src/components/AppShell.tsx:

    ```typescript
    import { Outlet } from 'react-router'
    import { Sidebar } from './Sidebar'

    export function AppShell() {
      return (
        <div className="flex h-screen overflow-hidden bg-surface">
          <Sidebar />
          <main className="flex-1 overflow-y-auto">
            <Outlet />
          </main>
        </div>
      )
    }
    ```

    Update src/App.tsx to replace the `<Outlet />` passthrough with `<AppShell />`:

    ```typescript
    // Remove: import { Outlet } from 'react-router' (if used only as passthrough)
    // Add: import { AppShell } from './components/AppShell'
    // Replace: <Route element={<Outlet />}>
    // With:    <Route element={<AppShell />}>
    ```

    Full updated App.tsx:
    ```typescript
    import { BrowserRouter, Routes, Route, Navigate } from 'react-router'
    import { ProtectedRoute } from './components/ProtectedRoute'
    import { AppShell } from './components/AppShell'
    import { AuthPage } from './pages/AuthPage'
    import { WheelPage } from './pages/WheelPage'
    import { SnapshotsPage } from './pages/SnapshotsPage'
    import { TrendPage } from './pages/TrendPage'
    import { SettingsPage } from './pages/SettingsPage'

    export function App() {
      return (
        <BrowserRouter>
          <Routes>
            <Route path="/auth" element={<AuthPage />} />
            <Route element={<ProtectedRoute />}>
              <Route element={<AppShell />}>
                <Route path="/wheel" element={<WheelPage />} />
                <Route path="/snapshots" element={<SnapshotsPage />} />
                <Route path="/trend" element={<TrendPage />} />
                <Route path="/settings" element={<SettingsPage />} />
                <Route path="/" element={<Navigate to="/wheel" replace />} />
                <Route path="*" element={<Navigate to="/wheel" replace />} />
              </Route>
            </Route>
          </Routes>
        </BrowserRouter>
      )
    }
    ```
  </action>
  <verify>
    <automated>npm test -- --run 2>&amp;&amp; npm run build</automated>
  </verify>
  <done>
    AppShell wraps all protected routes. Sidebar visible on all authenticated pages. Active route link is highlighted. Sign-out button calls signOut(). `npm test -- --run` exits 0 (all tests pass). `npm run build` exits 0.
  </done>
</task>

</tasks>

<verification>
- `npm test -- --run src/components/Sidebar.test.tsx` — 2 tests passing
- `npm test -- --run` — full suite passing (all plans' tests green)
- `npm run build` exits 0
- Browser: authenticated user at /wheel sees sidebar with 4 nav links and sign-out button
- Clicking sign-out → user is redirected to /auth
- Active nav item is visually distinct from inactive items
</verification>

<success_criteria>
- AppShell renders Sidebar + content area side by side
- Sidebar has My Wheel, Snapshots, Trend, Settings nav links (all functional routes)
- Sign-out button calls useAuth().signOut() — AUTH-05 complete
- Warm/earthy styling: dark sidebar (stone-800), warm surface content area
- App.tsx updated — AppShell replaces the Outlet passthrough
- 2 Sidebar unit tests passing
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-05-SUMMARY.md`
</output>
