---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - supabase/config.toml
  - supabase/.env.local.example
  - supabase/seed.sql
  - README.md
autonomous: true
requirements:
  - AUTH-02
  - AUTH-03
  - DEV-01
  - DEV-02
  - DEV-03
  - DEV-04

must_haves:
  truths:
    - "After `supabase db reset`, free@test.com can sign in with password test123"
    - "After `supabase db reset`, premium@test.com can sign in with password test123"
    - "The free-tier user has 8 categories with mid-range scores (4-7 range), and action items mixing open, completed, and deadline-bearing"
    - "The premium-tier user has 4+ snapshots with a visible mixed-trajectory story in scores"
    - "supabase/config.toml has Google OAuth external provider configured"
    - "README.md documents Google Cloud Console setup steps required for Google OAuth"
  artifacts:
    - path: "supabase/config.toml"
      provides: "Local Supabase config: site_url, Google OAuth external provider"
      contains: "auth.external.google"
    - path: "supabase/seed.sql"
      provides: "Both dev users inserted into auth.users and auth.identities with bcrypt passwords"
      contains: "crypt('test123', gen_salt('bf'))"
    - path: "README.md"
      provides: "Developer setup instructions including Google OAuth one-time configuration"
  key_links:
    - from: "supabase/config.toml"
      to: "supabase/.env.local.example"
      via: "SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_ID env var reference in config.toml"
      pattern: "env(SUPABASE_AUTH_EXTERNAL_GOOGLE"
    - from: "supabase/seed.sql"
      to: "auth.users + auth.identities"
      via: "Both tables must be populated — identity row required for email sign-in"
      pattern: "auth.identities"
---

<objective>
Configure Supabase local environment (config.toml with Google OAuth) and create seed.sql with two dev users whose data tells a visible story for testing overlay comparison and trend charts.

Purpose: AUTH-02 (Google OAuth) requires config.toml setup before the browser flow works. DEV-01..04 seed data enables testing from Phase 1 onward. AUTH-03 (Apple OAuth) is documented as deferred here.
Output: config.toml with Google OAuth provider, seed.sql with free and premium users, README setup docs.
</objective>

<execution_context>
@C:/Users/KestutisTomkevicius/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/KestutisTomkevicius/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Supabase config.toml with Google OAuth + developer README</name>
  <files>
    supabase/config.toml,
    supabase/.env.local.example,
    README.md
  </files>
  <action>
    If `supabase/config.toml` does not exist, run `supabase init` to create the supabase/ directory with a default config.toml. Then edit config.toml.

    Set in config.toml:
    ```toml
    [auth]
    site_url = "http://localhost:5173"
    additional_redirect_urls = ["http://localhost:5173/wheel", "http://localhost:5173/auth", "http://localhost:5173/**"]
    enable_signup = true
    # Disable email confirmation for local development — allows immediate sign-in after registration
    enable_confirmations = false

    [auth.external.google]
    enabled = true
    client_id = "env(SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_ID)"
    secret = "env(SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_SECRET)"
    # Redirect URI to register in Google Cloud Console:
    # http://127.0.0.1:54321/auth/v1/callback (note: 127.0.0.1 not localhost)
    ```

    AUTH-03 note: Apple OAuth is DEFERRED to Phase 7. No [auth.external.apple] block is added here.
    When Phase 7 arrives, add:
    ```toml
    # [auth.external.apple]
    # enabled = true
    # client_id = "env(SUPABASE_AUTH_EXTERNAL_APPLE_CLIENT_ID)"
    # secret = "env(SUPABASE_AUTH_EXTERNAL_APPLE_SECRET)"
    # Requires Apple Developer account and cannot use localhost callback URLs.
    ```

    Create supabase/.env.local.example (template — never commit the real .env.local):
    ```
    # supabase/.env.local — Google OAuth credentials for local development
    # Obtain from: Google Cloud Console -> APIs & Services -> Credentials -> OAuth 2.0 Client ID
    SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
    SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_SECRET=your-google-client-secret
    ```

    Create README.md with developer setup instructions:
    ```markdown
    # JustAWheelOfLife — Developer Setup

    ## Prerequisites
    - Docker Desktop (running)
    - Node.js >= 20
    - Supabase CLI

    ## First-time setup

    1. Clone the repo
    2. Install frontend dependencies: `npm install`
    3. Copy environment files:
       - `cp .env.local.example .env.local` — fill in anon key from `supabase status`
       - `cp supabase/.env.local.example supabase/.env.local` — fill in Google OAuth credentials (see below)
    4. Start local Supabase: `supabase start`
    5. Populate .env.local with the ANON_KEY from `supabase status`
    6. Seed the database: `supabase db reset`
    7. Start the dev server: `npm run dev`
    8. Open http://localhost:5173

    ## Google OAuth setup (one-time per developer)

    Google OAuth requires real credentials — it cannot be mocked locally.

    1. Go to https://console.cloud.google.com/
    2. Create a project (or use an existing one)
    3. Enable the "Google+ API" or "Google Identity"
    4. Go to APIs & Services -> Credentials -> Create Credentials -> OAuth 2.0 Client ID
    5. Application type: Web application
    6. Authorized redirect URIs — add EXACTLY: `http://127.0.0.1:54321/auth/v1/callback`
       (note: 127.0.0.1, not localhost)
    7. Copy the Client ID and Client Secret into `supabase/.env.local`
    8. Restart Supabase: `supabase stop && supabase start`

    Email/password sign-in works without Google setup.

    ## Apple OAuth (Phase 7 only)

    Apple OAuth is deferred to production launch (Phase 7). It requires an Apple Developer account
    and cannot use localhost callback URLs. Not needed for local development.

    ## Seed users

    After `supabase db reset`:

    | Email | Password | Tier | Notes |
    |-------|----------|------|-------|
    | free@test.com | test123 | free | 8 categories, mid-range scores, mixed action items |
    | premium@test.com | test123 | premium | 8 categories, 4 snapshots, mixed-trajectory story |

    ## Daily workflow

    ```bash
    supabase start     # start all Supabase services
    npm run dev        # Vite dev server → http://localhost:5173
    # Studio dashboard → http://localhost:54323
    ```
    ```

    Add `supabase/.env.local` to .gitignore if not already present.
  </action>
  <verify>
    <automated>npm run build 2>&amp;&amp; echo "CONFIG OK"</automated>
  </verify>
  <done>
    supabase/config.toml contains [auth.external.google] with env() references. README.md has complete developer setup instructions including Google OAuth steps and the exact redirect URI (127.0.0.1:54321/auth/v1/callback). supabase/.env.local.example is committed. `npm run build` still passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: seed.sql — free and premium dev users with story data</name>
  <files>
    supabase/seed.sql
  </files>
  <action>
    Create supabase/seed.sql. The seed must insert BOTH auth.users AND auth.identities for each user (Supabase requires an identity row for email sign-in — a user without one returns "Invalid login credentials").

    Phase 1 only creates auth users. The wheel, category, action item, and snapshot data cannot be seeded yet because those tables do not exist until Phases 2-4. However, document the full story intention in comments for when those phases arrive.

    The seed.sql pattern uses a DO block with declared UUIDs so they can be referenced across multiple INSERT statements:

    ```sql
    -- supabase/seed.sql
    -- Dev seed data for local development.
    -- Run: supabase db reset (wipes DB and re-runs all migrations + this seed)
    --
    -- SEED USERS:
    -- free@test.com / test123   → free-tier developer test account
    -- premium@test.com / test123 → premium-tier developer test account
    --
    -- NOTE: Wheel, category, action item, and snapshot data are seeded in later phases
    -- when those tables exist (Phase 2+ migrations).

    DO $$
    DECLARE
      free_user_id uuid := '00000000-0000-0000-0000-000000000001';
      premium_user_id uuid := '00000000-0000-0000-0000-000000000002';
    BEGIN

      -- ============================================================
      -- FREE-TIER USER: free@test.com / test123
      -- Persona: generic mid-career professional
      -- Wheel: default 8 categories (seeded in Phase 2)
      -- Scores: mid-range 4-7, some to-be higher than as-is
      -- Action items: mix of open, completed, and with deadlines (Phase 3)
      -- ============================================================

      INSERT INTO auth.users (
        id, instance_id, aud, role, email,
        encrypted_password, email_confirmed_at,
        raw_app_meta_data, raw_user_meta_data,
        created_at, updated_at,
        confirmation_token, email_change,
        email_change_token_new, recovery_token
      ) VALUES (
        free_user_id,
        '00000000-0000-0000-0000-000000000000',
        'authenticated', 'authenticated',
        'free@test.com',
        crypt('test123', gen_salt('bf')),
        now(),
        '{"provider":"email","providers":["email"]}'::jsonb,
        '{"name":"Free Tester"}'::jsonb,
        now() - interval '30 days', now(),
        '', '', '', ''
      )
      ON CONFLICT (id) DO NOTHING;

      INSERT INTO auth.identities (
        id, user_id, provider_id,
        identity_data, provider,
        last_sign_in_at, created_at, updated_at
      ) VALUES (
        gen_random_uuid(),
        free_user_id,
        free_user_id::text,
        jsonb_build_object('sub', free_user_id::text, 'email', 'free@test.com'),
        'email',
        now(), now(), now()
      )
      ON CONFLICT DO NOTHING;

      -- ============================================================
      -- PREMIUM-TIER USER: premium@test.com / test123
      -- Wheel: 8 categories with 4 snapshots telling a mixed story (Phase 2/4)
      -- Score story across 4 snapshots:
      --   Career:         5 → 6 → 7 → 8  (improving)
      --   Health:         6 → 5 → 6 → 7  (dip then recovery)
      --   Relationships:  8 → 7 → 6 → 5  (declining)
      --   Finance:        4 → 4 → 5 → 6  (slowly improving)
      --   Fun & Rec:      7 → 6 → 5 → 4  (declining — traded for career)
      --   Personal Growth:5 → 6 → 7 → 7  (improving then plateau)
      --   Phys. Env:      6 → 6 → 6 → 7  (stable then jump)
      --   Family/Friends: 7 → 6 → 5 → 5  (declining)
      -- Snapshot dates: ~3 months apart
      -- Action items: some completed, some with past/future deadlines, some open
      -- ============================================================

      INSERT INTO auth.users (
        id, instance_id, aud, role, email,
        encrypted_password, email_confirmed_at,
        raw_app_meta_data, raw_user_meta_data,
        created_at, updated_at,
        confirmation_token, email_change,
        email_change_token_new, recovery_token
      ) VALUES (
        premium_user_id,
        '00000000-0000-0000-0000-000000000000',
        'authenticated', 'authenticated',
        'premium@test.com',
        crypt('test123', gen_salt('bf')),
        now(),
        '{"provider":"email","providers":["email"]}'::jsonb,
        '{"name":"Premium Tester"}'::jsonb,
        now() - interval '365 days', now(),
        '', '', '', ''
      )
      ON CONFLICT (id) DO NOTHING;

      INSERT INTO auth.identities (
        id, user_id, provider_id,
        identity_data, provider,
        last_sign_in_at, created_at, updated_at
      ) VALUES (
        gen_random_uuid(),
        premium_user_id,
        premium_user_id::text,
        jsonb_build_object('sub', premium_user_id::text, 'email', 'premium@test.com'),
        'email',
        now(), now(), now()
      )
      ON CONFLICT DO NOTHING;

    END $$;
    ```

    Note on fixed UUIDs: Using deterministic UUIDs (000...0001, 000...0002) means later phases can reference these user IDs in their seed data without querying for them. Document this in comments.

    Note on ON CONFLICT: The DO NOTHING clauses make the seed idempotent — running it multiple times does not fail.
  </action>
  <verify>
    <automated>npm test -- --run 2>&amp;&amp; echo "SEED FILE CREATED"</automated>
  </verify>
  <done>
    supabase/seed.sql exists. Contains bcrypt password hashing (`crypt('test123', gen_salt('bf'))`), inserts into BOTH auth.users AND auth.identities for each user. Uses deterministic UUIDs (000...0001, 000...0002). Includes comments documenting the score story for Phase 4 seeding. After `supabase db reset`, both users can sign in with email/password.
  </done>
</task>

</tasks>

<verification>
Manual verification (requires running Supabase):
- `supabase db reset` runs without SQL errors
- Sign in as free@test.com / test123 → success
- Sign in as premium@test.com / test123 → success
- Supabase Studio (localhost:54323) → Authentication → Users shows both accounts

Automated: `npm run build` exits 0 (config.toml changes do not break frontend build)
</verification>

<success_criteria>
- supabase/config.toml has Google OAuth configured with env() references
- README.md documents Google Cloud Console setup including the exact redirect URI (127.0.0.1:54321/auth/v1/callback)
- Apple OAuth documented as Phase 7 deferred (AUTH-03 explicitly noted)
- seed.sql inserts into auth.users AND auth.identities for both dev users
- Passwords use bcrypt via `crypt('test123', gen_salt('bf'))`
- Deterministic UUIDs used for predictable cross-phase seed references
- Mixed-trajectory score story documented in comments for Phase 4 seeding
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
