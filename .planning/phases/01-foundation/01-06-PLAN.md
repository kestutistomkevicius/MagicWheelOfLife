---
phase: 01-foundation
plan: 06
type: execute
wave: 4
depends_on:
  - 01-03
  - 01-04
  - 01-05
files_modified: []
autonomous: false
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - DEV-01
  - DEV-02
  - DEV-03
  - DEV-04

must_haves:
  truths:
    - "Developer can clone, run two commands, and reach the working app"
    - "Email/password sign-in works end-to-end with a real browser session"
    - "Google OAuth completes the redirect loop and establishes a session"
    - "Session survives a browser refresh without a flash-to-auth redirect"
    - "Sign-out from the sidebar redirects to /auth and session is cleared"
    - "Both seed users can sign in after supabase db reset"
  artifacts: []
  key_links: []
---

<objective>
Human verification checkpoint for the complete Phase 1 foundation. Verifies all auth flows work end-to-end in a real browser with a real running Supabase instance.

Purpose: Unit tests validated individual components; this checkpoint validates the full integrated system — session persistence, OAuth redirect loop, sidebar navigation, and seed user sign-in.
Output: Human sign-off that Phase 1 is complete and ready for Phase 2.
</objective>

<execution_context>
@C:/Users/KestutisTomkevicius/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/KestutisTomkevicius/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 1 foundation:
    - Vite + React + TypeScript + Tailwind + shadcn/ui project scaffold
    - AuthContext with session race condition guard (undefined/null/Session states)
    - ProtectedRoute redirecting unauthenticated users to /auth
    - AuthPage at /auth with sign-in/create-account toggle, email/password form, Google OAuth button (no Apple button)
    - AppShell with warm/earthy sidebar (dark stone-800) + four nav routes
    - Supabase config.toml with Google OAuth external provider configured
    - seed.sql with free@test.com and premium@test.com (bcrypt passwords, both auth.users and auth.identities rows)
    - README.md with developer setup and Google Cloud Console instructions
  </what-built>
  <how-to-verify>
    Prerequisites: Supabase running (`supabase start`), .env.local populated with VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY, Vite running (`npm run dev`).

    **Test 1 — Automated suite (confirm before manual tests):**
    Run: `npm test -- --run`
    Expected: All tests pass, zero failures.

    **Test 2 — Seed users sign-in (DEV-01, DEV-02):**
    1. Run: `supabase db reset`
    2. Open http://localhost:5173
    3. Sign in as free@test.com / test123 → should land on /wheel with sidebar visible
    4. Sign out → should redirect to /auth
    5. Sign in as premium@test.com / test123 → should land on /wheel
    6. Sign out

    **Test 3 — New account registration (AUTH-01):**
    1. On /auth, click "Create account"
    2. Enter a new email (e.g., test-new@example.com) and password (min 6 chars)
    3. Click "Create account"
    4. Should land on /wheel (no email confirmation required in local dev)

    **Test 4 — Session persistence (AUTH-04):**
    1. Sign in as free@test.com
    2. Press F5 (browser refresh) or Cmd+R
    3. Expected: brief spinner, then back on /wheel — NO redirect to /auth
    4. No flash of the auth page should be visible

    **Test 5 — Sign-out (AUTH-05):**
    1. While signed in, click "Sign out" in the sidebar
    2. Expected: redirect to /auth, session cleared
    3. Pressing browser back should NOT re-enter the app (session is gone)

    **Test 6 — Protected route redirect (AUTH-04):**
    1. While signed out, navigate directly to http://localhost:5173/wheel
    2. Expected: redirect to /auth

    **Test 7 — Sidebar navigation:**
    1. While signed in, click each sidebar link: My Wheel, Snapshots, Trend, Settings
    2. Expected: URL changes, placeholder content appears, active link is visually highlighted
    3. Sidebar should be visible on all four routes

    **Test 8 — Google OAuth (AUTH-02) — REQUIRES Google Cloud credentials:**
    If Google Cloud credentials are configured in supabase/.env.local:
    1. Click "Continue with Google" on /auth
    2. Complete Google consent screen
    3. Expected: redirect back to /wheel with a valid session
    If credentials are NOT yet set up: skip this test and note in response.

    **Test 9 — Auth error display:**
    1. On /auth sign-in mode, enter wrong credentials (e.g., free@test.com / wrongpassword)
    2. Click Sign in
    3. Expected: inline error message appears below the button (not a toast/banner)
    4. Error should say something like "Invalid login credentials"
  </how-to-verify>
  <resume-signal>
    Type "approved" if all tests pass.
    Or describe which tests failed and what you observed — Claude will investigate and fix before re-running verification.
  </resume-signal>
</task>

</tasks>

<verification>
Human sign-off required. All 9 tests above should pass (Test 8 may be skipped if Google credentials not yet configured).
</verification>

<success_criteria>
Phase 1 is complete when:
- All unit tests pass (`npm test -- --run` exits 0)
- Both seed users sign in successfully after `supabase db reset`
- Session persists across browser refresh (no auth flash)
- Sign-out works from the sidebar
- Protected routes redirect unauthenticated users to /auth
- Auth errors appear inline (no toasts)
- Sidebar shows all four navigation links with active state
- Human has typed "approved"
</success_criteria>

<output>
After human approval, create `.planning/phases/01-foundation/01-06-SUMMARY.md`
</output>
