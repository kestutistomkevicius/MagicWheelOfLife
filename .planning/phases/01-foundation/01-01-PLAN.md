---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - tsconfig.app.json
  - tsconfig.json
  - index.html
  - tailwind.config.ts
  - postcss.config.js
  - src/main.tsx
  - src/App.tsx
  - src/index.css
  - src/lib/supabase.ts
  - src/types/database.ts
  - src/test/setup.ts
  - src/pages/AuthPage.test.tsx
  - src/components/ProtectedRoute.test.tsx
  - src/components/Sidebar.test.tsx
  - .env.local.example
  - .gitignore
autonomous: true
requirements:
  - AUTH-01
  - AUTH-04

must_haves:
  truths:
    - "Running `npm run dev` starts Vite dev server on localhost:5173 with no errors"
    - "Running `npm test -- --run` executes the test suite and all tests pass (stubs pass trivially)"
    - "Running `npm run build` produces a production build without TypeScript errors"
    - "The `@/` import alias resolves correctly in both Vite and TypeScript"
  artifacts:
    - path: "vite.config.ts"
      provides: "Vite build config with path alias (@/) and Vitest test config"
      contains: "resolve.alias"
    - path: "src/lib/supabase.ts"
      provides: "Typed Supabase client singleton"
      exports: ["supabase"]
    - path: "src/test/setup.ts"
      provides: "Vitest + jest-dom global setup"
    - path: "src/pages/AuthPage.test.tsx"
      provides: "Test stubs for AUTH-01 and AUTH-02"
    - path: "src/components/ProtectedRoute.test.tsx"
      provides: "Test stubs for AUTH-04 session states"
    - path: "src/components/Sidebar.test.tsx"
      provides: "Test stub for AUTH-05 sign-out"
  key_links:
    - from: "vite.config.ts"
      to: "tsconfig.app.json"
      via: "paths alias must match resolve.alias"
      pattern: "\"@\":"
    - from: "src/lib/supabase.ts"
      to: "import.meta.env"
      via: "VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY"
      pattern: "import.meta.env.VITE_SUPABASE"
---

<objective>
Create the Vite + React + TypeScript + Tailwind CSS + shadcn/ui project scaffold with Vitest testing infrastructure and failing-stub tests.

Purpose: Every subsequent plan depends on this scaffold existing. Test stubs are written now so Wave 2 plans implement code that makes them pass — fulfilling the Nyquist requirement.
Output: A compilable, testable project skeleton with stub tests that clearly fail (because the components they test do not yet exist).
</objective>

<execution_context>
@C:/Users/KestutisTomkevicius/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/KestutisTomkevicius/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite + React + TypeScript + Tailwind + shadcn/ui project</name>
  <files>
    package.json, vite.config.ts, tsconfig.app.json, tsconfig.json, index.html,
    tailwind.config.ts, postcss.config.js, src/main.tsx, src/App.tsx, src/index.css,
    src/lib/supabase.ts, src/types/database.ts, .env.local.example, .gitignore
  </files>
  <action>
    Initialize a Vite + React + TypeScript project in the current directory:
    ```bash
    npm create vite@latest . -- --template react-ts
    npm install
    npm install react-router @supabase/supabase-js
    ```

    Install Tailwind + shadcn (shadcn init handles Tailwind config automatically):
    ```bash
    npx shadcn@latest init
    ```
    When prompted by shadcn init: choose "Default" style, use "stone" as base color (fits warm/earthy direction), confirm globals.css path. This configures tailwind.config.ts, postcss.config.js, vite.config.ts path alias, tsconfig.app.json paths, and src/index.css.

    After shadcn init, update tailwind.config.ts to add the warm/earthy brand palette:
    ```typescript
    // Extend theme.extend.colors with:
    brand: {
      50:  '#fdf8f0',
      100: '#faefd8',
      200: '#f5d9a8',
      300: '#efc06b',
      400: '#e8a23a',  // primary amber
      500: '#d4841a',
      600: '#b56a10',
      700: '#8f500f',
      800: '#6b3d10',
      900: '#4d2d0f',
    },
    surface: {
      DEFAULT: '#fdf8f0',
      sidebar: '#292524',  // warm dark sidebar (stone-800)
    }
    ```

    Add path alias to vite.config.ts (shadcn init may already do this, verify):
    ```typescript
    resolve: { alias: { '@': path.resolve(__dirname, './src') } }
    ```

    Add matching paths to tsconfig.app.json:
    ```json
    "paths": { "@/*": ["./src/*"] }
    ```

    Create src/lib/supabase.ts — typed Supabase client singleton:
    ```typescript
    import { createClient } from '@supabase/supabase-js'
    import type { Database } from '../types/database'

    const supabaseUrl = import.meta.env.VITE_SUPABASE_URL as string
    const supabaseAnonKey = import.meta.env.VITE_SUPABASE_ANON_KEY as string

    if (!supabaseUrl || !supabaseAnonKey) {
      throw new Error('Missing VITE_SUPABASE_URL or VITE_SUPABASE_ANON_KEY environment variables')
    }

    export const supabase = createClient<Database>(supabaseUrl, supabaseAnonKey)
    ```

    Create src/types/database.ts — placeholder until Phase 2 adds public schema tables:
    ```typescript
    // Generated types will be added when public schema tables exist.
    // Run: supabase gen types typescript --local > src/types/database.ts
    export type Database = {
      public: {
        Tables: Record<string, never>
        Views: Record<string, never>
        Functions: Record<string, never>
        Enums: Record<string, never>
      }
    }
    ```

    Create .env.local.example (committed, shows required vars without values):
    ```
    VITE_SUPABASE_URL=http://localhost:54321
    VITE_SUPABASE_ANON_KEY=your-local-anon-key-from-supabase-status
    ```

    Ensure .gitignore includes: `.env.local`, `node_modules/`, `dist/`

    Replace src/App.tsx with a minimal placeholder (routes wired in Plan 02):
    ```typescript
    export function App() {
      return <div>Loading...</div>
    }
    ```

    Replace src/main.tsx with:
    ```typescript
    import { StrictMode } from 'react'
    import { createRoot } from 'react-dom/client'
    import './index.css'
    import { App } from './App'

    createRoot(document.getElementById('root')!).render(
      <StrictMode>
        <App />
      </StrictMode>,
    )
    ```
  </action>
  <verify>
    <automated>npm run build 2>&amp;&amp; echo "BUILD OK"</automated>
  </verify>
  <done>
    `npm run build` exits 0 with no TypeScript errors. `npm run dev` starts without errors and renders the placeholder at localhost:5173. The `@/` import alias resolves in TypeScript (no IDE errors on `import { supabase } from '@/lib/supabase'`).
  </done>
</task>

<task type="auto">
  <name>Task 2: Install Vitest + write test stubs (Wave 0)</name>
  <files>
    vite.config.ts, src/test/setup.ts,
    src/pages/AuthPage.test.tsx,
    src/components/ProtectedRoute.test.tsx,
    src/components/Sidebar.test.tsx
  </files>
  <action>
    Install test dependencies:
    ```bash
    npm install -D vitest @testing-library/react @testing-library/dom @testing-library/jest-dom jsdom
    ```

    Update vite.config.ts to add test configuration block (merge with existing content — do NOT replace the existing resolve.alias or plugins):
    ```typescript
    /// <reference types="vitest" />
    // Add to defineConfig:
    test: {
      globals: true,
      environment: 'jsdom',
      setupFiles: ['./src/test/setup.ts'],
    }
    ```

    Create src/test/setup.ts:
    ```typescript
    import '@testing-library/jest-dom'
    ```

    Create src/pages/AuthPage.test.tsx — stubs that FAIL until AuthPage is implemented:
    ```typescript
    import { describe, it, expect, vi } from 'vitest'

    // Stub: AuthPage does not exist yet. These tests will pass once Plan 04 implements it.
    // AUTH-01: email/password sign-up
    // AUTH-02: Google OAuth sign-in

    describe('AuthPage', () => {
      it.todo('renders sign-in form with email and password fields')
      it.todo('calls supabase.auth.signUp on create account submit')
      it.todo('calls supabase.auth.signInWithPassword on sign-in submit')
      it.todo('shows inline error when credentials are invalid')
      it.todo('renders "Continue with Google" button')
      it.todo('calls supabase.auth.signInWithOAuth with provider google on Google button click')
    })
    ```

    Create src/components/ProtectedRoute.test.tsx — stubs for session state handling:
    ```typescript
    import { describe, it } from 'vitest'

    // Stub: ProtectedRoute does not exist yet. These tests will pass once Plan 02 implements it.
    // AUTH-04: session persists across refresh (undefined/null/session states)

    describe('ProtectedRoute', () => {
      it.todo('renders loading spinner when session is undefined (still loading)')
      it.todo('redirects to /auth when session is null (unauthenticated)')
      it.todo('renders children when session is a valid Session object')
    })
    ```

    Create src/components/Sidebar.test.tsx — stub for sign-out:
    ```typescript
    import { describe, it } from 'vitest'

    // Stub: Sidebar does not exist yet. These tests will pass once Plan 05 implements it.
    // AUTH-05: user can log out from any page

    describe('Sidebar', () => {
      it.todo('renders navigation links for My Wheel, Snapshots, Trend, Settings')
      it.todo('calls signOut when the sign-out button is clicked')
    })
    ```

    Note: Using `it.todo` means these tests are acknowledged but skipped — they do not fail the suite, allowing `npm test -- --run` to pass. Plans 02, 04, and 05 will replace these stubs with real passing tests.
  </action>
  <verify>
    <automated>npm test -- --run</automated>
  </verify>
  <done>
    `npm test -- --run` exits 0. All todo tests are listed as "skipped". No test failures. The test framework is operational.
  </done>
</task>

</tasks>

<verification>
After both tasks:
- `npm run build` exits 0 (no TypeScript errors)
- `npm test -- --run` exits 0 (all todos, no failures)
- `npm run dev` starts Vite on localhost:5173
- `@/lib/supabase` import resolves in TypeScript
- src/test/setup.ts, AuthPage.test.tsx, ProtectedRoute.test.tsx, Sidebar.test.tsx all exist
</verification>

<success_criteria>
- Vite project compiles with zero TypeScript errors
- Tailwind CSS with warm/earthy brand palette configured
- shadcn/ui initialized with `@/` path alias working
- Supabase client singleton created (typed with Database placeholder)
- Vitest test suite runs and passes (stub tests are todos, not failures)
- .env.local.example committed showing required env vars
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
