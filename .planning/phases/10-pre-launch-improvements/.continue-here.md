---
phase: 10-pre-launch-improvements
plan: 02
wave: 1
status: in_progress
last_updated: 2026-03-22T20:31:12.977Z
---

<current_state>
Executing Phase 10 via /gsd:execute-phase 10. Wave 1 is partially done.

Plans 10-01, 10-03, 10-04, 10-05 are committed with implementation. Plan 10-02 (useWheel soft-delete hook) is partially implemented — interface and select query updated, but the core logic (canCreateWheel fix, firstWheel fix, softDeleteWheel/undoDeleteWheel functions, return object) is NOT yet added. Tests for 10-02 and 10-03 are still it.todo stubs.

Wave 2 (10-06) and Wave 3 (10-07) have not started.

SUMMARY.md files are missing for plans 10-03, 10-04, 10-05.
</current_state>

<completed_work>

- **10-01** ✓ COMMITTED (0f05315) — WheelRow + deleted_at type, test stubs (useWheel 5 stubs, WheelPage 6 stubs, SnapshotsPage 3 stubs). SUMMARY.md committed (975fa96).
- **10-03** ✓ COMMITTED (2ec29c8) — SnapshotsPage.tsx: Delete button per row, pendingDeleteId state, deleteSnapshot() with optimistic removal. **Tests still it.todo stubs** — Task 2 not done.
- **10-04** ✓ COMMITTED (4e988e5, 55bd0ec) — Sidebar.tsx legal footer (Terms/Privacy NavLinks), Sidebar.test.tsx 2 passing tests. **SUMMARY.md missing.**
- **10-05** ✓ COMMITTED (a3c9470, f97cdc5) — WheelChart.tsx customTick renders `<line>` spoke when highlighted, WheelChart.test.tsx 2 passing tests. **SUMMARY.md missing.**
- **10-02** ⚠ PARTIAL (975fa96 WIP) — UseWheelResult interface has softDeleteWheel/undoDeleteWheel. Wheels query selects deleted_at. **Core logic NOT YET ADDED.**
- Migration file committed: `supabase/migrations/20260321000001_wheel_soft_delete.sql`

</completed_work>

<remaining_work>

### Plan 10-02: useWheel soft-delete hook (HIGHEST PRIORITY — Wave 2 depends on it)

In `src/hooks/useWheel.ts`, the following changes are still needed (see 10-02-PLAN.md for exact code):

1. **Fix canCreateWheel derivation** (after `setWheels(allWheels)`):
   ```typescript
   const activeWheels = allWheels.filter(w => !w.deleted_at)
   setCanCreateWheel(userTier === 'premium' || activeWheels.length === 0)
   const firstWheel = activeWheels[0] ?? null
   ```
   Replace the existing: `setCanCreateWheel(userTier === 'premium' || allWheels.length === 0)` and `const firstWheel = allWheels[0] ?? null`

2. **Add softDeleteWheel function** (after `renameWheel`): Full implementation in 10-02-PLAN.md Task 1 step 4.

3. **Add undoDeleteWheel function** (after softDeleteWheel): Full implementation in 10-02-PLAN.md Task 1 step 5.

4. **Add to return object**: `softDeleteWheel, undoDeleteWheel`

5. **Replace 5 it.todo stubs** in `useWheel.test.ts` describe('soft delete') with real tests (see 10-02-PLAN.md Task 2).

6. **Commit Task 1** then **commit Task 2**.

### Plan 10-03: SnapshotsPage tests (Task 2 only)

In `src/pages/SnapshotsPage.test.tsx`:
- Add `vi.mock('@/lib/supabase', ...)` mock (supabase is now directly imported by SnapshotsPage.tsx)
- Replace 3 it.todo stubs in describe('snapshot delete') with real tests using userEvent
- See 10-03-PLAN.md Task 2 for exact test structure
- Commit + create 10-03-SUMMARY.md

### Missing SUMMARY.md files

Create these (then commit together):
- `.planning/phases/10-pre-launch-improvements/10-03-SUMMARY.md`
- `.planning/phases/10-pre-launch-improvements/10-04-SUMMARY.md`
- `.planning/phases/10-pre-launch-improvements/10-05-SUMMARY.md`

Use `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" roadmap update-plan-progress "10" "XX" "complete"` for each.

### Wave 2: Plan 10-06 (WheelPage soft-delete UI)

Depends on 10-02 being complete. See 10-06-PLAN.md.

### Wave 3: Plan 10-07 (human browser verification checkpoint)

Requires Supabase running. Note: Docker port 54322 had binding issues — check `supabase start` before browser testing.

</remaining_work>

<decisions_made>

- Subagents were denied Edit/Bash tools by the permission system. Orchestrator (main Claude) took over and executed plans directly.
- `supabase db reset` was skipped due to Docker port 54322 not being available (Windows port binding issue). Migration SQL file exists and will apply on next successful `supabase start`.
- Sidebar.test.tsx and WheelChart.test.tsx already have **real tests** (not stubs) — 10-04/10-05 agents wrote the full tests directly. This is better than stubs, so no stub-to-real conversion needed for those files.
- SnapshotsPage.test.tsx needs a `vi.mock('@/lib/supabase')` added because SnapshotsPage.tsx now directly imports supabase for the delete call.

</decisions_made>

<blockers>

- **Docker port 54322**: `supabase db reset` and `supabase start` fail due to Windows port binding issue. Migration is committed but not applied to local DB. Will need Docker restart or port fix before Wave 3 browser testing.
- **Subagent tool permissions**: Subagents spawned via Agent tool had Edit/Bash denied. Orchestrator handled the work directly. For next session, same pattern — orchestrator executes plans directly (don't spawn subagents).

</blockers>

<context>
Executing /gsd:execute-phase 10. Wave 1 is ~80% done. The critical missing piece is the useWheel.ts soft-delete logic (10-02) because Wave 2 (WheelPage UI) imports softDeleteWheel and undoDeleteWheel from it.

After completing 10-02 and 10-03, Wave 2 (10-06) can run — it adds the Delete button and Undo banner to WheelPage.tsx. Then Wave 3 (10-07) is human browser verification.

The test structure in useWheel.test.ts uses a `buildChain` mock pattern. For soft-delete tests, the tricky part is that `supabase.from` is called multiple times in sequence. Use `vi.mocked(supabase.from).mockImplementation()` to differentiate per table.
</context>

<next_action>
Start by completing useWheel.ts (10-02 Task 1):

1. Open `src/hooks/useWheel.ts`
2. Fix lines ~93-97 (canCreateWheel + firstWheel derivation)
3. Add softDeleteWheel function after renameWheel (~line 197)
4. Add undoDeleteWheel function after softDeleteWheel
5. Add both to return object (~line 235)
6. Run `npm run build` to verify
7. Then do 10-02 Task 2 (replace 5 stubs in useWheel.test.ts)

Full code for all functions is in `.planning/phases/10-pre-launch-improvements/10-02-PLAN.md`.

Resume with: `/gsd:resume-work`
</next_action>
