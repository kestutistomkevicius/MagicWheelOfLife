---
phase: 10-pre-launch-improvements
plan: "07"
type: execute
wave: 3
depends_on: ["10-03", "10-04", "10-05", "10-06"]
files_modified: []
autonomous: false
requirements:
  - SOFT-DELETE-UI
  - SNAPSHOT-DELETE
  - AUTHENTICATED-FOOTER
  - DUESOON-HIGHLIGHT
must_haves:
  truths:
    - "Soft-deleting a wheel shows the pending-deletion suffix and Undo button"
    - "Undo restores the wheel as active"
    - "Empty state shows Recover section when all wheels are deleted"
    - "Snapshot delete with confirmation removes the snapshot from the list"
    - "Terms and Privacy links are visible in the Sidebar on every authenticated page"
    - "Hovering a due-soon item highlights the spoke and label in the wheel chart simultaneously"
  artifacts: []
  key_links: []
---

<objective>
Human verification of all 5 Phase 10 success criteria in the browser against the local Supabase instance.

Purpose: Automated tests cover logic and rendering; this checkpoint confirms the full visual and interactive experience works end-to-end. Catches any cx/cy spoke positioning issues, visual Tailwind rendering, and actual database round-trips that unit tests cannot verify.
Output: User confirmation that all 5 success criteria are met, or a list of issues to fix.
</objective>

<execution_context>
@C:/Users/KestutisTomkevicius/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/KestutisTomkevicius/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-pre-launch-improvements/10-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and start local services</name>
  <files></files>
  <action>
    Run the full automated test suite and start local services for browser verification.

    Step 1 — Full test suite:
    ```bash
    npm test -- --run
    ```
    All tests must be green before proceeding. If any test fails, fix it before starting services.

    Step 2 — Start Supabase and Vite:
    ```bash
    npx supabase start
    npm run dev
    ```
    App will be available at http://localhost:5173.
    Confirm both services started without error before handing off to the user.
  </action>
  <verify>
    <automated>npm test -- --run 2>&1 | tail -5</automated>
  </verify>
  <done>
    `npm test -- --run` exits 0, all tests green. Supabase and Vite are running.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify all 5 Phase 10 improvements in the browser</name>
  <files></files>
  <action>
    Present the verification instructions below to the user and pause for their response.
  </action>
  <verify>
    <automated>MANUAL — user confirms all 5 criteria pass in browser</automated>
  </verify>
  <done>User types "approved"</done>
  <what-built>
    All 5 Phase 10 improvements:
    1. Soft-delete wheels with 10-min undo window (WheelPage)
    2. "Recover a wheel" empty state section (WheelPage)
    3. Snapshot hard-delete with inline confirmation (SnapshotsPage)
    4. Terms + Privacy footer links (Sidebar)
    5. DueSoon hover highlights spoke line + label simultaneously (WheelChart)
  </what-built>
  <how-to-verify>
    Open http://localhost:5173 and sign in with the dev free-tier user (user1@example.com or the seed free user).

    --- Success criterion 1: Soft-delete with undo ---
    1. Go to /wheel. Confirm you see "Delete wheel" button near the wheel name.
    2. Click "Delete wheel". Confirm a pending-deletion banner appears below the heading showing "[Wheel Name] — Deleting in ~10 min" and an Undo button.
    3. Click Undo. Confirm the banner disappears and the wheel is active again (renders normally).
    4. Click "Delete wheel" again. Confirm the pending-deletion banner reappears.

    --- Success criterion 2: Empty state recovery ---
    5. With the wheel soft-deleted (from step 4), if this is the only wheel, confirm the main WheelPage content is replaced by an empty state that includes a "Recover a wheel" section listing the deleted wheel with an Undo button.
    6. Click Undo in the recovery section. Confirm the wheel becomes active.

    --- Success criterion 3: Snapshot delete ---
    7. Go to /snapshots. Confirm a "Delete" button appears on each snapshot row.
    8. Click Delete on one snapshot. Confirm "Confirm delete" and "Cancel" appear in place of the Delete button.
    9. Click Cancel. Confirm the snapshot row is restored to normal.
    10. Click Delete again, then "Confirm delete". Confirm the snapshot disappears from the list immediately.

    --- Success criterion 4: Sidebar footer ---
    11. On any authenticated page, look at the bottom of the Sidebar.
    12. Confirm "Terms" and "Privacy" links are visible at the very bottom.
    13. Click Terms — confirm it navigates to /terms (Terms of Service page).
    14. Click Privacy — confirm it navigates to /privacy (Privacy Policy page).

    --- Success criterion 5: DueSoon hover highlight ---
    15. Go to /wheel. Ensure at least one action item has a deadline within the next 7 days (create one in a category if needed).
    16. Hover over an item in the DueSoon widget.
    17. Confirm BOTH the axis label text AND the spoke line (from chart center to that axis) change to the highlight color simultaneously.
    18. Move the mouse away. Confirm both revert to normal.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 5 criteria pass.
    Or describe which criteria failed and what you observed — Claude will investigate.
  </resume-signal>
</task>

</tasks>

<verification>
User types "approved" confirming all 5 success criteria are visually and functionally correct.
</verification>

<success_criteria>
All 5 Phase 10 success criteria confirmed working in the browser by the user.
</success_criteria>

<output>
After completion, create `.planning/phases/10-pre-launch-improvements/10-07-SUMMARY.md`
</output>
