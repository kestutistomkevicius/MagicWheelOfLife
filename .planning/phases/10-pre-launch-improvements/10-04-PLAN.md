---
phase: 10-pre-launch-improvements
plan: "04"
type: execute
wave: 1
depends_on: ["10-01"]
files_modified:
  - src/components/Sidebar.tsx
  - src/components/Sidebar.test.tsx
autonomous: true
requirements:
  - AUTHENTICATED-FOOTER
must_haves:
  truths:
    - "Sidebar renders a Terms link that navigates to /terms"
    - "Sidebar renders a Privacy link that navigates to /privacy"
    - "Links are visible on every authenticated page (Sidebar is in AppShell which wraps all auth pages)"
    - "Links are compact and visually subdued, consistent with sidebar aesthetics"
  artifacts:
    - path: "src/components/Sidebar.tsx"
      provides: "Legal footer with Terms and Privacy NavLinks pinned at the bottom of the aside"
      contains: "/terms"
    - path: "src/components/Sidebar.test.tsx"
      provides: "2 passing tests for footer links"
  key_links:
    - from: "Sidebar.tsx aside bottom"
      to: "NavLink to='/terms' and NavLink to='/privacy'"
      via: "new legal footer div after User+Sign-out block"
      pattern: "/terms|/privacy"
---

<objective>
Add a compact legal footer (Terms + Privacy links) pinned at the bottom of the Sidebar component.

Purpose: Satisfies success criterion 4 — Terms and Privacy links visible on every authenticated page. The Sidebar is already inside AppShell which wraps all auth routes, so a single change here covers the entire app. Both /terms and /privacy routes already exist.
Output: Updated Sidebar.tsx with legal footer div; 2 passing tests replacing it.todo stubs.
</objective>

<execution_context>
@C:/Users/KestutisTomkevicius/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/KestutisTomkevicius/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-pre-launch-improvements/10-RESEARCH.md

<interfaces>
<!-- Current Sidebar.tsx bottom structure — executor modifies this -->
From src/components/Sidebar.tsx (last ~25 lines):

```tsx
      {/* User + Sign out */}
      <div className="border-t border-stone-700 p-3 space-y-2">
        <div className="flex items-center gap-3 px-1">
          {avatarUrl ? (
            <img src={avatarUrl} alt="Your avatar" className="h-7 w-7 shrink-0 rounded-full object-cover" />
          ) : (
            <div className="flex h-7 w-7 shrink-0 items-center justify-center rounded-full bg-brand-400 text-xs font-bold text-white">
              {initial}
            </div>
          )}
          <span className="truncate text-xs text-stone-300">{email}</span>
        </div>
        <button onClick={signOut} className="flex w-full items-center gap-3 rounded-lg px-3 py-2 text-sm text-stone-400 transition-colors hover:bg-stone-700 hover:text-stone-100">
          <LogOut className="h-4 w-4 shrink-0" aria-hidden="true" />
          Sign out
        </button>
      </div>
      <FeatureRequestModal open={feedbackOpen} userId={userId} onClose={() => setFeedbackOpen(false)} />
    </aside>
```

NavLink is already imported from 'react-router' in Sidebar.tsx.
TermsPage and PrivacyPage are routed in App.tsx at /terms and /privacy.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add legal footer to Sidebar</name>
  <files>src/components/Sidebar.tsx</files>
  <action>
    Insert a legal footer div immediately before `<FeatureRequestModal>` (after the User+Sign-out block)
    in the Sidebar component's aside element. The aside uses `flex flex-col h-screen` so this div
    will naturally appear at the bottom.

    Add this block:
    ```tsx
    {/* Legal footer */}
    <div className="px-4 pb-3 flex gap-4">
      <NavLink
        to="/terms"
        className="text-xs text-stone-500 hover:text-stone-300 transition-colors"
      >
        Terms
      </NavLink>
      <NavLink
        to="/privacy"
        className="text-xs text-stone-500 hover:text-stone-300 transition-colors"
      >
        Privacy
      </NavLink>
    </div>
    ```

    NavLink is already imported in Sidebar.tsx from 'react-router'. No new imports needed.
    Do not change any other part of the Sidebar.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    `npm run build` exits 0. Sidebar.tsx contains NavLink to="/terms" and NavLink to="/privacy".
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Sidebar footer tests</name>
  <files>src/components/Sidebar.test.tsx</files>
  <behavior>
    - Sidebar renders a link with text "Terms" and href/to="/terms"
    - Sidebar renders a link with text "Privacy" and href/to="/privacy"
  </behavior>
  <action>
    Replace the 2 it.todo stubs in the 'legal footer' describe block (from Plan 01) with real tests.

    Follow the existing test setup pattern in Sidebar.test.tsx. The existing tests mock
    useAuth, useProfile, and useWheel. Use the same mock setup.

    Test 1 "renders Terms link pointing to /terms":
    ```typescript
    it('renders Terms link pointing to /terms', async () => {
      render(<Sidebar />)
      const link = screen.getByRole('link', { name: /terms/i })
      expect(link).toBeInTheDocument()
      expect(link).toHaveAttribute('href', '/terms')
    })
    ```

    Test 2 "renders Privacy link pointing to /privacy":
    ```typescript
    it('renders Privacy link pointing to /privacy', async () => {
      render(<Sidebar />)
      const link = screen.getByRole('link', { name: /privacy/i })
      expect(link).toBeInTheDocument()
      expect(link).toHaveAttribute('href', '/privacy')
    })
    ```

    Wrap both tests inside the existing 'legal footer' describe block.
    Ensure the existing mock setup (MemoryRouter or BrowserRouter wrapper if needed) is applied —
    check how other Sidebar tests wrap the component.
  </action>
  <verify>
    <automated>npm test -- --run src/components/Sidebar.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>
    Both legal footer tests pass. No it.todo remaining in the 'legal footer' describe block.
    Full test suite still green.
  </done>
</task>

</tasks>

<verification>
`npm test -- --run src/components/Sidebar.test.tsx` — all tests pass.
`npm run build` — no TypeScript errors.
Sidebar.tsx contains Terms and Privacy NavLinks at the bottom.
</verification>

<success_criteria>
- Sidebar.tsx has legal footer with NavLink to="/terms" and NavLink to="/privacy"
- Both tests pass
- `npm test -- --run` fully green
</success_criteria>

<output>
After completion, create `.planning/phases/10-pre-launch-improvements/10-04-SUMMARY.md`
</output>
