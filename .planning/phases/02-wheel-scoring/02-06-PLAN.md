---
phase: 02-wheel-scoring
plan: 06
type: execute
wave: 4
depends_on: [02-05]
files_modified: []
autonomous: false
requirements: [WHEEL-01, WHEEL-02, WHEEL-03, WHEEL-04, WHEEL-05, WHEEL-06, WHEEL-07, SCORE-01, SCORE-02, SCORE-03]

must_haves:
  truths:
    - "Free-tier seed user's wheel loads with 8 categories and visible radar chart"
    - "Dragging a slider causes the radar chart to redraw in real time with no save action"
    - "Adding a new category appends it to the list and redraws the chart"
    - "Renaming and removing categories work without errors"
    - "Free-tier user attempting to create a second wheel sees the upgrade prompt"
    - "Premium-tier user can create a second wheel successfully"
    - "All automated tests pass before this checkpoint is reached"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete Phase 2 wheel experience. Confirm the radar chart, sliders, category management, and tier enforcement all work correctly in a real browser against the local Supabase instance.

Purpose: Automated tests validate logic. This checkpoint validates the visual and interactive behavior that only a human can confirm — chart rendering, smooth slider drag, responsive layout.
Output: Phase 2 signed off or issues captured for gap closure.
</objective>

<execution_context>
@C:/Users/KestutisTomkevicius/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/KestutisTomkevicius/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-wheel-scoring/02-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify Phase 2 wheel experience end-to-end</name>
  <action>Run automated suite, then verify all interactive wheel behaviors in a real browser with local Supabase.</action>
  <verify>
    <automated>npm test -- --run 2>&1 | tail -5</automated>
  </verify>
  <done>All 9 verification tests pass and `npm test -- --run` shows 0 failures.</done>
  <what-built>
Complete Phase 2 wheel experience:
- Interactive radar chart with dual as-is/to-be series (Recharts RadarChart)
- Per-category sliders that redraw the chart in real time without saving
- Category management: add, rename, remove with min/max enforcement
- Wheel creation flow: template (8 categories) or blank, with free-tier upgrade prompt
- All automated tests green
  </what-built>
  <how-to-verify>
**Setup:** Ensure `supabase start` is running and `npm run dev` is serving at http://localhost:5173. Run `supabase db reset` to ensure fresh seed data.

**Test 1 — Automated suite (run first):**
Open a terminal and run `npm test -- --run`. Confirm 0 failures before proceeding.

**Test 2 — Free-tier wheel loads:**
1. Go to http://localhost:5173 and sign in as `free@test.com` / `test123`
2. Navigate to the Wheel page (should redirect there after login)
3. Confirm: radar chart is visible with 8 colored areas (as-is amber, to-be blue)
4. Confirm: 8 category names are visible as labels around the chart perimeter
5. Confirm: all 8 categories appear in the slider list below/beside the chart

**Test 3 — Real-time slider updates:**
1. While on the free-tier wheel page, drag the "Health" as-is slider from its current value to 1
2. Confirm: the radar chart redraws immediately as you drag — no save button needed
3. Drag the slider back to 8 and confirm the chart updates again
4. Refresh the page (Ctrl+R / Cmd+R) and confirm the score was persisted (chart shows the last committed value)

**Test 4 — Add category:**
1. Click the "Add category" button
2. Type a new category name (e.g., "Spirituality") and confirm
3. Confirm: new category appears in the slider list with default scores
4. Confirm: radar chart redraws with the new category segment

**Test 5 — Rename and remove category:**
1. Click on a category name to rename it; change it to something else and confirm
2. Confirm: the new name appears in both the list and the chart axis label
3. Click remove on a non-essential category (while count > 3)
4. Confirm: category disappears from list and chart redraws
5. Reduce to exactly 3 categories; confirm the remove button is disabled

**Test 6 — Max category limit:**
1. Keep adding categories until you reach 12
2. Confirm: the "Add category" button becomes disabled at 12

**Test 7 — Free-tier wheel limit (WHEEL-06):**
1. While signed in as free@test.com, click "Create new wheel" (or however the button is labeled)
2. Confirm: an upgrade prompt appears ("Upgrade to Premium" or similar) — NOT a creation form
3. Confirm: clicking the dismiss/close button closes the prompt without creating a wheel

**Test 8 — Premium-tier wheel creation (WHEEL-07):**
1. Sign out and sign in as `premium@test.com` / `test123`
2. Go to the Wheel page — existing wheel should load
3. Click "Create new wheel"
4. Confirm: the creation modal appears (template vs. blank choice) — NOT an upgrade prompt
5. Choose "Start from template" and confirm
6. Confirm: a new wheel is created with 8 categories and the chart shows it

**Test 9 — Manual DB-level enforcement smoke test (WHEEL-06):**
1. Using Supabase Studio at http://localhost:54323, open the SQL editor
2. Run: `SET LOCAL ROLE authenticated; SET LOCAL "request.jwt.claims" = '{"sub":"00000000-0000-0000-0000-000000000001","role":"authenticated"}'; INSERT INTO public.wheels (user_id, name) VALUES ('00000000-0000-0000-0000-000000000001', 'Second Wheel');`
3. Confirm: the INSERT is rejected by RLS (error message about policy violation)
  </how-to-verify>
  <resume-signal>
Type "approved" if all 9 tests pass. If issues are found, describe them so gap-closure plans can be created.
  </resume-signal>
</task>

</tasks>

<verification>
All 9 verification tests passed as described in the task above.
</verification>

<success_criteria>
- All 9 human verification tests pass
- `npm test -- --run` passes with 0 failures
- Phase 2 signed off: users can create a wheel, see the radar chart, score categories in real time, manage categories, and tier enforcement is visible in the UI and enforced at the DB level
</success_criteria>

<output>
After completion, create `.planning/phases/02-wheel-scoring/02-06-SUMMARY.md`
</output>
