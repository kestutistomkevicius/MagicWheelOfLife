---
phase: 02-wheel-scoring
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useWheel.test.ts
  - src/hooks/useCategories.test.ts
  - src/components/CategorySlider.test.tsx
  - src/components/WheelChart.test.tsx
  - src/components/WheelPage.test.tsx
autonomous: true
requirements: [WHEEL-03, WHEEL-04, WHEEL-05, SCORE-01, SCORE-02, SCORE-03]

must_haves:
  truths:
    - "All 5 test files exist and the suite runs green (stubs use it.todo so they pass without implementation)"
    - "Every requirement in WHEEL-03 through WHEEL-07 and SCORE-01 through SCORE-03 has at least one named it.todo stub"
    - "No test file imports real implementation files — stubs are self-contained"
  artifacts:
    - path: "src/hooks/useWheel.test.ts"
      provides: "Test stubs for wheel load, create from template, create from blank"
    - path: "src/hooks/useCategories.test.ts"
      provides: "Test stubs for add category (max 12), rename, remove (min 3)"
    - path: "src/components/CategorySlider.test.tsx"
      provides: "Test stubs for as-is slider, to-be slider, onChange/onCommit behavior"
    - path: "src/components/WheelChart.test.tsx"
      provides: "Test stub for chart renders with data array"
    - path: "src/components/WheelPage.test.tsx"
      provides: "Test stubs for snapshot warning dialog, upgrade prompt, real-time chart update"
  key_links:
    - from: "npm test -- --run"
      to: "all 5 test files"
      via: "Vitest test runner"
      pattern: "it.todo"
---

<objective>
Create the Wave 0 test scaffolds for all Phase 2 components and hooks. These are `it.todo()` stubs — they name the expected behaviors without any implementation, ensuring Vitest has targets before code is written and fulfilling the Nyquist sampling requirement.

Purpose: Plans 03–05 require test files to exist (verify commands reference specific test files). Creating stubs now means every subsequent plan can immediately run its verify command and see green (pending todos are not failures in Vitest).
Output: 5 test files with named stubs — no production code, no imports of implementation files yet.
</objective>

<execution_context>
@C:/Users/KestutisTomkevicius/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/KestutisTomkevicius/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-wheel-scoring/02-VALIDATION.md
@.planning/phases/02-wheel-scoring/02-RESEARCH.md

Phase 1 established pattern: `it.todo()` stubs are used for unimplemented tests — acknowledged in output but not failing. Test files use Vitest imports (`import { describe, it } from 'vitest'`). React Testing Library is available (`@testing-library/react`).
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create hook test scaffolds</name>
  <files>src/hooks/useWheel.test.ts, src/hooks/useCategories.test.ts</files>
  <action>
**src/hooks/useWheel.test.ts**
```typescript
import { describe, it } from 'vitest'

describe('useWheel', () => {
  describe('loading wheel', () => {
    it.todo('returns loading=true initially then loading=false after fetch')
    it.todo('returns wheel and categories when user has an existing wheel')
    it.todo('returns wheel=null when user has no wheels')
  })

  describe('createWheel - template (WHEEL-01)', () => {
    it.todo('inserts a wheel row and 8 default categories when mode is template')
    it.todo('returns the new wheel id after creation')
  })

  describe('createWheel - blank (WHEEL-02)', () => {
    it.todo('inserts a wheel row with 0 categories when mode is blank')
  })

  describe('tier enforcement (WHEEL-06, WHEEL-07)', () => {
    it.todo('returns canCreateWheel=false when free-tier user already has 1 wheel')
    it.todo('returns canCreateWheel=true when premium-tier user already has 1 wheel')
  })
})
```

**src/hooks/useCategories.test.ts**
```typescript
import { describe, it } from 'vitest'

describe('useCategories', () => {
  describe('addCategory (WHEEL-03)', () => {
    it.todo('inserts a new category and returns updated list')
    it.todo('blocks insertion when category count is already 12')
    it.todo('assigns next sequential position to new category')
  })

  describe('renameCategory (WHEEL-04)', () => {
    it.todo('updates category name in Supabase')
    it.todo('calls onSnapshotWarning callback before rename when hasSnapshots is true')
    it.todo('proceeds without warning when hasSnapshots is false')
  })

  describe('removeCategory (WHEEL-05)', () => {
    it.todo('deletes category from Supabase')
    it.todo('blocks deletion when category count is already 3')
    it.todo('calls onSnapshotWarning callback before remove when hasSnapshots is true')
  })
})
```
  </action>
  <verify>
    <automated>npm test -- --run src/hooks/useWheel.test.ts src/hooks/useCategories.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>Both test files run with all tests in "todo" state (0 failures, 0 errors). Vitest output shows "X todo" lines.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create component test scaffolds</name>
  <files>src/components/CategorySlider.test.tsx, src/components/WheelChart.test.tsx, src/components/WheelPage.test.tsx</files>
  <action>
**src/components/CategorySlider.test.tsx**
```typescript
import { describe, it } from 'vitest'

describe('CategorySlider', () => {
  describe('as-is slider (SCORE-01)', () => {
    it.todo('renders with correct initial value for as-is score')
    it.todo('calls onChange with new value when slider moves')
    it.todo('calls onCommit with value on pointer up (not on every change)')
  })

  describe('to-be slider (SCORE-02)', () => {
    it.todo('renders with correct initial value for to-be score')
    it.todo('calls onChange with new value when slider moves')
    it.todo('calls onCommit with value on pointer up')
  })
})
```

**src/components/WheelChart.test.tsx**
```typescript
import { describe, it } from 'vitest'

describe('WheelChart', () => {
  it.todo('renders without crashing when given a non-empty data array')
  it.todo('renders an empty/placeholder state when data array is empty')
  it.todo('displays category names from the data prop on the angle axis')
})
```

**src/components/WheelPage.test.tsx**
```typescript
import { describe, it } from 'vitest'

describe('WheelPage', () => {
  describe('real-time chart update (SCORE-03)', () => {
    it.todo('updates chart data immediately when slider onChange fires — no Supabase call yet')
    it.todo('calls Supabase update only when slider onCommit fires (pointer up)')
  })

  describe('snapshot warning dialog (WHEEL-04, WHEEL-05)', () => {
    it.todo('shows AlertDialog before rename when wheel has existing snapshots')
    it.todo('skips AlertDialog and renames immediately when wheel has no snapshots')
    it.todo('shows AlertDialog before remove when wheel has existing snapshots')
    it.todo('skips AlertDialog and removes immediately when wheel has no snapshots')
  })

  describe('upgrade prompt (WHEEL-06, WHEEL-07)', () => {
    it.todo('shows upgrade prompt dialog when free-tier user clicks create wheel and already has one wheel')
    it.todo('opens create wheel modal when premium-tier user clicks create wheel and already has one wheel')
  })

  describe('category management', () => {
    it.todo('add category button is disabled when category count reaches 12')
    it.todo('remove category button is disabled when category count is 3')
  })
})
```
  </action>
  <verify>
    <automated>npm test -- --run 2>&1 | tail -10</automated>
  </verify>
  <done>All 5 test files run, full suite shows 0 failures and 0 errors. All tests are in todo state. `npm test -- --run` exits with code 0.</done>
</task>

</tasks>

<verification>
- `npm test -- --run` exits 0 with no failures
- Output shows todo stubs for all 5 files
- No test imports production code (no import errors)
</verification>

<success_criteria>
- 5 test files created in correct locations
- All stubs use `it.todo()` — no failing tests
- `npm test -- --run` passes (exit code 0)
</success_criteria>

<output>
After completion, create `.planning/phases/02-wheel-scoring/02-02-SUMMARY.md`
</output>
