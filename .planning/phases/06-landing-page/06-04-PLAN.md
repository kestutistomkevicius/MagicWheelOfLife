---
phase: 06-landing-page
plan: 04
type: execute
wave: 4
depends_on:
  - "06-03"
files_modified: []
autonomous: false
requirements:
  - LAND-01
  - LAND-02
  - LAND-03
  - LAND-04

must_haves:
  truths:
    - "Unauthenticated visitor can load /, see the hero, and click 'Start your wheel' to reach /auth"
    - "Authenticated user is redirected from / to /wheel without seeing the landing page"
    - "Visitor can click 'See how it works' and the page smoothly scrolls to the feature showcase"
    - "Feature showcase section is visible with 3 rows including live WheelChart and ComparisonChart demos"
    - "Social proof section shows 3 testimonial cards"
    - "Pricing section shows Free and Premium columns with correct CTAs"
    - "Footer links to /privacy and /terms which render placeholder stub pages"
    - "Layout is responsive: hero stack on mobile, alternating feature rows on desktop, 3-column testimonials on desktop"
  artifacts:
    - path: "src/pages/LandingPage.tsx"
      provides: "Complete verified landing page"
  key_links:
    - from: "Landing page"
      to: "/auth"
      via: "Primary CTAs"
      pattern: "href.*auth"
---

<objective>
Human verification: confirm the landing page works end-to-end in the browser.

Purpose: The automated tests mock the charts and cannot verify visual rendering, responsive layout, scroll animation, or the full user flow from landing page to signup. Human eyes needed.
Output: All LAND-01 through LAND-04 flows confirmed working.
</objective>

<execution_context>
@C:/Users/KestutisTomkevicius/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/KestutisTomkevicius/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-landing-page/06-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete landing page at / with:
    - Public route at / (unauthenticated access)
    - Nav: wordmark + Sign in + Start free CTA
    - Hero: gradient background, headline, two CTAs, WheelChart preview (8 categories, static data)
    - Feature showcase (id="features"): 3 alternating rows — Wheel scoring, Snapshot comparison, Action items
    - Testimonials: 3 cards with initials avatars (RK amber, MT stone, AS blue)
    - Pricing: Free ($0) / Premium ($5) columns, disabled "Coming soon" Premium CTA, Free CTA to /auth
    - Final CTA section: warm gradient, closing headline, Start your wheel button
    - Footer: copyright + Privacy Policy + Terms of Service links
    - Scroll animations: fade-in on all sections except hero
    - Responsive layout: mobile stacked, desktop alternating feature rows + 3-col testimonials
    - /privacy and /terms render placeholder stub pages
    - Authenticated users redirected from / to /wheel

    Automated tests: all LAND-01 through LAND-04 pass (run `npm test -- --run` to confirm before starting verification).
  </what-built>
  <how-to-verify>
    Run `npx supabase start` if not already running (required for auth session check at /).
    Run `npm run dev` if not already running.
    Open http://localhost:5173 in a browser (logged out / incognito window).

    **LAND-01 — Hero and routing:**
    1. Load http://localhost:5173 — confirm landing page loads (not redirected to /auth)
    2. Confirm hero headline is visible and coaching/aspirational in tone
    3. Confirm WheelChart preview is visible in the hero (radar chart, 8 categories)
    4. Confirm Nav shows "JustAWheelOfLife" wordmark, "Sign in" link, and "Start free →" button
    5. Click "Start your wheel →" primary CTA — confirm redirected to /auth (signup page)
    6. Go back. Click "See how it works" — confirm page smoothly scrolls to the feature showcase section
    7. Open a second browser tab logged in as the dev seed user (free@example.com) — navigate to http://localhost:5173 — confirm immediately redirected to /wheel

    **LAND-02 — Feature showcase:**
    8. Scroll to the feature showcase section
    9. Confirm 3 feature rows are visible with titles and descriptions
    10. Confirm Row 1 (Wheel scoring) shows a live WheelChart radar chart
    11. Confirm Row 2 (Snapshot comparison) shows a ComparisonChart with two overlaid wheels
    12. Confirm Row 3 (Action items) shows a styled checklist card with mixed checked/unchecked items
    13. Confirm sections animate in as you scroll down (fade-up effect)

    **LAND-03 — Testimonials:**
    14. Scroll to "What people are saying" section
    15. Confirm 3 testimonial cards are visible side-by-side (or stacked on mobile)
    16. Confirm each card shows a quote, name, role, and colored initials circle
    17. Confirm the 3 avatar colors are distinct (amber, stone/gray, blue)

    **LAND-04 — Pricing:**
    18. Scroll to pricing section
    19. Confirm two columns: Free ($0/mo) and Premium ($5/mo)
    20. Confirm both show the shared feature list with checkmarks
    21. Confirm Premium shows "Unlimited wheels" as an additional feature
    22. Confirm Premium CTA button is disabled ("Coming soon" — cannot be clicked)
    23. Confirm Free CTA "Start free →" leads to /auth

    **Footer + stub pages:**
    24. Scroll to footer — confirm copyright text and both links visible
    25. Click "Privacy Policy" — confirm /privacy loads with "Coming soon" placeholder content
    26. Go back. Click "Terms of Service" — confirm /terms loads with "Coming soon" placeholder content

    **Responsive (resize browser):**
    27. Resize browser to mobile width (~375px) — confirm hero text is full-width centered, WheelChart appears below text
    28. Confirm feature showcase rows stack vertically (not side-by-side) at mobile width
    29. Confirm testimonials stack vertically at mobile width
    30. Confirm pricing columns stack at mobile width

    **SEO meta (optional visual check):**
    31. Open browser DevTools → Elements → find `<head>` — confirm og:title meta tag with "JustAWheelOfLife" is present
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
Human approval of all LAND-01 through LAND-04 visual and functional flows.
</verification>

<success_criteria>
- All 31 verification steps pass
- Hero loads correctly for unauthenticated visitors
- Authenticated users redirected from / to /wheel
- All 4 sections visible and correctly rendered
- Responsive layout confirmed on mobile and desktop
- Stub pages accessible at /privacy and /terms
</success_criteria>

<output>
After completion, create `.planning/phases/06-landing-page/06-04-SUMMARY.md`
</output>
