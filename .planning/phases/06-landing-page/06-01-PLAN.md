---
phase: 06-landing-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/LandingPage.test.tsx
  - src/hooks/useInView.ts
  - src/pages/PrivacyPage.tsx
  - src/pages/TermsPage.tsx
  - index.html
  - public/og-image.svg
autonomous: true
requirements:
  - LAND-01
  - LAND-02
  - LAND-03
  - LAND-04

must_haves:
  truths:
    - "LandingPage test file exists with it.todo stubs for all LAND-01 through LAND-04 behaviors"
    - "useInView hook exists and exports the ref + inView pair"
    - "PrivacyPage and TermsPage stub pages exist and are renderable"
    - "index.html has correct page title, meta description, Open Graph tags, Twitter Card tags, and commented-out Plausible script stub"
    - "public/og-image.svg placeholder exists at the correct path"
  artifacts:
    - path: "src/pages/LandingPage.test.tsx"
      provides: "Wave 0 test stubs for all landing page requirements"
      exports: []
    - path: "src/hooks/useInView.ts"
      provides: "Intersection Observer scroll hook"
      exports: ["useInView"]
    - path: "src/pages/PrivacyPage.tsx"
      provides: "Placeholder Privacy Policy stub page"
      exports: ["PrivacyPage"]
    - path: "src/pages/TermsPage.tsx"
      provides: "Placeholder Terms of Service stub page"
      exports: ["TermsPage"]
    - path: "index.html"
      provides: "SEO meta tags, OG tags, analytics stub"
      contains: "JustAWheelOfLife"
    - path: "public/og-image.svg"
      provides: "OG image placeholder"
  key_links:
    - from: "src/pages/LandingPage.test.tsx"
      to: "src/pages/LandingPage.tsx"
      via: "import LandingPage"
      pattern: "import.*LandingPage"
---

<objective>
Wave 0 infrastructure: test stubs, scroll hook, stub pages, SEO meta, and OG image placeholder.

Purpose: Establish the test scaffold so every subsequent plan's `npm test` verify command runs green (stubs pass via it.todo). Create the useInView hook and stub pages so they can be imported without error when the LandingPage is wired.
Output: LandingPage.test.tsx with it.todo stubs, useInView hook, PrivacyPage + TermsPage stubs, updated index.html, placeholder OG image.
</objective>

<execution_context>
@C:/Users/KestutisTomkevicius/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/KestutisTomkevicius/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-landing-page/06-CONTEXT.md
@.planning/phases/06-landing-page/06-RESEARCH.md
@.planning/phases/06-landing-page/06-VALIDATION.md
</context>

<interfaces>
<!-- Key types and contracts for this plan. -->

From src/components/WheelChart.tsx:
```typescript
export type WheelChartPoint = {
  category: string
  asis: number
  tobe: number
}
export function WheelChart({ data }: { data: WheelChartPoint[] }): JSX.Element
```

From src/components/ComparisonChart.tsx:
```typescript
import type { SnapshotScoreRow } from '@/types/database'
export function ComparisonChart({
  snap1Scores,
  snap2Scores,
  snap1Label,
  snap2Label,
}: {
  snap1Scores: SnapshotScoreRow[]
  snap2Scores: SnapshotScoreRow[]
  snap1Label: string
  snap2Label: string
}): JSX.Element
// Only uses: category_name, score_asis, score_tobe, position from SnapshotScoreRow
```

Existing mock pattern (from WheelPage.test.tsx, ProtectedRoute.test.tsx):
```typescript
vi.mock('@/contexts/AuthContext', () => ({
  useAuth: vi.fn(),
}))
vi.mock('react-router', () => ({
  useNavigate: vi.fn(),
}))
vi.mock('@/components/WheelChart', () => ({
  WheelChart: () => <div data-testid="wheel-chart-mock" />,
}))
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — LandingPage test stubs + useInView hook</name>
  <files>src/pages/LandingPage.test.tsx, src/hooks/useInView.ts</files>
  <action>
Create `src/hooks/useInView.ts`:
```typescript
import { useEffect, useRef, useState } from 'react'

export function useInView(threshold = 0.15) {
  const ref = useRef<HTMLElement>(null)
  const [inView, setInView] = useState(false)

  useEffect(() => {
    const el = ref.current
    if (!el) return
    const observer = new IntersectionObserver(
      ([entry]) => {
        if (entry.isIntersecting) {
          setInView(true)
          observer.disconnect()
        }
      },
      { threshold }
    )
    observer.observe(el)
    return () => observer.disconnect()
  }, [threshold])

  return { ref, inView }
}
```

Create `src/pages/LandingPage.test.tsx` with it.todo stubs. Mock useAuth and useNavigate. Mock WheelChart and ComparisonChart as data-testid divs (jsdom does not render SVG/Recharts). Follow the exact mock pattern from ProtectedRoute.test.tsx and WheelPage.test.tsx.

Stubs to include (use it.todo for each):
- LAND-01: "shows hero section with value proposition for unauthenticated visitor"
- LAND-01: "hero section contains 'Start your wheel' CTA linking to /auth"
- LAND-01: "returns null while session is undefined (no flash)"
- LAND-01: "redirects authenticated user to /wheel"
- LAND-02: "feature showcase section is present with 3 feature rows"
- LAND-02: "feature showcase renders wheel chart and comparison chart demos"
- LAND-03: "social proof section shows 3 testimonial cards with quote, name, role"
- LAND-04: "pricing section shows Free ($0/mo) and Premium ($5/mo) columns"
- LAND-04: "premium CTA button is disabled with text 'Coming soon'"
- LAND-04: "free CTA links to /auth"

Import ONLY `describe` and `it` from 'vitest' — no LandingPage import yet (file does not exist until plan 02). This is the Wave 0 stub pattern from prior phases.
  </action>
  <verify>
    <automated>npm test -- --run src/pages/LandingPage.test.tsx</automated>
  </verify>
  <done>LandingPage.test.tsx runs with all stubs acknowledged (it.todo output), zero failures. useInView.ts exports useInView function returning { ref, inView }.</done>
</task>

<task type="auto">
  <name>Task 2: Stub pages + SEO meta + OG image placeholder</name>
  <files>src/pages/PrivacyPage.tsx, src/pages/TermsPage.tsx, index.html, public/og-image.svg</files>
  <action>
Create `src/pages/PrivacyPage.tsx`:
```tsx
export function PrivacyPage() {
  return (
    <div className="min-h-screen flex items-center justify-center p-8">
      <div className="text-center max-w-md">
        <h1 className="text-2xl font-semibold mb-4">Privacy Policy</h1>
        <p className="text-muted-foreground">Coming soon — check back before launch.</p>
        <a href="/" className="mt-6 inline-block text-sm text-primary hover:underline">
          ← Back to home
        </a>
      </div>
    </div>
  )
}
```

Create `src/pages/TermsPage.tsx` — identical structure but title "Terms of Service".

Update `index.html` — replace the existing `<title>` and add meta tags in `<head>`. Keep all existing Vite script tags unchanged. Add after `<meta name="viewport">`:
```html
<title>JustAWheelOfLife — Wheel of Life Self-Assessment Tool</title>
<meta name="description" content="Score your life areas, set goals, and track progress with the Wheel of Life. Free to start." />

<!-- Open Graph -->
<meta property="og:title" content="JustAWheelOfLife — Wheel of Life Self-Assessment Tool" />
<meta property="og:description" content="Score your life areas, set goals, and track progress with the Wheel of Life. Free to start." />
<meta property="og:image" content="/og-image.svg" />
<meta property="og:type" content="website" />

<!-- Twitter Card -->
<meta name="twitter:card" content="summary_large_image" />
<meta name="twitter:title" content="JustAWheelOfLife — Wheel of Life Self-Assessment Tool" />
<meta name="twitter:description" content="Score your life areas, set goals, and track progress with the Wheel of Life. Free to start." />
<meta name="twitter:image" content="/og-image.svg" />

<!-- Plausible Analytics (activate when domain is set up) -->
<!-- <script defer data-domain="REPLACE_WITH_DOMAIN" src="https://plausible.io/js/script.js"></script> -->
```

Create `public/og-image.svg` — a minimal 1200x630 SVG placeholder using brand warm colors. Use warm amber background (#fdf8f0), brand amber text (#b45309), centered wordmark "JustAWheelOfLife" and tagline "Wheel of Life Self-Assessment Tool". This is a placeholder only — replaced with a designed PNG before Phase 7 launch.

```svg
<svg xmlns="http://www.w3.org/2000/svg" width="1200" height="630" viewBox="0 0 1200 630">
  <rect width="1200" height="630" fill="#fdf8f0"/>
  <rect x="0" y="0" width="1200" height="630" fill="url(#grad)"/>
  <defs>
    <linearGradient id="grad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" style="stop-color:#fdf8f0;stop-opacity:1" />
      <stop offset="100%" style="stop-color:#faefd8;stop-opacity:1" />
    </linearGradient>
  </defs>
  <text x="600" y="280" font-family="Georgia, serif" font-size="64" font-weight="bold" fill="#92400e" text-anchor="middle">JustAWheelOfLife</text>
  <text x="600" y="360" font-family="Georgia, serif" font-size="32" fill="#b45309" text-anchor="middle">Wheel of Life Self-Assessment Tool</text>
  <text x="600" y="430" font-family="Georgia, serif" font-size="24" fill="#d97706" text-anchor="middle">Free to start.</text>
</svg>
```
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>PrivacyPage and TermsPage export their components. index.html contains the og:title meta tag with "JustAWheelOfLife". public/og-image.svg exists. Build passes with no errors.</done>
</task>

</tasks>

<verification>
After both tasks:
- `npm test -- --run src/pages/LandingPage.test.tsx` — all stubs acknowledged, zero failures
- `npm run build` — clean build, no TypeScript errors
- `index.html` contains `og:title` meta property
</verification>

<success_criteria>
- LandingPage.test.tsx exists with 10 it.todo stubs covering LAND-01 through LAND-04
- useInView.ts exports useInView returning { ref, inView }
- PrivacyPage and TermsPage exist and render placeholder content
- index.html updated with full SEO/OG/Twitter meta block and commented analytics stub
- public/og-image.svg placeholder exists
- npm test green, npm run build green
</success_criteria>

<output>
After completion, create `.planning/phases/06-landing-page/06-01-SUMMARY.md`
</output>
