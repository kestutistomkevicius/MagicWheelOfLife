---
phase: 06-landing-page
plan: 03
type: execute
wave: 3
depends_on:
  - "06-02"
files_modified:
  - src/pages/LandingPage.tsx
  - src/pages/LandingPage.test.tsx
autonomous: true
requirements:
  - LAND-02
  - LAND-03
  - LAND-04

must_haves:
  truths:
    - "Feature showcase section (id='features') has 3 alternating rows: Wheel scoring (WheelChart demo), Snapshot comparison (ComparisonChart demo), Action items (static checklist card)"
    - "Each feature row has a title and a 1–2 sentence description"
    - "Social proof section shows 3 testimonial cards with: quote, name, role, and initials avatar (colored circle)"
    - "Pricing section shows Free ($0/mo) and Premium ($5/mo) columns with feature checklists"
    - "Premium CTA button is disabled and labeled 'Coming soon'"
    - "Free CTA button links to /auth"
    - "Final CTA section with closing headline and 'Start your wheel' button"
    - "Footer with copyright, Privacy Policy link (/privacy), and Terms of Service link (/terms)"
    - "All sections animate in on scroll (useInView fade-in transition)"
    - "Layout is fully responsive: feature rows alternate on desktop, stack vertically on mobile"
  artifacts:
    - path: "src/pages/LandingPage.tsx"
      provides: "Complete landing page — all 7 sections"
      exports: ["LandingPage"]
  key_links:
    - from: "src/pages/LandingPage.tsx"
      to: "src/hooks/useInView.ts"
      via: "useInView hook on each animated section"
      pattern: "useInView"
    - from: "src/pages/LandingPage.tsx"
      to: "src/components/ComparisonChart.tsx"
      via: "static SnapshotScoreRow[] prop data"
      pattern: "ComparisonChart"
---

<objective>
Complete all remaining landing page sections: Feature showcase, Testimonials, Pricing, Final CTA, and Footer.

Purpose: Deliver the full public-facing landing page that communicates product value and drives sign-ups. Implements LAND-02, LAND-03, LAND-04.
Output: LandingPage.tsx complete with all sections, all LAND-02/03/04 tests passing.
</objective>

<execution_context>
@C:/Users/KestutisTomkevicius/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/KestutisTomkevicius/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-landing-page/06-CONTEXT.md
@.planning/phases/06-landing-page/06-RESEARCH.md
@.planning/phases/06-landing-page/06-02-SUMMARY.md
</context>

<interfaces>
<!-- Key contracts and patterns for this plan. -->

From src/components/ComparisonChart.tsx:
```typescript
import type { SnapshotScoreRow } from '@/types/database'
export function ComparisonChart({
  snap1Scores,    // SnapshotScoreRow[] — only uses: category_name, score_asis, score_tobe, position
  snap2Scores,
  snap1Label,
  snap2Label,
}: ComparisonChartProps): JSX.Element
```

Static SnapshotScoreRow cast pattern (per RESEARCH.md Pitfall 2):
```typescript
import type { SnapshotScoreRow } from '@/types/database'
const SNAP1_SCORES = [
  { category_name: 'Health', score_asis: 5, score_tobe: 8, position: 0 },
  { category_name: 'Career', score_asis: 4, score_tobe: 7, position: 1 },
  // ... 6 more categories
] as SnapshotScoreRow[]
```

From src/hooks/useInView.ts (created in plan 01):
```typescript
export function useInView(threshold?: number): {
  ref: React.RefObject<HTMLElement>
  inView: boolean
}
```

Scroll animation pattern:
```tsx
const { ref, inView } = useInView()
<section
  ref={ref as React.RefObject<HTMLDivElement>}
  className={`transition-all duration-700 ${inView ? 'opacity-100 translate-y-0' : 'opacity-0 translate-y-6'}`}
>
```

Tailwind purge-safe avatar color pattern (per RESEARCH.md Pitfall 3):
```typescript
// CORRECT: full class names in static data
{ name: 'Sarah M.', avatarColor: 'bg-brand-400' }
// WRONG: dynamic interpolation — bg-${color} gets purged
```

From src/components/ui/button.tsx:
```typescript
// variants: "default" (primary), "outline", "ghost", "link"
// disabled prop renders a disabled button
```

From lucide-react (already installed):
```typescript
import { Check } from 'lucide-react'
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Feature showcase + Testimonials sections</name>
  <files>src/pages/LandingPage.tsx, src/pages/LandingPage.test.tsx</files>
  <behavior>
    - LAND-02: Feature showcase section exists with id="features" containing 3 feature rows
    - LAND-02: Row 1 renders WheelChart with static data (Wheel scoring)
    - LAND-02: Row 2 renders ComparisonChart with static data (Snapshot comparison)
    - LAND-02: Row 3 renders a static checklist card with mixed checked/unchecked items (Action items)
    - LAND-03: Social proof section contains exactly 3 testimonial cards
    - LAND-03: Each card has a blockquote/quote, person name, and role
  </behavior>
  <action>
Replace the `{/* TODO: FeatureShowcase id="features" */}` and `{/* TODO: TestimonialsSection */}` comments in `LandingPage.tsx` with the actual section components. Add them in order between HeroSection and the pricing TODO comment.

**FeatureShowcase** (internal component):
Section element with `id="features"` and scroll animation via useInView.
- Section intro: `<h2>` — e.g., "Everything you need to take stock, and take action." — centered, `text-3xl font-bold text-center mb-4`
- Subtitle: `<p>` — e.g., "Three powerful views. One simple habit." — `text-center text-muted-foreground mb-16`
- Three feature rows in alternating left/right layout on desktop, stacked on mobile.
  - Desktop: `grid grid-cols-1 lg:grid-cols-2 gap-12 items-center`
  - Even-indexed rows (0, 2): text on left, visual on right
  - Odd-indexed row (1): visual on left, text on right (use `lg:order-first` on the visual div)
  - Each row's text: `<h3>` feature title (`text-xl font-semibold mb-3`) + `<p>` description (`text-muted-foreground`)

Feature row content (in order):
1. **Wheel scoring** — Title: "Score your life areas at a glance" — Description: "Drag sliders to rate where you are (as-is) and where you want to be (to-be). Your wheel redraws in real time." — Visual: `<WheelChart data={HERO_WHEEL_DATA} />` (reuse the same static data constant)
2. **Snapshot comparison** — Title: "Compare any two moments in time" — Description: "Save a snapshot after each check-in. Layer any two snapshots to see exactly where you've grown." — Visual: `<ComparisonChart snap1Scores={SNAP1_SCORES} snap2Scores={SNAP2_SCORES} snap1Label="Q1 2024" snap2Label="Q4 2024" />` with static data
3. **Action items** — Title: "Turn insight into action" — Description: "Attach up to 7 action items to each life area. Set deadlines, check them off, and watch your wheel follow." — Visual: a styled static checklist card (HTML/Tailwind mockup — NOT a React component import)

Static checklist card for action items (hardcoded HTML, not a component):
```tsx
<div className="rounded-xl border border-border bg-card p-6 shadow-sm">
  <p className="text-sm font-medium text-muted-foreground mb-4">Health</p>
  <ul className="space-y-3">
    {[
      { label: 'Walk 30 min every morning', done: true },
      { label: 'Cook at home 4x per week',  done: true },
      { label: 'Schedule annual check-up',  done: false },
      { label: 'Cut sugar to 3 days/week',  done: false },
    ].map((item) => (
      <li key={item.label} className="flex items-center gap-3 text-sm">
        <span className={`w-4 h-4 rounded border-2 flex items-center justify-center flex-shrink-0 ${item.done ? 'bg-brand-500 border-brand-500' : 'border-border'}`}>
          {item.done && <Check className="w-3 h-3 text-white" />}
        </span>
        <span className={item.done ? 'line-through text-muted-foreground' : ''}>{item.label}</span>
      </li>
    ))}
  </ul>
</div>
```

Static ComparisonChart data — create module-level constants using `as SnapshotScoreRow[]` cast:
```typescript
const SNAP1_SCORES = [
  { category_name: 'Health',        score_asis: 5, score_tobe: 8, position: 0 },
  { category_name: 'Career',        score_asis: 4, score_tobe: 7, position: 1 },
  { category_name: 'Relationships', score_asis: 6, score_tobe: 8, position: 2 },
  { category_name: 'Finance',       score_asis: 3, score_tobe: 6, position: 3 },
  { category_name: 'Fun',           score_asis: 4, score_tobe: 7, position: 4 },
  { category_name: 'Growth',        score_asis: 5, score_tobe: 9, position: 5 },
  { category_name: 'Environment',   score_asis: 6, score_tobe: 7, position: 6 },
  { category_name: 'Family',        score_asis: 7, score_tobe: 9, position: 7 },
] as SnapshotScoreRow[]

const SNAP2_SCORES = [
  { category_name: 'Health',        score_asis: 7, score_tobe: 9, position: 0 },
  { category_name: 'Career',        score_asis: 6, score_tobe: 8, position: 1 },
  { category_name: 'Relationships', score_asis: 7, score_tobe: 8, position: 2 },
  { category_name: 'Finance',       score_asis: 5, score_tobe: 7, position: 3 },
  { category_name: 'Fun',           score_asis: 6, score_tobe: 8, position: 4 },
  { category_name: 'Growth',        score_asis: 7, score_tobe: 9, position: 5 },
  { category_name: 'Environment',   score_asis: 7, score_tobe: 8, position: 6 },
  { category_name: 'Family',        score_asis: 8, score_tobe: 9, position: 7 },
] as SnapshotScoreRow[]
```

Apply useInView scroll animation to the outer `<section>` of FeatureShowcase.

**TestimonialsSection** (internal component):
Apply useInView on the outer `<section>`.
- Section headline: `<h2>` — "What people are saying" — centered, same styling as FeatureShowcase h2
- Three cards in a row on desktop: `grid grid-cols-1 md:grid-cols-3 gap-6 mt-12`
- Each card: `rounded-xl border border-border bg-card p-6 shadow-sm` with quote + name + role + initials avatar

Testimonial data (static, module-level):
```typescript
const TESTIMONIALS = [
  {
    quote: "I've used the Wheel of Life in client sessions for years. Having a tool that tracks progress over time — and shows clients the delta — is a game-changer.",
    name: 'Rachel K.',
    role: 'Certified Life Coach',
    initials: 'RK',
    avatarColor: 'bg-brand-400',
  },
  {
    quote: "I do a monthly check-in and it takes 10 minutes. Seeing my scores shift over six months made me realise I was neglecting relationships while chasing career goals.",
    name: 'Marcus T.',
    role: 'Product Manager',
    initials: 'MT',
    avatarColor: 'bg-stone-500',
  },
  {
    quote: "As a student it's easy to let everything outside studies slide. This forces me to look at the whole picture once a month. Simple, but it actually works.",
    name: 'Anya S.',
    role: 'University Student',
    initials: 'AS',
    avatarColor: 'bg-blue-400',
  },
]
```

CRITICAL: Use full class names in the data array (e.g. `'bg-brand-400'`), not dynamic interpolation — Tailwind will purge dynamic classes (Pitfall 3 from RESEARCH.md).

TestimonialCard internal render:
```tsx
<div key={t.name} className="rounded-xl border border-border bg-card p-6 shadow-sm">
  <p className="text-muted-foreground italic mb-4">"{t.quote}"</p>
  <div className="flex items-center gap-3">
    <div className={`w-10 h-10 rounded-full flex items-center justify-center text-white font-semibold text-sm ${t.avatarColor}`}>
      {t.initials}
    </div>
    <div>
      <p className="font-medium text-sm">{t.name}</p>
      <p className="text-muted-foreground text-xs">{t.role}</p>
    </div>
  </div>
</div>
```

Update `src/pages/LandingPage.test.tsx`: implement the LAND-02 and LAND-03 stubs:
- "feature showcase section is present with 3 feature rows" — render LandingPage with session=null, look for the h2 "Everything you need to take stock" (or partial match), assert 3 feature row headings visible (assert by feature title text or count)
- "feature showcase renders wheel chart and comparison chart demos" — assert `data-testid="wheel-chart-mock"` and `data-testid="comparison-chart-mock"` are present (WheelChart and ComparisonChart are mocked — add ComparisonChart mock to test file following WheelChart pattern)
- "social proof section shows 3 testimonial cards with quote, name, role" — assert all 3 names (Rachel K., Marcus T., Anya S.) present in document
  </action>
  <verify>
    <automated>npm test -- --run src/pages/LandingPage.test.tsx</automated>
  </verify>
  <done>LAND-02 and LAND-03 tests pass. Feature showcase renders 3 rows with correct demos. Testimonials section shows 3 cards. Remaining LAND-04 stubs still pending. Build clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pricing + Final CTA + Footer sections</name>
  <files>src/pages/LandingPage.tsx, src/pages/LandingPage.test.tsx</files>
  <behavior>
    - LAND-04: Pricing section shows "Free" heading and "$0" price, and "Premium" heading and "$5" price
    - LAND-04: Premium CTA button has text "Coming soon" and is disabled
    - LAND-04: Free CTA links to /auth
    - LAND-04: Both columns show shared features with checkmarks; Premium shows "Unlimited wheels" as differentiator
    - Footer: contains Privacy Policy link to /privacy and Terms of Service link to /terms
  </behavior>
  <action>
Replace `{/* TODO: PricingSection */}`, `{/* TODO: FinalCTASection */}`, and `{/* TODO: LandingFooter */}` comments in `LandingPage.tsx`.

**PricingSection** (internal component):
Apply useInView on the outer `<section>`.
- Section headline: `<h2>` — "Simple, transparent pricing" — centered
- Subtitle: `<p>` — "Start free. Upgrade when you want more." — centered text-muted-foreground
- Two-column grid: `grid grid-cols-1 md:grid-cols-2 gap-6 max-w-3xl mx-auto mt-12`

Feature list for both tiers (shared features checked on both sides):
```typescript
const SHARED_FEATURES = [
  'Unlimited categories per wheel',
  'As-is and to-be scoring',
  'Snapshot history',
  'Snapshot comparison overlay',
  'Trend chart',
  'Action items per category',
]
```

Free tier card:
- Title: "Free" / Price: "$0/mo"
- List: all SHARED_FEATURES with Check icon in `text-brand-500`
- Additional item: "1 wheel" with Check icon
- CTA: `<Button className="w-full mt-8" asChild><a href="/auth">Start free →</a></Button>`

Premium tier card:
- Title: "Premium" / Price: "$5/mo"
- List: all SHARED_FEATURES with Check icon in `text-brand-500`
- Additional item: "Unlimited wheels" with Check icon in `text-brand-500` — this is the differentiator
- Note below list: `<p className="text-xs text-muted-foreground mt-3">More coming soon</p>`
- CTA: `<Button className="w-full mt-8" disabled>Coming soon</Button>` — disabled, no href

Use `<Check className="w-4 h-4 text-brand-500 flex-shrink-0" />` from lucide-react for list items.
Each feature row: `<li className="flex items-center gap-2 text-sm"><Check .../><span>{label}</span></li>`

**FinalCTASection** (internal component):
Apply useInView on the outer `<section>`.
- Warm gradient background (same as hero): `bg-gradient-to-br from-brand-50 via-brand-100 to-surface`
- Content centered: `text-center py-24 px-6`
- Headline: `<h2>` — e.g., "Ready to see your life clearly?" — `text-3xl lg:text-4xl font-bold`
- Subline: `<p>` — e.g., "Five minutes of honest scoring. A lifetime of direction." — `text-muted-foreground mt-4 text-lg`
- CTA: `<Button size="lg" asChild className="mt-8"><a href="/auth">Start your wheel →</a></Button>`

**LandingFooter** (internal component):
- `<footer>` with `border-t border-border bg-background py-8`
- Container: `max-w-6xl mx-auto px-6 flex flex-col sm:flex-row items-center justify-between gap-4 text-sm text-muted-foreground`
- Left: `<p>© {new Date().getFullYear()} JustAWheelOfLife. All rights reserved.</p>`
- Right: `<nav className="flex gap-6">` with `<a href="/privacy" className="hover:text-foreground transition-colors">Privacy Policy</a>` and `<a href="/terms" className="hover:text-foreground transition-colors">Terms of Service</a>`

Update `src/pages/LandingPage.test.tsx`: implement all remaining LAND-04 stubs:
- "pricing section shows Free ($0/mo) and Premium ($5/mo) columns with CTA buttons" — assert "Free" and "Premium" headings present, "$0" and "$5" visible
- "premium CTA button is disabled with text 'Coming soon'" — query button by text "Coming soon", assert `disabled` attribute
- "free CTA links to /auth" — query "Start free →" link, assert href contains "/auth"
  </action>
  <verify>
    <automated>npm test -- --run src/pages/LandingPage.test.tsx && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>All LAND-04 tests pass. No it.todo stubs remaining. Full test suite green for LandingPage.test.tsx. Pricing section renders correctly with disabled Premium CTA. Footer links to /privacy and /terms. Build clean.</done>
</task>

</tasks>

<verification>
After both tasks:
- `npm test -- --run src/pages/LandingPage.test.tsx` — ALL tests pass, zero it.todo stubs remaining
- `npm test -- --run` — full suite green (no regressions in prior phases)
- `npm run build` — clean, no TypeScript errors
- Tailwind dynamic classes avoided — all avatar and icon color classes are full static strings
</verification>

<success_criteria>
- LandingPage.tsx complete: all 7 sections (Nav, Hero, Feature showcase, Testimonials, Pricing, Final CTA, Footer)
- All LAND-01 through LAND-04 tests pass with zero it.todo stubs remaining
- Feature showcase: 3 rows with WheelChart, ComparisonChart, static checklist card
- Testimonials: 3 cards with initials avatars (full Tailwind class names, not dynamic)
- Pricing: Free ($0) + Premium ($5) cards, shared features, disabled "Coming soon" Premium CTA
- Footer: /privacy and /terms links
- useInView applied to all sections except hero (hero is above the fold, no animation needed)
- Full test suite green, build clean
</success_criteria>

<output>
After completion, create `.planning/phases/06-landing-page/06-03-SUMMARY.md`
</output>
