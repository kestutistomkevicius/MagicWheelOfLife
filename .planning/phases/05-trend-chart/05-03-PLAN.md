---
phase: 05-trend-chart
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified: []
autonomous: false
requirements:
  - TREND-01
must_haves:
  truths:
    - "premium@test.com sees a line chart with 4 data points on /trend"
    - "Changing the category dropdown updates the chart"
    - "free@test.com sees an empty state message showing 0 snapshots"
  artifacts: []
  key_links: []
---

<objective>
Human verification checkpoint for Phase 5: confirm the trend chart works end-to-end in the browser against live local Supabase.

Purpose: Automated tests mock hooks and recharts. This checkpoint confirms the real data flow works: live auth, seeded snapshots, and actual Recharts SVG rendering in the browser.

Output: Phase 5 verified complete.
</objective>

<execution_context>
@C:/Users/KestutisTomkevicius/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/KestutisTomkevicius/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    TrendPage with:
    - Single-category line chart (as-is + to-be series) using Recharts LineChart
    - Category dropdown to switch between categories
    - Graceful empty state for fewer than 3 snapshots
    - Data loaded from live Supabase via useSnapshots hook
  </what-built>
  <how-to-verify>
    Prerequisites: `supabase start` and `npm run dev` must be running.

    **Scenario 1 — Chart renders for premium user (4 seeded snapshots):**
    1. Open http://localhost:5173
    2. Sign in as premium@test.com (password: password123 or whatever is in seed.sql)
    3. Navigate to the Trend page via sidebar
    4. Verify: A line chart is visible with two colored lines (amber for As-Is, blue for To-Be)
    5. Verify: The X-axis shows 4 date labels (one per snapshot)
    6. Verify: A "Category:" label and a dropdown appear above the chart
    7. Change the category in the dropdown
    8. Verify: The chart updates to show that category's scores
    9. Verify: The chart lines show a visible difference in scores across snapshots (the seed data tells a story — e.g. Health improved, Career declined)

    **Scenario 2 — Empty state for free user (no snapshots):**
    1. Sign out and sign in as free@test.com
    2. Navigate to the Trend page
    3. Verify: No chart is rendered
    4. Verify: A message appears containing "at least 3 snapshots" and "0 so far" (or "You have 0")
    5. Verify: No JavaScript errors in the browser console
  </how-to-verify>
  <resume-signal>Type "approved" if both scenarios work correctly, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Manual browser verification as described above.
</verification>

<success_criteria>
- premium@test.com sees a 4-point line chart with category switching working
- free@test.com sees a friendly empty state with snapshot count
- No console errors in either scenario
- TREND-01 fully satisfied
</success_criteria>

<output>
After approval, create `.planning/phases/05-trend-chart/05-03-SUMMARY.md`
</output>
