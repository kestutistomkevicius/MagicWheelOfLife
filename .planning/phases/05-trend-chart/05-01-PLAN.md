---
phase: 05-trend-chart
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/TrendChart.tsx
  - src/components/TrendChart.test.tsx
  - src/pages/TrendPage.test.tsx
autonomous: true
requirements:
  - TREND-01
must_haves:
  truths:
    - "TrendChart renders a LineChart with two Line series (as-is, to-be) given 3+ data points"
    - "as-is Line uses amber color (#e8a23a), to-be Line uses blue (#60a5fa)"
    - "TrendPage test stubs exist and report as pending (not failing)"
  artifacts:
    - path: "src/components/TrendChart.tsx"
      provides: "Recharts LineChart wrapper accepting TrendChartPoint[] data"
      exports: ["TrendChart", "TrendChartPoint"]
    - path: "src/components/TrendChart.test.tsx"
      provides: "Unit tests for TrendChart component"
      contains: "vi.mock('recharts'"
    - path: "src/pages/TrendPage.test.tsx"
      provides: "Wave 0 it.todo stubs for TrendPage tests"
      contains: "it.todo"
  key_links:
    - from: "src/components/TrendChart.tsx"
      to: "recharts"
      via: "LineChart, Line, XAxis, YAxis, CartesianGrid, Tooltip, Legend, ResponsiveContainer imports"
      pattern: "from 'recharts'"
---

<objective>
Create the TrendChart component and establish Wave 0 test stubs for TrendPage.

Purpose: TrendChart is the purely presentational Recharts LineChart wrapper — it accepts pre-computed TrendChartPoint[] data and renders two Line series (as-is/to-be). Creating it before TrendPage lets Plan 02 import and use it without circular dependency. The TrendPage stubs unblock the verify commands in Plan 02 from the moment execution starts.

Output: src/components/TrendChart.tsx (full implementation), src/components/TrendChart.test.tsx (full tests), src/pages/TrendPage.test.tsx (Wave 0 it.todo stubs only).
</objective>

<execution_context>
@C:/Users/KestutisTomkevicius/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/KestutisTomkevicius/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-trend-chart/05-RESEARCH.md

@src/components/ComparisonChart.test.tsx
@src/components/WheelChart.tsx
</context>

<interfaces>
<!-- Types and contracts the executor needs. No codebase exploration required. -->

From src/types/database.ts:
```typescript
export type SnapshotRow = {
  id: string
  wheel_id: string
  user_id: string
  name: string
  saved_at: string  // ISO timestamptz
}

export type SnapshotScoreRow = {
  id: string
  snapshot_id: string
  user_id: string
  category_name: string
  position: number
  score_asis: number
  score_tobe: number
}
```

Recharts mock pattern (from ComparisonChart.test.tsx — use this exact pattern, adapted for LineChart):
```typescript
vi.mock('recharts', () => ({
  ResponsiveContainer: ({ children }: { children: React.ReactNode }) => (
    <div data-testid="responsive-container">{children}</div>
  ),
  LineChart: ({ children, data }: { children: React.ReactNode; data: unknown[] }) => (
    <div data-testid="line-chart" data-points={data?.length}>{children}</div>
  ),
  Line: ({ dataKey, stroke }: { dataKey: string; stroke: string }) => (
    <div data-testid="line" data-key={dataKey} data-stroke={stroke} />
  ),
  XAxis: () => <div data-testid="x-axis" />,
  YAxis: () => <div data-testid="y-axis" />,
  CartesianGrid: () => <div data-testid="cartesian-grid" />,
  Tooltip: () => <div data-testid="tooltip" />,
  Legend: () => <div data-testid="legend" />,
}))
```

Color convention (from WheelChart.tsx — do not deviate):
- as-is: stroke="#e8a23a" (amber)
- to-be: stroke="#60a5fa" (blue)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TrendChart component + tests</name>
  <files>src/components/TrendChart.tsx, src/components/TrendChart.test.tsx</files>
  <behavior>
    - TrendChart renders a `data-testid="line-chart"` element when given a non-empty data array
    - TrendChart renders a `data-testid="line"` with `data-key="asis"` and `data-stroke="#e8a23a"` (amber)
    - TrendChart renders a `data-testid="line"` with `data-key="tobe"` and `data-stroke="#60a5fa"` (blue)
    - `data-points` attribute on the line-chart mock equals the length of the data prop
    - `data-testid="responsive-container"` is present as the root wrapper
  </behavior>
  <action>
    Write TrendChart.test.tsx first (RED), then TrendChart.tsx (GREEN).

    TrendChart.test.tsx:
    - vi.mock('recharts', ...) using the LineChart mock pattern from the interfaces block above — expose `data-stroke` on the Line mock so color tests can query it
    - `import { describe, it, expect } from 'vitest'`
    - `import { render, screen } from '@testing-library/react'`
    - `import { TrendChart } from './TrendChart'`
    - Helper: `const makePoint = (date: string, asis: number, tobe: number) => ({ date, asis, tobe })`
    - Test 1: "renders line chart with correct data point count" — pass 3 points, expect `data-points="3"`
    - Test 2: "renders as-is line with amber color" — expect Line with data-key="asis" and data-stroke="#e8a23a"
    - Test 3: "renders to-be line with blue color" — expect Line with data-key="tobe" and data-stroke="#60a5fa"
    - Test 4: "wraps in responsive container" — expect data-testid="responsive-container"

    TrendChart.tsx:
    ```typescript
    import {
      LineChart, Line, XAxis, YAxis, CartesianGrid,
      Tooltip, Legend, ResponsiveContainer,
    } from 'recharts'

    export type TrendChartPoint = {
      date: string   // formatted for display e.g. "15 Jan 2026"
      asis: number
      tobe: number
    }

    interface TrendChartProps {
      data: TrendChartPoint[]
      categoryName: string
    }

    export function TrendChart({ data, categoryName: _categoryName }: TrendChartProps) {
      return (
        <ResponsiveContainer width="100%" height={360}>
          <LineChart data={data} margin={{ top: 8, right: 16, left: 0, bottom: 8 }}>
            <CartesianGrid strokeDasharray="3 3" stroke="#e7e5e4" />
            <XAxis
              dataKey="date"
              tick={{ fontSize: 12, fill: '#78716c' }}
              tickLine={false}
            />
            <YAxis
              domain={[0, 10]}
              ticks={[0, 2, 4, 6, 8, 10]}
              tick={{ fontSize: 12, fill: '#78716c' }}
              tickLine={false}
              axisLine={false}
              width={24}
            />
            <Tooltip
              contentStyle={{ fontSize: 13 }}
              formatter={(value: number, name: string) =>
                [value, name === 'asis' ? 'As-Is' : 'To-Be']
              }
            />
            <Legend
              formatter={(value: string) => (value === 'asis' ? 'As-Is' : 'To-Be')}
            />
            <Line
              type="monotone"
              dataKey="asis"
              name="asis"
              stroke="#e8a23a"
              strokeWidth={2}
              dot={{ r: 4, fill: '#e8a23a' }}
              activeDot={{ r: 5 }}
            />
            <Line
              type="monotone"
              dataKey="tobe"
              name="tobe"
              stroke="#60a5fa"
              strokeWidth={2}
              dot={{ r: 4, fill: '#60a5fa' }}
              activeDot={{ r: 5 }}
            />
          </LineChart>
        </ResponsiveContainer>
      )
    }
    ```

    Run tests after each file:
    - After TrendChart.test.tsx: `npm test -- --run src/components/TrendChart.test.tsx` (should FAIL — no implementation yet)
    - After TrendChart.tsx: `npm test -- --run src/components/TrendChart.test.tsx` (must PASS)
  </action>
  <verify>
    <automated>npm test -- --run src/components/TrendChart.test.tsx</automated>
  </verify>
  <done>All 4 TrendChart tests pass. TrendChart exports TrendChartPoint type and TrendChart function. Two Line series render with correct amber/blue strokes.</done>
</task>

<task type="auto">
  <name>Task 2: Wave 0 TrendPage test stubs</name>
  <files>src/pages/TrendPage.test.tsx</files>
  <action>
    Create src/pages/TrendPage.test.tsx with it.todo stubs only. Follow the Wave 0 stub pattern established in Phases 2-4: import only `describe` and `it` from vitest, no feature module imports. This lets the verify command in Plan 02 run immediately without failing.

    ```typescript
    import { describe, it } from 'vitest'

    describe('TrendPage', () => {
      it.todo('shows empty state message when fewer than 3 snapshots exist')
      it.todo('shows snapshot count in empty state message')
      it.todo('renders TrendChart when 3 or more snapshots exist')
      it.todo('shows category select when snapshots exist')
      it.todo('category select lists all unique category names from snapshot scores')
      it.todo('chart data is in chronological order (oldest snapshot leftmost)')
      it.todo('omits chart points for categories with no score in that snapshot (does not plot as 0)')
    })
    ```

    Run: `npm test -- --run src/pages/TrendPage.test.tsx` — all tests must report as todo (pending), none failing.
  </action>
  <verify>
    <automated>npm test -- --run src/pages/TrendPage.test.tsx</automated>
  </verify>
  <done>TrendPage.test.tsx exists with 7 it.todo stubs. Test run reports 7 pending, 0 failing, 0 passing.</done>
</task>

</tasks>

<verification>
After both tasks complete, run the full test suite to confirm no regressions:

```
npm test -- --run
```

Expected: All existing tests pass. New TrendChart tests pass (4 passing). TrendPage stubs show as pending (7 todo).
</verification>

<success_criteria>
- src/components/TrendChart.tsx exists and exports TrendChart function and TrendChartPoint type
- src/components/TrendChart.test.tsx exists with 4 passing tests
- src/pages/TrendPage.test.tsx exists with 7 it.todo stubs (pending, not failing)
- Full test suite green with no regressions
- TrendChart uses amber #e8a23a for as-is, blue #60a5fa for to-be (matching WheelChart color convention)
</success_criteria>

<output>
After completion, create `.planning/phases/05-trend-chart/05-01-SUMMARY.md`
</output>
