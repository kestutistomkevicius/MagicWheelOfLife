---
phase: 04-snapshots-and-comparison
plan: 05
type: execute
wave: 5
depends_on:
  - 04-04
files_modified: []
autonomous: false
requirements:
  - SNAP-01
  - SNAP-02
  - COMP-01
  - COMP-02

must_haves:
  truths:
    - "User can save a named snapshot from the Snapshots page and see it appear in the list"
    - "User can select two snapshots and see an overlay radar chart with two distinct color pairs"
    - "User can select a category and see a score history table with 4 rows for the premium seed user"
    - "WheelPage rename/remove warning dialog appears for the premium user's wheel (which has snapshots)"
  artifacts:
    - path: "src/pages/SnapshotsPage.tsx"
      provides: "Full page verified working in browser"
    - path: "src/pages/WheelPage.tsx"
      provides: "hasSnapshots warning verified working in browser"
  key_links:
    - from: "SnapshotsPage"
      to: "Supabase snapshots table"
      via: "listSnapshots + saveSnapshot + fetchSnapshotScores"
      pattern: "browser network tab shows /rest/v1/snapshots"
---

<objective>
Human verification checkpoint: confirm all Phase 4 flows work end-to-end in the browser against live local Supabase.

Purpose: Visual and functional verification that automated tests cannot fully cover — overlay chart colors, date formatting, and real DB round-trips all need human eyes.
Output: Human approval or issue report to fix.
</objective>

<execution_context>
@C:/Users/KestutisTomkevicius/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/KestutisTomkevicius/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-snapshots-and-comparison/04-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 4 implementation:
- DB: snapshots + snapshot_scores tables with RLS + 4 seeded snapshots for premium user
- useSnapshots hook: saveSnapshot, listSnapshots, fetchSnapshotScores, checkSnapshotsExist
- SnapshotNameDialog component: name input + date preview
- ComparisonChart component: 4-series amber/blue radar overlay
- SnapshotsPage: full UI with list, comparison picker, score history table
- WheelPage: hasSnapshots wired to real Supabase check (pessimistic default = true)
  </what-built>
  <how-to-verify>
**Pre-flight:** Ensure local Supabase is running (`supabase start`) and Vite dev server is running (`npm run dev`).

**Test 1 — Snapshots page with seeded data (premium user):**
1. Visit http://localhost:5173 and sign in as premium@test.com / testpass123 (or whichever credentials your seed.sql uses)
2. Click "Snapshots" in the sidebar
3. Verify: 4 snapshot rows appear in chronological order, each showing a name and a date in "DD Mon YYYY" format
   - Expected: "Q1 Annual Review — [date ~12 months ago]", "Mid-Year Check-In", "Q3 Progress", "Year End Review"
4. Verify: the save date format is correct (e.g., "15 Mar 2025" style, NOT ISO format)

**Test 2 — Snapshot comparison overlay (COMP-01):**
5. Check the checkbox next to "Q1 Annual Review"
6. Check the checkbox next to "Year End Review"
7. Verify: A comparison radar chart appears below the snapshot list
8. Verify: The chart has TWO distinct color schemes visible (amber tones for one snapshot, blue tones for the other)
9. Verify: Both snapshot labels appear in the chart legend
10. Verify: Third checkbox is disabled (max 2 selection enforced)
11. Uncheck one snapshot — verify chart disappears

**Test 3 — Score history table (COMP-02):**
12. Locate the score history section below the comparison chart (or below the list if no comparison active)
13. Verify: A category dropdown/select exists, populated with category names (Health, Career, etc.)
14. Select "Career" from the dropdown
15. Verify: Table shows 4 rows (one per snapshot), each with a snapshot name/date, As-Is score, and To-Be score
16. Verify: Career As-Is scores show the improvement story: 5, 6, 7, 8 (ascending over time, oldest first)

**Test 4 — Save a new snapshot:**
17. Click the "Save snapshot" button
18. Verify: A dialog appears with a name input field
19. Verify: The dialog shows a preview like "Will be saved as: My snapshot — 15 Mar 2026" (today's date)
20. Type "Test Manual Save" in the input
21. Verify: Preview updates to "Test Manual Save — [today's date]"
22. Click "Save snapshot"
23. Verify: Dialog closes; the new snapshot appears at the TOP of the list (most recent)
24. Verify: The new snapshot label shows "Test Manual Save — [today's date formatted correctly]"

**Test 5 — WheelPage rename warning (WHEEL-04 + hasSnapshots activation):**
25. Click "Wheel" in the sidebar
26. Attempt to rename a category (click rename icon on any CategorySlider row)
27. Verify: A warning dialog appears mentioning that snapshots exist and the rename will not update historical data
28. Cancel the rename

**Test 6 — Free user (no snapshots):**
29. Sign out; sign in as free@test.com / testpass123
30. Click "Snapshots" in the sidebar
31. Verify: Empty state message appears (something like "No snapshots yet")
32. Click "Wheel" → attempt to rename a category
33. Verify: NO warning dialog appears (hasSnapshots = false for free user)
  </how-to-verify>
  <resume-signal>
Type "approved" if all 6 tests (Test 1–6) pass, or describe which specific step failed and what you saw instead.
  </resume-signal>
</task>

</tasks>

<verification>
Human approval received for all Phase 4 flows.
</verification>

<success_criteria>
- All 6 verification tests pass in the browser
- Snapshot list shows correct names and formatted dates
- Comparison chart renders with two distinct color pairs
- Score history table shows correct historical data
- Save snapshot creates a new entry visible immediately
- WheelPage rename warning activates correctly for users with snapshots
- Free user sees no warning when renaming (no snapshots = no warning)
</success_criteria>

<output>
After completion, create `.planning/phases/04-snapshots-and-comparison/04-05-SUMMARY.md`
</output>
