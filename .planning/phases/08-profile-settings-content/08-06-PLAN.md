---
phase: 08-profile-settings-content
plan: 06
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/pages/TermsPage.tsx
  - src/pages/TermsPage.test.tsx
  - src/pages/PrivacyPage.tsx
  - src/pages/PrivacyPage.test.tsx
autonomous: true
requirements:
  - CONTENT-01
  - CONTENT-02

must_haves:
  truths:
    - "TermsPage renders full SaaS Terms of Service text (not 'Coming soon')"
    - "PrivacyPage renders full Privacy Policy text covering data collection, Supabase hosting, user rights"
    - "Both pages have readable typography, section headings, last updated date"
    - "Both pages link back to home ('Back to home')"
    - "Smoke tests confirm 'Coming soon' is absent and at least one h2 heading is present"
  artifacts:
    - path: "src/pages/TermsPage.tsx"
      provides: "Full Terms of Service content"
      contains: "Terms of Service"
    - path: "src/pages/PrivacyPage.tsx"
      provides: "Full Privacy Policy content"
      contains: "Privacy Policy"
    - path: "src/pages/TermsPage.test.tsx"
      provides: "Smoke test: no Coming soon, h2 headings present"
      contains: "Coming soon"
    - path: "src/pages/PrivacyPage.test.tsx"
      provides: "Smoke test: no Coming soon, h2 headings present"
      contains: "Coming soon"
  key_links:
    - from: "src/pages/TermsPage.tsx"
      to: "App.tsx /terms route"
      via: "already routed — just replace stub content"
      pattern: "TermsPage"
    - from: "src/pages/PrivacyPage.tsx"
      to: "App.tsx /privacy route"
      via: "already routed — just replace stub content"
      pattern: "PrivacyPage"
---

<objective>
Replace "coming soon" stubs with full legal content on Terms and Privacy pages.

Purpose: CONTENT-01 + CONTENT-02 — the app is legally ready for users before launch prep in Phase 10.
Output: Full Terms of Service and Privacy Policy pages with SaaS-appropriate legal text, plus smoke tests.
</objective>

<execution_context>
@C:/Users/KestutisTomkevicius/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/KestutisTomkevicius/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-profile-settings-content/08-RESEARCH.md

<interfaces>
<!-- Current stub structure to replace -->

Current TermsPage.tsx (stub):
```tsx
export function TermsPage() {
  return (
    <div className="min-h-screen flex items-center justify-center p-8">
      <div className="text-center max-w-md">
        <h1 className="text-2xl font-semibold mb-4">Terms of Service</h1>
        <p className="text-muted-foreground">Coming soon — check back before launch.</p>
        <a href="/" ...>← Back to home</a>
      </div>
    </div>
  )
}
```

App data model known (from CLAUDE.md + data-model.md):
- Service: JustAWheelOfLife — wheel of life self-assessment tool
- Data collected: email/password via Supabase Auth, wheel scores, action items, snapshots
- Hosting: Supabase Cloud (PostgreSQL, Auth) + Vercel (frontend)
- No payment processing in Phase 8 (Stripe is Phase 9)
- No third-party analytics trackers beyond what Vercel provides

From RESEARCH.md:
- Use inline text (not external legal service)
- Pitfall 6 note: Tailwind styles work fine — these pages are under App.tsx which loads global styles
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full Terms of Service page</name>
  <files>src/pages/TermsPage.tsx, src/pages/TermsPage.test.tsx</files>
  <action>
Replace the stub with a full Terms of Service page. Use a scrollable layout (max-w-3xl mx-auto p-8) with proper prose typography (Tailwind prose classes if available, otherwise manual heading/paragraph classes).

Last updated date: 2026-03-18

Include these sections:
1. **Acceptance of Terms** — By using JustAWheelOfLife you agree to these terms.
2. **Description of Service** — Personal self-assessment tool for wheel of life coaching. Free and premium tiers.
3. **User Accounts** — Email/password registration via Supabase Auth. User responsible for account security. Must be 16+.
4. **User Content** — Wheel data, action items, scores belong to you. We store them to provide the service. We do not sell your data.
5. **Acceptable Use** — No automated scraping, no impersonation, no illegal activities.
6. **Premium Tier** — Premium features (additional wheels, important categories) available via subscription. Subscription terms to be published when payment is enabled.
7. **Service Availability** — Provided "as is." No uptime guarantees. We may change or discontinue features.
8. **Limitation of Liability** — Not liable for indirect or consequential damages. Maximum liability is the amount paid in the 12 months prior to the claim.
9. **Governing Law** — Laws of Lithuania (founder's jurisdiction). Disputes resolved via Lithuanian courts.
10. **Changes to Terms** — We may update these terms. Continued use after changes constitutes acceptance.
11. **Contact** — contact@justawheeloflife.com (placeholder)

Page layout:
- h1: "Terms of Service"
- Subtitle: "Last updated: 18 March 2026"
- Each section: `<h2>` heading + `<p>` content
- Bottom: "← Back to home" link using `<a href="/">` (plain anchor, same as existing stub)

Create `src/pages/TermsPage.test.tsx` with two smoke tests:
```typescript
import { render, screen } from '@testing-library/react'
import { TermsPage } from './TermsPage'

describe('TermsPage', () => {
  it('does not render "Coming soon" placeholder text', () => {
    render(<TermsPage />)
    expect(screen.queryByText(/coming soon/i)).toBeNull()
  })
  it('renders at least one h2 section heading', () => {
    render(<TermsPage />)
    const headings = screen.getAllByRole('heading', { level: 2 })
    expect(headings.length).toBeGreaterThan(0)
  })
})
```
  </action>
  <verify>
    <automated>npm test -- --run TermsPage</automated>
  </verify>
  <done>TermsPage smoke tests green; page no longer contains "Coming soon"; full legal content renders; build passes</done>
</task>

<task type="auto">
  <name>Task 2: Full Privacy Policy page</name>
  <files>src/pages/PrivacyPage.tsx, src/pages/PrivacyPage.test.tsx</files>
  <action>
Replace the stub with a full Privacy Policy page. Same layout as TermsPage (max-w-3xl mx-auto p-8, prose typography).

Last updated date: 2026-03-18

Include these sections:
1. **Introduction** — JustAWheelOfLife ("we", "us") operates at justawheeloflife.com. This policy explains what data we collect and how we use it.
2. **Data We Collect**
   - Account data: email address, encrypted password hash (via Supabase Auth — we never store plaintext passwords)
   - Wheel data: wheel names, category names, scores (1–10), action item text and deadlines, snapshot names and dates
   - Usage data: Vercel may collect anonymized access logs (IP address, pages visited, timestamps)
3. **How We Use Your Data** — To provide the wheel of life service; to enable you to track your progress over time; to support account recovery; we do not sell, rent, or share your data with third parties for marketing.
4. **Data Storage** — Your data is stored in Supabase Cloud (hosted on AWS infrastructure in the EU region). Avatar images are stored in Supabase Storage (same infrastructure).
5. **Data Retention** — We retain your data while your account is active. You may request deletion by emailing us.
6. **Your Rights (GDPR)** — You have the right to access, correct, export, or delete your personal data. Contact us at contact@justawheeloflife.com.
7. **Cookies** — We use functional cookies only (Supabase Auth session token). No advertising or tracking cookies.
8. **Children** — The service is not intended for users under 16. We do not knowingly collect data from minors.
9. **Changes to This Policy** — We will notify users by updating the "Last updated" date above. Continued use constitutes acceptance.
10. **Contact** — contact@justawheeloflife.com (placeholder)

Same page structure: h1 "Privacy Policy", subtitle "Last updated: 18 March 2026", h2+p sections, "← Back to home" link.

Create `src/pages/PrivacyPage.test.tsx` with two smoke tests:
```typescript
import { render, screen } from '@testing-library/react'
import { PrivacyPage } from './PrivacyPage'

describe('PrivacyPage', () => {
  it('does not render "Coming soon" placeholder text', () => {
    render(<PrivacyPage />)
    expect(screen.queryByText(/coming soon/i)).toBeNull()
  })
  it('renders at least one h2 section heading', () => {
    render(<PrivacyPage />)
    const headings = screen.getAllByRole('heading', { level: 2 })
    expect(headings.length).toBeGreaterThan(0)
  })
})
```
  </action>
  <verify>
    <automated>npm test -- --run PrivacyPage</automated>
  </verify>
  <done>PrivacyPage smoke tests green; page no longer contains "Coming soon"; full GDPR-aware privacy policy renders; build passes</done>
</task>

</tasks>

<verification>
- `npm test -- --run TermsPage` — smoke tests green
- `npm test -- --run PrivacyPage` — smoke tests green
- `npm run build` — no TypeScript errors
- Navigate to /terms and /privacy in browser — full legal text visible, not "Coming soon"
- Both pages have "Last updated: 18 March 2026" subtitle
</verification>

<success_criteria>
- Terms of Service: 10 substantive sections, no placeholder content, smoke tests green
- Privacy Policy: covers data collection, Supabase storage, GDPR rights, no placeholder content, smoke tests green
- Both pages compile cleanly and are visually readable
</success_criteria>

<output>
After completion, create `.planning/phases/08-profile-settings-content/08-06-SUMMARY.md`
</output>
