---
phase: 08-profile-settings-content
plan: 07
type: execute
wave: 2
depends_on:
  - 08-01
  - 08-02
files_modified:
  - src/pages/SnapshotsPage.tsx
  - src/pages/TrendPage.tsx
  - src/pages/TrendPage.test.tsx
autonomous: true
requirements:
  - CONTENT-04
  - CONTENT-05

must_haves:
  truths:
    - "SnapshotsPage shows an onboarding callout explaining snapshots when snapshots.length === 0"
    - "Onboarding callout is NOT shown when at least one snapshot exists"
    - "TrendPage wheel selector renders when wheels.length > 1 (already implemented — test coverage added)"
    - "Selecting a different wheel on TrendPage calls selectWheel with the new wheel id"
    - "TrendPage tests for wheel selector are green"
  artifacts:
    - path: "src/pages/SnapshotsPage.tsx"
      provides: "Onboarding callout in empty state section"
      contains: "What is a snapshot"
    - path: "src/pages/TrendPage.tsx"
      provides: "Multi-wheel selector (already present — no code change needed)"
      contains: "wheels.length > 1"
    - path: "src/pages/TrendPage.test.tsx"
      provides: "Tests for wheel selector behavior"
      contains: "wheel selector"
  key_links:
    - from: "src/pages/SnapshotsPage.tsx"
      to: "snapshots state"
      via: "snapshots.length === 0 conditional renders onboarding callout"
      pattern: "snapshots.length === 0"
    - from: "src/pages/TrendPage.tsx"
      to: "useWheel.selectWheel"
      via: "onChange handler on wheel selector calls selectWheel(e.target.value)"
      pattern: "selectWheel"
---

<objective>
Snapshot onboarding callout for new users + TrendPage wheel selector test coverage.

Purpose: CONTENT-04 — new users understand what a snapshot is before their first save; CONTENT-05 — premium multi-wheel selector is tested and seed data enables it.
Output: Onboarding callout in SnapshotsPage empty state; TrendPage wheel selector tests.
</objective>

<execution_context>
@C:/Users/KestutisTomkevicius/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/KestutisTomkevicius/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-profile-settings-content/08-RESEARCH.md
@.planning/phases/08-profile-settings-content/08-01-SUMMARY.md

<interfaces>
<!-- Extracted from codebase for executor reference -->

From src/pages/SnapshotsPage.tsx (current empty state block at lines 183–187):
```tsx
{snapshots.length === 0 ? (
  <div className="rounded-lg border border-stone-200 p-8 text-center text-stone-500">
    <p>No snapshots yet. Save your first snapshot to get started.</p>
  </div>
) : (
  /* ... snapshot list ... */
)}
```

The onboarding callout should be added ABOVE the empty state div (or replace it — keep the "No snapshots yet" message and add the educational block above it).

From RESEARCH.md (Pattern 5 — recommended option 1):
```tsx
{snapshots.length === 0 && (
  <div className="rounded-lg border border-brand-200 bg-brand-50 p-4 text-sm text-brand-800">
    <p className="font-medium">What is a snapshot?</p>
    <p className="mt-1 text-stone-600">
      A snapshot is a saved copy of your current wheel scores. Take one now to
      capture where you stand today — you'll be able to compare it with future
      snapshots to see how you've grown.
    </p>
  </div>
)}
```

From src/pages/TrendPage.tsx (lines 120–131 — already implemented):
```tsx
{wheels.length > 1 && (
  <select
    value={wheel?.id ?? ''}
    onChange={e => void selectWheel(e.target.value)}
    className="text-sm border border-stone-300 rounded px-2 py-1 ..."
  >
    {wheels.map(w => (
      <option key={w.id} value={w.id}>{w.name}</option>
    ))}
  </select>
)}
```
TrendPage code does NOT need to change. Only tests need to be added.

From src/pages/TrendPage.test.tsx (existing file — extend, do not replace).
The file already has tests. Add new tests for wheel selector behavior.
Mock useWheel to return { wheels: [wheel1, wheel2], wheel: wheel1, selectWheel: vi.fn(), ... }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Snapshot onboarding callout in SnapshotsPage</name>
  <files>src/pages/SnapshotsPage.tsx</files>
  <action>
In `src/pages/SnapshotsPage.tsx`, add an onboarding callout to the empty state section.

Find the block starting at `{snapshots.length === 0 ? (` (around line 183). Modify the empty state branch to include the educational callout ABOVE the "No snapshots yet" prompt:

```tsx
{snapshots.length === 0 ? (
  <div className="space-y-4">
    <div className="rounded-lg border border-brand-200 bg-brand-50 p-4 text-sm">
      <p className="font-medium text-brand-800">What is a snapshot?</p>
      <p className="mt-1 text-stone-600">
        A snapshot is a saved copy of your current wheel scores. Take one now to
        capture where you stand today — you'll be able to compare it with future
        snapshots to see how you've grown over time.
      </p>
    </div>
    <div className="rounded-lg border border-stone-200 p-8 text-center text-stone-500">
      <p>No snapshots yet. Save your first snapshot to get started.</p>
    </div>
  </div>
) : (
  /* ... existing snapshot list ... */
)}
```

Check if brand-200 and brand-50 Tailwind tokens exist in the project (check tailwind.config.ts or existing usages). If brand-* colors are not defined as custom tokens, use stone-200/stone-50 instead — consistent with the landing page color approach.

The existing SnapshotsPage tests (src/pages/SnapshotsPage.test.tsx or src/pages/__tests__/SnapshotsPage.test.tsx) — add two tests:
1. "shows onboarding callout when no snapshots exist" — mock useWheel + useSnapshots returning empty snapshots; find text "What is a snapshot?"
2. "does not show onboarding callout when snapshots exist" — mock with 1+ snapshots; verify "What is a snapshot?" not in document
  </action>
  <verify>
    <automated>npm test -- --run SnapshotsPage</automated>
  </verify>
  <done>SnapshotsPage tests green; onboarding callout appears in empty state, absent when snapshots exist</done>
</task>

<task type="auto">
  <name>Task 2: TrendPage wheel selector tests (CONTENT-05)</name>
  <files>src/pages/TrendPage.test.tsx</files>
  <action>
The TrendPage wheel selector UI is ALREADY implemented (lines 120–131 of TrendPage.tsx). No code change to TrendPage.tsx is needed. This task adds test coverage only.

Open `src/pages/TrendPage.test.tsx` (existing file). Add the following tests:

1. "does not render wheel selector when only one wheel exists"
   - Mock useWheel with `{ wheels: [wheel1], wheel: wheel1, ... }`
   - Assert: `queryByRole('combobox', { name: /wheel/i })` returns null (or the wheel selector select element is not present near the h1)
   - Alternative: check that there's only one `<select>` in the component (the category selector), not two

2. "renders wheel selector when multiple wheels exist"
   - Mock useWheel with `{ wheels: [wheel1, wheel2], wheel: wheel1, selectWheel: mockSelectWheel, ... }`
   - Assert: a `<select>` containing option text for wheel1.name and wheel2.name is present near the h1 "Trend"

3. "selecting a different wheel calls selectWheel"
   - Same mock as above
   - Fire change event on the wheel selector select with value = wheel2.id
   - Assert: mockSelectWheel called with wheel2.id

Use the existing mock patterns in TrendPage.test.tsx as a guide. The file already mocks useWheel — extend those mocks.

Note on seed data: The second wheel with 3 snapshots was added in Plan 01 (seed.sql). The CONTENT-05 selector already works in the browser — these tests verify the conditional rendering logic.
  </action>
  <verify>
    <automated>npm test -- --run TrendPage</automated>
  </verify>
  <done>All TrendPage tests green including the 3 new wheel selector tests</done>
</task>

</tasks>

<verification>
- `npm test -- --run SnapshotsPage` — all tests green (including 2 new onboarding tests)
- `npm test -- --run TrendPage` — all tests green (including 3 new wheel selector tests)
- `npm test -- --run` — full suite green
</verification>

<success_criteria>
- SnapshotsPage: onboarding callout visible for 0-snapshot state; hidden for 1+ snapshots
- TrendPage: wheel selector behavior is tested and green; selector already renders when wheels.length > 1
- All tests green
</success_criteria>

<output>
After completion, create `.planning/phases/08-profile-settings-content/08-07-SUMMARY.md`
</output>
