---
phase: 08-profile-settings-content
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useProfile.test.ts
  - src/pages/SettingsPage.test.tsx
  - src/components/FeatureRequestModal.test.tsx
autonomous: true
requirements:
  - PROFILE-01
  - PROFILE-02
  - CONTENT-03

must_haves:
  truths:
    - "Wave 0 test stubs exist for useProfile, SettingsPage, and FeatureRequestModal"
    - "npm test passes (all stubs are it.todo — no implementation yet)"
  artifacts:
    - path: "src/hooks/useProfile.test.ts"
      provides: "it.todo stubs for PROFILE-01 avatar upload behavior"
      contains: "it.todo"
    - path: "src/pages/SettingsPage.test.tsx"
      provides: "it.todo stubs for PROFILE-02 settings UI behavior"
      contains: "it.todo"
    - path: "src/components/FeatureRequestModal.test.tsx"
      provides: "it.todo stubs for CONTENT-03 feature request form behavior"
      contains: "it.todo"
  key_links:
    - from: "src/hooks/useProfile.test.ts"
      to: "src/hooks/useProfile.ts"
      via: "test stubs become green when Plan 03 implements the hook"
      pattern: "useProfile"
---

<objective>
Wave 0 test scaffolds for PROFILE-01, PROFILE-02, and CONTENT-03.

Purpose: Establish the Nyquist test stubs before implementation so verification commands in Plans 03–05 run immediately without failing test infrastructure.
Output: Three new test files with `it.todo()` stubs only — no module imports from unbuilt files.
</objective>

<execution_context>
@C:/Users/KestutisTomkevicius/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/KestutisTomkevicius/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-profile-settings-content/08-RESEARCH.md

<interfaces>
<!-- Wave 0 pattern established in earlier phases (from STATE.md decisions) -->

Wave 0 stub pattern: import ONLY { describe, it } from 'vitest'. Use ONLY it.todo().
Do NOT import any feature module (useProfile, SettingsPage, FeatureRequestModal) — those files do not exist yet.
Stubs survive until implementation plans run.

Example from Phase 3 (src/hooks/useActionItems.test.ts Wave 0):
```typescript
import { describe, it } from 'vitest'

describe('useActionItems', () => {
  it.todo('loads action items for a category')
  it.todo('adds an action item (limit 7)')
  it.todo('toggles completion state')
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 stubs — useProfile and SettingsPage tests</name>
  <files>src/hooks/useProfile.test.ts, src/pages/SettingsPage.test.tsx</files>
  <action>
Create `src/hooks/useProfile.test.ts`:
```typescript
import { describe, it } from 'vitest'

describe('useProfile', () => {
  it.todo('returns avatarUrl as null when no avatar is set')
  it.todo('returns tier from profiles row')
  it.todo('updateAvatar uploads file to storage and updates profiles.avatar_url')
  it.todo('updateAvatar throws if file exceeds 2 MB')
  it.todo('updateTier writes new tier to profiles row (dev only)')
})
```

Create `src/pages/SettingsPage.test.tsx`:
```typescript
import { describe, it } from 'vitest'

describe('SettingsPage', () => {
  it.todo('renders avatar upload section')
  it.todo('renders current tier display')
  it.todo('renders dev-only tier toggle when import.meta.env.DEV is true')
  it.todo('does not render dev-only tier toggle when import.meta.env.DEV is false')
  it.todo('tier toggle calls updateTier with opposite tier')
})
```
  </action>
  <verify>
    <automated>npm test -- --run useProfile SettingsPage</automated>
  </verify>
  <done>Both test files exist; npm test passes (stubs show as "todo", not failures)</done>
</task>

<task type="auto">
  <name>Task 2: Wave 0 stub — FeatureRequestModal test</name>
  <files>src/components/FeatureRequestModal.test.tsx</files>
  <action>
Create `src/components/FeatureRequestModal.test.tsx`:
```typescript
import { describe, it } from 'vitest'

describe('FeatureRequestModal', () => {
  it.todo('renders a textarea for feature request text')
  it.todo('disables submit button when text is fewer than 10 characters')
  it.todo('calls supabase insert with user_id and text on submit')
  it.todo('shows success state after successful submit')
  it.todo('shows loading state during submit')
})
```
  </action>
  <verify>
    <automated>npm test -- --run FeatureRequestModal</automated>
  </verify>
  <done>Test file exists; npm test passes (stubs show as "todo")</done>
</task>

</tasks>

<verification>
`npm test -- --run` passes with all existing tests green and new stubs shown as "todo" (not failing).
</verification>

<success_criteria>
- Three test stub files created with correct Wave 0 pattern (only vitest import, only it.todo)
- `npm test -- --run` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/08-profile-settings-content/08-02-SUMMARY.md`
</output>
